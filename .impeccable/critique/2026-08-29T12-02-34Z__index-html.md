---
target: the assembled surface (closing round)
total_score: 36
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
timestamp: 2026-08-29T12-02-34Z
slug: index-html
---
⚠️ DEGRADED: single-context (no sub-agent/Task tool exposed in this session — same harness constraint as the prior round). The reference's sequential fallback was applied: Assessment A was fully recorded and timestamped (/tmp/critique-closing-assessment-a.md) BEFORE any detector or test output entered the synthesis context. No real browser exists in this harness; no visual verification is claimed anywhere in this critique.

# Closing Critique — The Disappearing Draft, assembled surface (2026-08-29, post-R1–R4)

- **Target (user phrasing):** the assembled surface · **Resolved:** `index.html` (mount shell; screens swap inside `#app`)
- **Round:** closing re-critique confirming the 2026-08-29T10-16-58Z snapshot's findings landed (F1–F4 + P3), judging the two routed crumbs, and issuing the material-issues verdict.
- **Artifacts reviewed:** index.html + direction contract (seed da64ca22), src/styles/{tokens,main,plates,write,outcome,archive,setup}.css, src/ui/{router,write-surface,outcome-screens,archive-screen}.ts, src/data/last-config.ts, DESIGN.md, README.md, docs/ultron/state.md.
- **Assessment order:** A (design review) recorded → B (detector + suite) run after. Detector findings never anchored A.

---

## Design Health Score (closing)

| # | Heuristic | Prior | Score | Key issue |
|---|-----------|-------|-------|-----------|
| 1 | Visibility of System Status | 4 | 4 | Lamps, board, transient plate states, persistent regions; the new guards SPEAK (hold + armed-delete both announced with recoveries) |
| 2 | Match System / Real World | 3 | 3 | MASTER ARM / T– / FADE/LOSS / Zulu dialect genre-coherent; still a translation layer for non-aviation writers |
| 3 | User Control and Freedom | 2 | 3 | F2+F4 closed the named gaps (recall, confirmed delete); RECONFIGURE adds an exit; remaining no-undo is intentional product permanence |
| 4 | Consistency and Standards | 3 | 4 | Setup outlier eliminated; one plate vocabulary tokenized (--focus-width) + shared-layer (plates.css); five screens, one grammar |
| 5 | Error Prevention | 3 | 4 | All three irreversible moments guarded: cover ritual (arm), two-step DELETE, DONE keystroke hold; validation disables ARM with the exact bound spoken |
| 6 | Recognition Rather Than Recall | 3 | 4 | Designed grouping on setup; limits printed on placards; RE-ARM carries its calibration on its face |
| 7 | Flexibility and Efficiency | 3 | 3 | One-motion restart + keyboard-complete cover are real accelerators; deliberately nothing deeper at scope |
| 8 | Aesthetic and Minimalist Design | 3 | 4 | Front door in-world; every setup element traces to a contract promise (needs-browser-verification) |
| 9 | Error Recovery | 4 | 4 | Every failure names the next move; both new guards announce both recoveries |
| 10 | Help and Documentation | 3 | 3 | Self-explaining by design + README (keys, ephemerality, residual risk); no in-product tour by rule |
| **Total** | | **31** | **36/40** | **Excellent (36–40), lower edge — rests on source discipline + the 218/218 record, not pixels** |

Applicable maximum 40 — no heuristics scored n/a. Movement vs prior: +1 each on 3, 4, 5, 6, 8 — exactly the heuristics the four refinements targeted (prior action summary predicted 33–36; landed at the band's top).

## Prior-finding resolution (all verified at source)

- **F1 [was P1] setup console unstyled** — RESOLVED. setup.css consumes every committed T7 hook (placard / placard-name / placard-limits); the duration board reuses write.css's .flap-board/.flap-slot classes (no fork — verified in both CSS and the JS factory), fixed 3-minute wells (zero layout shift, role="img" plain-language label, dashed wells on invalid); `--dur-arm` is spent on the MASTER ARM cover hinge; validation = amber field edge + reserved one-line status placard + role="status", ARM disabling never shifts the station.
- **F2 [was P2] two-motion amnesiac re-arm** — RESOLVED. `reArm()` → last-config recall → `startSession` directly: one motion, fresh controller + editor, editor focused; versioned own-key store (`the-disappearing-draft:last-config`), in-memory authoritative, corrupt payloads ignored-not-quarantined; the plate prints the calibration from the same read the restart consumes (can never promise what it won't deliver); RECONFIGURE secondary keeps setup reachable; null-config falls back to setup.
- **F3 [was P2] disarm keystroke yank onto DONE** — RESOLVED by mechanism (a): keyboard activation held 1000ms (Enter/Space keydown+keyup preventDefault, detail-0 synthesized clicks swallowed), pointer clicks always pass, fresh focus lifts the hold, one honest spoken hold per window; T12 focus map untouched and test-pinned.
- **F4 [was P2] unguarded single-entry DELETE** — RESOLVED: DELETE arms → CONFIRM DELETE in destruct red + spoken caution (title, no-recovery, Escape); second press performs; 5s lapse auto-reverts the safe direction; Escape anywhere on the binder; one confirm live at a time; same-log-position focus continuation preserved.
- **P3 seam list** — RESOLVED: plates.css shared layer (first-imported, cascade preserved, test-pinned); `--focus-width` at all six focus sites, zero literals; theme-color + inline-SVG favicon in committed palette (test-asserted); README names both storage keys + accurate 218 count; RE-ARM 320px min() measure with the type-scale math documented in-rule; whitespace-only flex children removed.

## The two routed crumbs (judged)

1. **`.archive-body` wrapper — POLISH-TIER, not material.** A semantic wrapper with no CSS rule anywhere (grep-verified). It is load-bearing-by-accident: removing it would newly apply the `.archive` flex gap between binder children and change spacing. Zero user-visible effect today; the crumb is brittleness (spacing silently depends on it being an unstyled block), not a defect. Disposition: keep + document (or give it an explicit inert rule) in the closing document step.
2. **confirm-settle opacity — POLISH-TIER, not material.** The clear-all panel's entrance animates opacity 0→1 (+4px translateY) against DESIGN.md's own "don't animate opacity as a state transition". Mitigations: it is not a lamp (the rule's core intent — never fade a lamp's meaning — holds), and the duration token collapses under reduced motion. It remains the room's only opacity-entrance. Disposition: two-line fix (drop the opacity keyframes, keep the transform-only settle) — safe to fold into any future touch; not worth a dedicated pass.

## New findings (severity-ranked; nothing above P3)

- **[P3] Duration group = 5 visible options** (the routed ≤4 working-memory question): 3/5/10/15 + CUSTOM. Boundary-adjacent, not overloaded — printed values make it recognition, CUSTOM reads as a mode rather than a peer value, and the placard row wraps on phones. Recommendation: KEEP and record the disposition (the R1 verifier's reading); alternative would be dropping the 15-minute preset. Planning-owned.
- **[P3] Setup preselect on RECONFIGURE**: arriving via RECONFIGURE shows defaults (5/STANDARD) even when the RE-ARM plate just printed a different calibration — a small discontinuity. Recommendation: preselect last-config on RECONFIGURE arrival only (boot stays defaults). Product thought; not material.
- **[P3] Stale comment**: outcome-screens.ts module header still credits archive.css for `.plate` (plates.css since R4). Comment-only.

## Design specificity verdict
AUTHORED, all five phases — the specificity hole is closed. The first viewport is now the instrument room: a presiding flap board echoing the chosen length, switch-plate placards printing real FADE/LOSS numbers, and the room's one energized action behind a physical cover whose lift is the gesture `--dur-arm` was minted for. The restart loop re-enters the room at its lowest emotional moment through a focused plate that carries its own calibration. No unrelated product could ship this unchanged.

## Overall impression
The room has its front door. Four-fifths of an authored instrument world a day ago; one whole room now — setup, write, both outcomes, and the log sharing one vocabulary, with every irreversible gesture guarded in the world's own grammar (a cover, a second step, a held beat) rather than by generic confirmation dialogs. Biggest remaining opportunity is verification, not construction: the owed real-browser capture session (first viewport, cover hinge, RE-ARM at 320px, DSEG14 line-fit) is the only thing between this and a signed finish.

## What's working
1. The guarded ARM ritual — two physical steps for the one consequential action, keyboard-complete, Escape-capable, and engineered so the deliberate RE-ARM/RECONFIGURE focus path lifts the cover by itself: the restart stays one motion without ever leaving ARM unguarded.
2. Recall honesty — the RE-ARM plate prints from the same store the restart consumes; corrupt payloads degrade to the setup console, never to a wrong promise.
3. Guards that speak — DONE's hold and DELETE's arm are announced through polite regions with recovery paths named; the console's no-silent-state discipline survived the hardening pass.

## Persona red flags (closing)
- **Alex (Power User):** restart = one Enter on the focused plate; nothing material remains.
- **Jordan (First-Timer):** first viewport explains itself — board, printed limits, guarded ARM, "NO PAUSE — NO RECOVERY" printed at the station.
- **Sam (Accessibility):** tokenized 2px focus rings everywhere incl. placard switch plates (:has on the covering radio); both guards are spoken, never color- or silence-alone; needs-browser-verification: :has() ring + cover lift under real AT.
- **Riley (Stress Tester):** armed DELETE auto-reverts the safe way; interrupted flips settle instantly; corrupt config degrades cleanly; nothing material remains at source.

## Minor observations
- needs-browser-verification (visual-only, NOT claimed as seen): setup first-viewport composition + flip-on-selection; the 260ms cover hinge; RE-ARM plate at 320px; DSEG14 one-line fit; grain vs banding; :has() focus rendering. These fold into the already-owed R1 human capture session.
- Routed elsewhere (NOT this round): the `.impeccable/design.json` sidecar refresh belongs to the closing document step per the coordinator's routing.

## Questions to consider
- Does RECONFIGURE owe the writer their last calibration as a starting point, or is a fresh slate the kinder ritual after a deletion?
- Is the fifth duration option (CUSTOM) a mode worth its extra slot, or would four total read calmer?

## Verdict — material issues open?
**NO.** Zero P0/P1/P2 findings. All four prior findings (F1–F4) verified resolved at source; the P3 seam list closed; both routed crumbs judged polish-tier; the three new observations are P3 product/documentation thoughts, none blocking. The surface is materially done at source level; what remains is the owed human browser-capture session and the closing document step (design.json sidecar refresh), both already routed.

Questions skipped: auto-mode closing subagent (this run reports to the orchestrator; the two product questions above are recorded as dispositions for the closing document step, per the prior round's simulated-proxy pattern).

## Run Notes
- Target slug: `index-html` (second run for this slug; trend available post-write).
- Ignore list: `.impeccable/critique/ignore.md` does not exist; nothing dropped.
- Assessment independence: sequential single-context fallback (no sub-agent tool exposed); A recorded and timestamped before B ran — declared in the header, not silent.
- CLI detector: two real attempts — (1) degraded regex fallback (htmlparser2/css-select/domutils absent from the resolution path), (2) full engine after provisioning the four parser modules via temp symlinks on the script's ESM resolution path (temp dir + symlinks removed after): `index.html` → `[]` exit 0; `dist/index.html` → `[]` exit 0.
- Coverage caveat (standing): the static detector sees only the mount shell — screens are TS-generated. Substitute signals: Assessment A's source read + the full suite rerun this round: **218/218 passed** (11 files), including computed-contrast and axe-clean a11y tests.
- Browser visualization: skipped — no real browser in this harness (coordinator-declared constraint, same as the prior round); no overlay claims. Fallback: source review + test record.
- Live server: never started. Temp files: /tmp/impeccable-detect-deps + skill-scripts node_modules symlinks (removed post-scan), /tmp/critique-closing-assessment-a.md + /tmp/critique-closing-body.md (removed at run close).
