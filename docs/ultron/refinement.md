# Refinement Checklist — Finishing Phase (auto mode)

Source: critique snapshot `.impeccable/critique/2026-08-29T10-16-58Z__index-html.md` (both raw assessments + synthesis; closing questions answered by simulated-user proxy, recorded "simulated (auto mode)" in the snapshot). Scores: 31/40. This checklist references the snapshot; it does not duplicate it.

| # | Command | Finding addressed | Status | Simulated answers / evidence |
|---|---------|-------------------|--------|------------------------------|
| 1 | `$impeccable shape` — build the setup console to the FIRST VIEWPORT contract: flap duration board, placard grid, guarded ARM (`--dur-arm`), plates + focus-green, ≤4 duration grouping | F1 [P1]: setup console functionally complete but visually unstyled; no sheet consumes placard hooks; ARM/VIEW ARCHIVE are UA buttons | done — auto-approved (ultron-supreme) | **Built.** New `src/styles/setup.css` (wired via main.css): the flap duration board on write.css's own classes with a FIXED 3-minute-well field (1–120, zero layout shift; digits flip on selection through the same capability gates, `role="img"` plain-language label); placards rebuilt on the committed `placard`/`placard-name`/`placard-limits` hooks as pressed-in switch plates (native radio = invisible full-placard hit target; focus = 2px focus-green on the placard); guarded ARM = MASTER ARM station low + isolated (margin-top:auto): LIFT COVER (hinges up over `--dur-arm`, leaves the tab order) → ARM (the one primary plate) — one motion, two steps, keyboard-complete (Enter/Escape), direct-ARM seam preserved for the RE-ARM one-motion loop (deliberate focus lifts the cover itself); VIEW ARCHIVE = standard secondary plate; validation hold = amber line (one reserved line, no shift) + amber hairline on the field. Tests 185→190 (5 new R1 structural: board echo/flips/timers-destroyed, reduced-motion static, guard open/Escape/direct-press, hooks consumed + all buttons plates, sheet-wiring + token pins incl. `--dur-arm` spend + no `.flap-*` fork); contrast.test extended (setup.css sheet + focus pin); T7/T12 behavior seams intact. Detector full-engine clean (parsers provisioned temp, removed). Duration grouping kept at 4 presets + CUSTOM = the entry's bound met with equality (critique's stricter ≤4-total reading would change planning-owned `DURATION_PRESET_MINUTES` — routed to coordinator, logged; verifier judgment: honors the intent — 4 numeric plates as one instrument row + CUSTOM as a fielded placard is limited grouping, not a long menu; stricter reading stays open at the closing re-critique). DESIGN.md setup sections rewritten from the built world; design.json +2 components. **Finish-reviewer disposition: SHIP** (in-thread substitution — no agent tool/CLI in this harness; review from code + DESIGN.md, zero visual claims). Craft feedback, non-blocking: title+intro precede the board (confirm board still presides in the owed first-viewport capture); Escape discoverability on the cover; `.plate` cross-sheet dependency now also in setup.css (polish entry #4 owns). Owed: real-browser setup first-viewport capture (no browser in harness). **Verifier PASS 2026-08-29** (independent rerun: 190/190, strict build green, dist wiring + markers, hooks/`--dur-arm` spend, plate classes, T7 seams, no-flap-fork both ways, detector `[]` clean in plain + full-engine modes, mtime inventory exact) — evidence: docs/ultron/production-log.md (Refinement R1 verifier block) | proxy: setup-first priority, full scope, tone unchanged |
| 2 | `$impeccable distill` — one-motion restart: RE-ARM goes straight to a re-armed session recalling last config | F2 [P2]: RE-ARM is two motions through amnesiac setup (config resets) — breaks "instant, shame-free restart" | done — auto-approved (ultron-supreme) | **Distilled.** RE-ARM on the deleted screen now starts the NEXT session IMMEDIATELY on the last-used duration + difficulty — one motion (Enter on the focused plate), zero re-configuration: fresh controller + fresh editor (T4 seam, never reused), threat from the top, editor focused exactly like ARM. Recall mechanism = new small module `src/data/last-config.ts` (remember-on-every-startSession / recall; in-memory per page-load is authoritative, persisted best-effort under its OWN versioned key `the-disappearing-draft:last-config` — never the archive envelope, config is not user content; corrupt/foreign payloads ignored-and-warned, never quarantined; write failures swallowed so the mercy moment never depends on storage health). No-surprise rule honored ON THE PLATE, pre-session not HUD (write surface stays time-only): `RE-ARM — 7 MIN · GENTLE` via exported `reArmPlateLabel`, printed from the SAME recall read the restart consumes (no divergence window — nothing can arm on the deleted board). Setup remains the FULL configuration surface, nothing removed, and stays reachable with no dead ends: new standard secondary plate RECONFIGURE on the deleted board → setup with ARM focused (the deliberate focus lifts the MASTER ARM cover), plus the pre-existing wind-down DONE → archive → NEW SESSION and boot paths — pinned by test (a completely different config arms and survives from RECONFIGURE). Tests 190→206 (+12 store unit: round-trip/copy/newest-wins, own-key versioned envelope + fresh-store cross-load read, swallowed write failure, 8 corrupt/foreign payload cases, hostile adapter; +4 router walkthrough: RE-ARM lands in a FRESH armed session asserting countdown paint = last duration ('7:00'), preset discrimination via fade math (0.5 @20s idle — only GENTLE can paint it; STANDARD/BRUTAL would be deleted), fresh-editor T4 seam, zero draft-writes (config key only, archive envelope untouched/null), loop repeatable; newest-arm-wins mid-loop; RECONFIGURE no-dead-ends full re-config; corrupt-config immunity via in-memory record; persistence envelope shape + fresh-router-over-same-storage; storage-spy tests re-based to key-filtered draft assertions). Detector `[]` clean (full-engine path) on last-config.ts, router.ts, outcome-screens.ts, outcome.css. DESIGN.md updated where the world's text described the old flow (Overview "re-arm is one motion straight into the next session on the last-used calibration"; instrument-readout section: two plates + recall-store sentence; Navigation focus map: editor again on RE-ARM; setup-console section: the cover-lift now credits RECONFIGURE/NEW SESSION); outcome.css's "the one action" comment corrected. `npm run test` 206/206 + `npm run build` strict-green (css 26.78 kB unchanged / js 49.00 kB). **Finish-reviewer disposition: SHIP** (in-thread substitution — no agent tool/CLI in this harness; review from code + DESIGN.md, zero visual claims). Non-blocking craft feedback: (a) 3-digit custom minutes push the plate to 27 chars ("RE-ARM — 120 MIN · STANDARD") vs the ~24-char placard guideline — flex-wrap keeps it from overflowing; check in the owed real-browser capture; (b) the persisted config READ is unreachable in today's UI (a deleted board always follows an arm on the same load, so memory always serves) — kept per the entry's explicit persistence allowance (one ~40-byte best-effort write per ARM); a future setup-preselect would exercise the read; (c) logged follow-ups: setup still boots defaults on RECONFIGURE (possible preselect later, routed to the closing re-critique), README one-liner naming the second localStorage key (polish #4 territory). **Verifier PASS 2026-08-29** (independent rerun: 206/206, strict build green css 26.78/js 49.00 kB, one-motion verified from source incl. re-computed GENTLE-only fade math, recall store draft-proof by API shape + key-filtered storage assertions, setup no-dead-ends, HUD time-only grep-proven, detector [] clean on last-config.ts/router.ts/outcome-screens.ts + outcome.css, dist markers survive, DESIGN.md four passages match, mtime inventory exact) — evidence: docs/ultron/production-log.md (Refinement R2 verifier block) | proxy: restart-first calibration, tone unchanged |
| 3 | `$impeccable harden` — DONE keystroke guard after disarm (mid-sentence Space/Enter must not prematurely finalize); inline confirm/undo on single-entry archive DELETE | F3 [P2]: disarm yanks focus to DONE — premature finalization risk. F4 [P2]: single DELETE immediate/unconfirmed while clear-all confirms | done — auto-approved (ultron-supreme) | **Hardened.** F3 — mechanism **(a) chosen and defended**: the disarm swap keeps DONE focused exactly as T12 pinned (focus map and its announcement/focus tests UNCHANGED and green), but DONE's KEYBOARD activation is held for `DONE_KEY_GUARD_MS = 1000` (inside the entry's 750–1500ms band; exported constant with the defense: longer than a typing burst's tail — 60–100 WPM inter-key intervals run 100–250ms — shorter than any deliberate pause; option (b) was rejected because moving the focus target would break the T12 a11y focus map to gain nothing (a) doesn't already give). While held: Enter/Space keydown AND keyup on the plate are `preventDefault`ed (activation dies before the synthesized click), and detail-0 clicks — exactly what browsers synthesize for Enter/Space activation — are swallowed with ONE honest spoken hold per window ('DONE holds keypresses for a moment after disarm — click now, or press again shortly.', the wind-down polite region); pointer clicks (detail ≥ 1) always finalize. A FRESH focus on DONE (Tab away and back, SR navigation — the disarm focus itself is flagged and consumed) lifts the hold immediately = deliberate intent; the hold also expires on its own. Extra guard pinned: Enter inside the wind-down textarea NEVER activates DONE even outside the window — no `<form>` exists anywhere on the console (grep-verified; buttons are `type="button"` by construction) and a new test pins both structure (`closest('form')` null) and behavior (Enter in the draft leaves the phase at wind-down). F4 — the **confirm pattern** (entry's stated default; undo rejected as heavier and inconsistent): single DELETE is now a two-step plate — first press ARMS in place (the plate morphs to **CONFIRM DELETE** in the destruct red via the existing `.plate-destruct`, `data-armed` hook, the caution SPOKEN through the polite region naming the entry title + 'no recovery' + 'Escape cancels'), second press on the same native button performs it. Escape anywhere on the binder cancels (focus stays on the plate — it reverts to DELETE under the cursor); a 5s lapse auto-reverts (`DELETE_ARM_TIMEOUT_MS`, injectable via a new optional `timer` seam on `ArchiveScreenDeps`, wired from the router's own seam) — the SAFE direction, so no lingering armed destruct control can catch a stale keypress; one confirm is live at a time (arming another row disarms the first; opening CLEAR LOG's panel cancels a row arm); `renderLog` clears the arm (rows rebuild unarmed). T11's same-log-position focus continuation PRESERVED and re-pinned (next entry's DELETE else previous else NEW SESSION on empty). Honest deviation from the entry's 'amber-caution/red-confirm' phrasing, recorded in DESIGN.md: the amber caution BAND is not replicated per row (DESIGN.md reserves the one raised caution panel for the multi-entry clear decision; the Locked Lamp / hue-rarity rules argue against scattering amber) — the caution beat is the first press + the spoken region, the red is the confirm control, exactly CLEAR LOG's vocabulary scaled to one row. Tests 206→**214** (+8: 4 router guard tests — hold covers Enter/' '/legacy 'Spacebar' keydown+keyup with defaultPrevented, detail-0 click swallowed + held-announcement once + pointer click finalizes inside the window; hold expires after the window; fresh focus lifts it; Enter-in-editor never finalizes + no form + all buttons type=button. 4 archive two-step tests — arm/confirm with red plate + spoken caution + focus kept on the plate + remaining plates revert; Escape cancels; lapse auto-reverts then re-arms cleanly; one-confirm-live incl. CLEAR LOG interplay). Updated existing: the T11 single-delete walkthrough now drives arm-then-confirm (continuation assertions unchanged); 8 DONE-click sites across router/a11y tests now spend the deliberate beat (`passDoneGuard` helper) or use the fresh-focus path (the T12 keyboard walkthrough now exercises Tab-away-and-back as the intent signal; the announcements test uses fresh-focus to keep its absolute second-disarm time exact); the two 'live loop fully down' timers assertions now spend the beat first (the ONE pending timer at disarm is the guard — threat engine still down at the swap itself, commented in both tests). Zero production CSS changes (css 26.78 kB byte-identical; js 49.00→50.32 kB). `npm run test` **214/214** + `npm run build` strict-green; detector `[]` exit 0 on outcome-screens.ts, archive-screen.ts, router.ts; dist markers survive minification (CONFIRM DELETE ×2, both announcements). DESIGN.md updated in three passages (Primary plate + Destruct plate list CONFIRM DELETE, The log two-step paragraph with the amber-deviation rationale, Navigation disarm-guard clause). **Finish-reviewer disposition: SHIP** (disclosed in-thread substitution — no agent tool/CLI in this harness; code + DESIGN.md reasoning, zero visual claims; non-blocking feedback: detail-0 swallow also holds AT-synthesized clicks inside the 1s window — covered by focus-lift on deliberate navigation; 'press DONE to retry' vs the 1s hold on the quota-failure path — honest hold announcement covers it). No new browser-verifiable visuals added (plate morph reuses existing classes) — nothing new owed to the R1 capture session beyond what it already owes. **Verifier PASS 2026-08-29** (independent rerun: 214/214, strict build green css 26.78 / js 50.32 kB, guard verified from source — hold scoped to the 1000ms window with pointer clicks structurally unaffected and fresh-focus lift with no clock advance, no-form pinned structurally at three sites + behaviorally outside the window, two-step verified from source with the arm state as the id and the existing plate-destruct red + spoken caution + focus continuation re-pinned, T12 focus map asserted not relaxed with axe clean over the guarded wind-down, both non-blocking calls judged non-traps — bounded ≤1s, announced with recoveries named, no data-loss path in either overlap; detector [] exit 0 on the three changed production files, dist markers counted incl. CONFIRM DELETE ×2, mtime window = exactly the logged set with zero CSS files) — evidence: docs/ultron/production-log.md (Refinement R3 verifier block). Follow-up noted for the closing document refresh: .impeccable/design.json sidecar predates R3 (no CONFIRM DELETE two-step / disarm guard entries yet). | proxy: guard rails after shape/distill, tone unchanged |
| 4 | `$impeccable polish` — cross-sheet `.plate` dependency, `--focus-width` token (T11 follow-up), theme-color/favicon, stray text node, + R2-routed: README last-config key, RE-ARM 27-char plate at 320px | P3 seam list (polish-tier findings in snapshot) + R2 follow-ups | done — auto-approved (ultron-supreme) | **Polished.** Six routed items, all closed as token/relocation/addition moves — zero value redesigns. (1) `.plate` seam: the full plate vocabulary (base/primary/destruct/transient states + the ≤40rem tightening that had been styling other screens' plates from inside archive.css) moved to NEW `src/styles/plates.css`, imported FIRST among sheets in main.css — the exact cascade position archive.css held, so per-screen refinements (`.setup-arm-trigger`, `.arm-cover`) keep winning at equal specificity; same principle as the `.vh` precedent ("no screen depends on another's sheet"); import order + completeness + no-redefinition test-pinned. (2) `--focus-width: 2px` minted in tokens.css beside `--focus-color` (DESIGN.md's constant focus band) and consumed at ALL six sites — five `:focus-visible` rings across archive/outcome/setup (incl. the placard `:has` and custom-input rules) plus the editor's leading band (the 2.4.11 indicator); contrast pins updated to the token + zero `outline: 2px` literals. (3) `<meta name="theme-color" content="#14161a">` (exact `--ground`, test-cross-checked) + hand-authored inline-SVG favicon — the signature split-flap well (recessed bed, 1px hinge seam, bone digit, raised bezel, machined corners), every hex a tokens.css value (test-asserted), data-URI, no external asset. (4) Stray text node: the flagged setup one was already gone (R1 rebuild — verified); grep per the entry found the three remaining whitespace-only flex children (outcome stats ×2, reduced-motion caution count) — dead per css-flexbox §4, removed, zero visual change. (5) README names both storage keys exactly (`…:archive`, `…:last-config`) with the routed framing (config ≠ user content; clearing resets recalled calibration); stale suite count 185→218 fixed in passing. (6) RE-ARM worst case ("RE-ARM — 120 MIN · STANDARD", 27 chars) gets a static safe measure scoped to `.outcome-deleted .plate-primary`: computed Michroma math (≈20.4em ≈ 204px at 10px) vs the 320px bed budget (66px chrome → 254px room, ≈20% slack) documented in-rule; `min(var(--size-placard-sm), calc((100vw - 66px)/22))` makes the fit structural; no `nowrap` anywhere so wrap remains the backstop — the plate cannot overflow; the detector's advisory on the initial 8px clamp floor was RESOLVED (floor removed; the ramp's 10px is the only size claimed) and the re-run is clean. Tests 214→**218** (+4 behavior-asserting: focus-token pin, plates-layer pin, two shell tests) + `npm run build` strict-green (css 27.01 kB / js 50.30 kB). Detector FULL-ENGINE `[]` exit 0 on all ten changed production targets (parsers provisioned temp on the engine's ESM path, removed after — same protocol as the critique's run); first plain pass had run DEGRADED (regex mode) and surfaced the 8px advisory honestly. Dist verified: theme-color + favicon + contract seed + all ring sites consuming the token + the min() rule survive minification. **Finish-reviewer disposition: SHIP** (disclosed in-thread substitution — no agent tool/CLI in this harness; code + DESIGN.md reasoning, zero visual claims). Owed (folds into R1's human capture session): favicon at 16px, theme-color in a real tab strip, RE-ARM plate at 320px; Michroma advances reasoned per class, not measured from the binary (mitigated by the padded divisor + wrap backstop). Out of scope, routed to #5: `.archive-body` wrapper + confirm-settle opacity crumbs (not in this entry's list), design.json sidecar refresh. **Verifier PASS 2026-08-29** (independent rerun: 218/218, strict build green css 27.01/js 50.30 kB with both deltas consistent with the claimed additions/removals; plates import order verified in-sheet + test-pinned with the CASCADE claim independently re-reasoned through every specificity interaction incl. the outcome.css order flip that flips nothing; token consumed at all six source sites and one-for-one in dist with zero 2px literals on either side; theme-color + four-rect favicon decoded with every hex matched to a token and the test cross-checks confirmed genuine; README keys exact-string-equal to the code constants; RE-ARM math re-computed at 320px — 66px chrome/254px room/204px label ≈19.7% slack, min() token-winning at all supported widths, no nowrap on any plate path, no clamp floor; mtime window exactly the logged 14 files with DESIGN.md untouched; detector full-engine [] exit 0 on all ten targets with temp parsers removed after) — evidence: docs/ultron/production-log.md (Refinement R4 verifier block) | proxy: seam list then closing re-critique, tone unchanged |
| 5 | Closing `$impeccable critique` re-run after fixes (expect 33–36 band) | verification of 1–4 | done — auto-approved (ultron-supreme) | **Re-critiqued + documents closed.** Verdict: **36/40 Excellent (lower edge of the 36–40 band — the prior action summary's predicted band, landed at its top)** vs prior 31/40, same slug `index-html`, trend 31 → 36; +1 each on heuristics 3/4/5/6/8 — exactly the set the four refinements targeted. All prior findings verified RESOLVED at source: F1 setup styled (setup.css consumes every committed T7 hook, duration board reuses write.css's flap classes, `--dur-arm` spent on the MASTER ARM cover hinge), F2 one-motion RE-ARM + last-config recall (own versioned key, plate prints the same read the restart consumes, RECONFIGURE keeps setup reachable), F3 DONE 1000ms keystroke guard (keyboard-only hold, pointer always passes, fresh focus lifts), F4 two-step DELETE (armed CONFIRM DELETE in destruct red, spoken caution, Escape + 5s lapse, one-confirm-live); P3 seam list closed (plates.css shared layer, `--focus-width` at all six sites, theme-color + inline-SVG favicon, README keys, RE-ARM 320px min() measure, whitespace flex children). Both routed crumbs judged POLISH-TIER not material (.archive-body wrapper = keep + document; confirm-settle opacity = two-line transform-only fix for any future touch). New findings all P3, none blocking: duration 5-option disposition → KEEP (planning-owned; the R1 verifier's reading), setup-preselect-on-RECONFIGURE recommended (product thought), stale archive.css credit in outcome-screens.ts header (comment-only). **MATERIAL-ISSUES VERDICT: NO (zero P0/P1/P2).** Detector full-engine `[]` clean on index.html + dist/index.html; suite rerun 218/218. Snapshot: `.impeccable/critique/2026-08-29T12-02-34Z__index-html.md`. Closing document refresh (this entry's second half) COMPLETE: DESIGN.md refresh pass from the current built world (nine refinement facts documented as the system — setup console + guarded MASTER ARM, plates layer in the lineage/components structure, `--focus-width` named as the one focus band, one-motion RE-ARM + recall, DONE guard, two-step DELETE with the 5s lapse, theme-color + favicon, RE-ARM min() measure; no contradictions remain; documented deviations preserved: latching annunciator, amber-band-per-row omission) + `.impeccable/design.json` regenerated from it (schemaVersion 2; stale `--dur-arm` "awaiting styling" purpose replaced; ARM-station focus path corrected to RECONFIGURE/NEW SESSION; narrative re-pulled verbatim — validated against DESIGN.md; 218/218 suite rerun green as a guard). | proxy: closing re-critique, tone unchanged |

Loop: entries execute one at a time, in priority order; each gets a worker with full UI build discipline (session setup if needed, craft floor before editing, shipped finish reviewer at completion) and an independent verifier pass in auto mode. Status values: queued / in-progress / done / skipped.
