# Production Log — The Disappearing Draft

Evidence trail for production tasks and research dispositions. Workers and verifiers append here; each entry records task ID, outcome, validation evidence, and approval state. Coordinator: ultron-supreme.

## Research dispositions

✅ R1 plural-event, phase-checked Date.now()-anchored reconcile() with rAF foreground tick and boundary setTimeout fallback — committed (evidence: research/R1-hidden-tab-throttling.md) — auto-approved (ultron-supreme) 2026-08-28
✅ R2 DOM split-flap with CSS 3D rotateX flaps, @supports fallback, role="timer" + aria-atomic a11y pairing — committed (evidence: research/R2-splitflap-technique.md) — auto-approved (ultron-supreme) 2026-08-28
✅ R3 layered deletion: blur → value/defaultValue scrub → editor node replacement + historyUndo/historyRedo interceptor — committed (evidence: research/R3-undo-defeat.md) — auto-approved (ultron-supreme) 2026-08-28
✅ R4 typefaces Michroma 400 / B612 Mono 400+700 / Source Sans 3 VF (~69 KB woff2, all OFL) — committed (evidence: research/R4-typefaces.md) — auto-approved (ultron-supreme) 2026-08-28

## Task evidence

✅ T1 Scaffold the workspace — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files (all new): `package.json`, `tsconfig.json`, `vite.config.ts` (doubles as vitest config), `index.html`, `src/main.ts`, `src/styles/main.css`, `tests/smoke.test.ts`, `.gitignore`.
- Stack: Vite 8.2.2 + TypeScript 7.0.2 (strict + `noUncheckedIndexedAccess`/`noUnusedLocals`/`noUnusedParameters`) + Vitest 4.1.11; Node 24.8.0, npm 11.18.0. Scripts exactly `dev`/`build`/`test`/`preview`; `build` = `tsc --noEmit && vite build` so typecheck gates every build.
- Conventions established: ESM single-quoted TS, small modules under `src/` (`engine/`, `data/`, `ui/` per plan), plain CSS in `src/styles/` (tokens.css arrives T8; main.css holds a minimal reset + placeholder charcoal/bone ground), tests in `tests/` or colocated `*.test.ts` (both wired into vitest `include`), test environment `node` for now (DOM-needing tasks add jsdom in their own scope), `#app` mount verified fail-fast in `src/main.ts` with no screens rendered (T5's router owns mounting). `index.html` body kept to `#app` + module script so T8's direction contract can be first child.
- Validation evidence: `npm install` clean (17 funding packages, 0 vulnerabilities); `npm run build` green — tsc strict noEmit passed, vite 8 built `dist/index.html` 0.40 kB + assets css 0.13 kB / js 0.77 kB (328 ms); `npm run test` green — 1 file, 1 test passed (tests/smoke.test.ts, 113 ms); `npm run dev -- --port 5199` smoke — `curl /` → HTTP 200 serving the shell (`#app` mount + `/src/main.ts` module script present), `/src/main.ts` transform → HTTP 200; server stopped after check.
- Deviations: none. Added `src/styles/main.css` (not in the task file list) to make the plain-CSS convention concrete and the served shell non-blank; ~12 lines, replaced by T8 token layer.
- Follow-ups: T2 + T6 may start immediately (deps: T1); DOM-test tasks (T3/T4) add a jsdom dependency when reached.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8.2.2 built `dist/index.html` 0.40 kB + assets css 0.13 kB / js 0.77 kB; `npm run test` → green — 1 file, 1 test passed (`tests/smoke.test.ts`); `npm run dev -- --port 5217 --strictPort` + curl smoke → `GET /` HTTP 200 serving the shell (`id="app"` and `/src/main.ts` module script both present in served HTML), `GET /src/main.ts` HTTP 200 (transformed module with CSS import served); server stopped after check.
  - Criteria verified from source: `tsconfig.json` has `"strict": true` (plus `noUncheckedIndexedAccess`/`noUnusedLocals`/`noUnusedParameters`); Vitest present (`vitest ^4.1.11` devDependency, `test` = `vitest run`, config via `vite.config.ts` test block); scripts `dev`/`build`/`test`/`preview` all defined in `package.json`; `.gitignore` covers `node_modules/`, `dist/`, `*.local`, `.DS_Store`, `npm-debug.log*` — sane; file inventory exactly scaffold scope (`package.json`, `package-lock.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `src/main.ts`, `tests/smoke.test.ts`, `.gitignore`, gitignored `dist/`, pre-existing `PRODUCT.md`) + the one logged deviation `src/styles/main.css` — trivial (~22-line reset + placeholder charcoal/bone ground) and noted → acceptable; no stray files.
  - T1 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above).

✅ T2 Pure timing state machine — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files (all new): `src/engine/timing.ts`, `src/engine/timing.test.ts` (colocated; both paths wired into vitest include per T1 conventions). No DOM, no timers, no mutable state — pure functions only, controller (T3) consumes.
- Exported API: `deriveTimingState(now, lastInputAt, fadeDelayMs, deleteThresholdMs, sessionEndAt)` → `{ phase: 'pre-arm'|'armed'|'fading'|'deleted'|'survived', opacity: 0..1, remainingMs, deletesInMs }` (idempotent, exactly the recompute R1's reconcile() calls for); `PRESETS` (GENTLE 10s/30s, STANDARD 5s/10s, BRUTAL 3s/6s — planning-decided values, `as const satisfies Record<string, TimingConfig>`); `DURATION_PRESET_MINUTES` = [3,5,10,15]; `MIN_SESSION_MINUTES` 1 / `MAX_SESSION_MINUTES` 120; `isValidSessionMinutes`; `sessionEndAt(armedAt, minutes)` (throws RangeError out-of-range); `MS_PER_MINUTE`; types `TimingPhase`/`TimingConfig`/`TimingState`/`PresetId`. Invalid timing config (fade ≥ delete, negative, non-finite) → RangeError.
- Load-bearing semantics decisions (documented in module docstring): (1) deletion-vs-disarm collisions resolved CHRONOLOGICALLY — deletion wins only when its wall-clock boundary `lastInputAt + deleteThresholdMs` is strictly before `sessionEndAt`; exact tie or session-first → `survived`. This is "disarm-at-zero precedence over deletion" AND makes R1's single hidden-tab-overshoot recompute historically correct (stop early → stayed deleted; stop late → disarm wins). (2) `survived` restores opacity to 1 (wind-down text fully legible; monotonic decay holds only within the live threat window, which the tests assert precisely). (3) `deleted`/`survived` are stable in pure re-evaluations — terminal-ness and single-emission are T3's job per R1. (4) `remainingMs` = session clock (0 when pre-arm/ended); `deletesInMs` = threat clock.
- Deviations: (a) added `deletesInMs` field beyond the plan's `{phase, opacity, remainingMs}` — T9's reduced-motion path needs a numeric inactivity countdown and this keeps T3/T9 from re-deriving math; (b) property tests use a seeded mulberry32 PRNG (deterministic seeds 0xc0ffee/0xbadc0de/0x5eed/0xda64ca22) instead of adding fast-check — keeps T1's zero-extra-dep test stance; (c) `sessionEndAt` param is `number | null` (null → pre-arm) since the phase union requires expressing the unarmed state.
- Test coverage vs acceptance (all in `src/engine/timing.test.ts`): exact fade start (armed at fadeDelay−1, fading at exactly fadeDelay with opacity 1, per-preset parametrized); exact delete boundary (fading at threshold−1 with opacity >0, deleted at threshold); input reset (instant re-arm, opacity 1, both threat clocks restart from new input, session clock unaffected; + property "typing now ⇒ armed/1/full clock"); disarm-at-zero precedence (exact delete/disarm collision → survived); monotonic opacity decay (1ms unit scans + property non-increasing over threat window, strictly decreasing within fade); remaining-time math (per-phase unit + per-phase property consistency); property opacity never <0/>1 (2000 random sessions + per-sample in no-skip suite); no state skip (500 sessions × adaptive boundary sampling b−2..b+2 + midpoints; observed collapsed chain must equal the chronologically expected chain; terminal stability).
- Validation evidence: `npm run test` — 2 files, 26 tests passed (25 timing: 21 unit + 4 property, 388ms; smoke 1); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist unchanged 0.40 kB html / 0.13 css / 0.77 js — timing.ts not yet imported by main.ts; T3/T5 wire it).
- Follow-ups: T3 consumes `deriveTimingState` + config validation; T7 consumes `PRESETS`/`DURATION_PRESET_MINUTES`/`isValidSessionMinutes`/`sessionEndAt`; T9 consumes `opacity` + `deletesInMs` (reduced-motion numeric countdown).
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 2 files, 26/26 tests passed (25 timing: 21 unit + 4 property, 382 ms; smoke 1); `npm run build` → green — `tsc --noEmit` strict gate passed (strict + noUncheckedIndexedAccess/noUnusedLocals/noUnusedParameters confirmed in tsconfig.json), vite 8 built clean (dist 0.40 kB html / 0.13 css / 0.77 js — timing.ts not yet imported by main.ts, T3/T5 wire it).
  - Acceptance coverage verified from test source (`src/engine/timing.test.ts`), all eight areas genuinely tested with fake clocks (explicit `now`, no timers): exact fade start (armed at fadeDelay−1, fading at exactly fadeDelay with opacity 1 and fadeDelay+1 < 1, per-preset parametrized); exact delete boundary (fading at threshold−1 with opacity >0, deleted at threshold with opacity 0, per-preset); input reset (instant re-arm from mid-fade 0.8 → opacity 1, both threat clocks restart from new input, session clock unaffected, fade window restarts from new input instant; + 1000-case property); disarm-at-zero precedence (exact delete/disarm collision → survived, not deleted); monotonic opacity decay (1 ms unit scan over whole threat window, strictly decreasing per ms inside fade, chain armed→fading→deleted; disarm-instant scan; property non-increasing up to terminal); remaining-time math (per-phase unit + per-phase property); property opacity never <0/>1 (2000 random sessions + per-sample assertion inside no-skip suite); no state skip (500 sessions × adaptive boundary sampling b−2..b+2 + midpoints, observed collapsed chain must equal chronologically expected chain, terminal stability).
  - Purity verified from source (`src/engine/timing.ts`): grep for Date.now/performance.now/window/document/setTimeout/setInterval/requestAnimationFrame/Math.random → zero code hits (docstring mentions only); all exported functions pure over epoch-ms inputs; constants immutable (`as const`).
  - Preset table + constraints verified: GENTLE 10s/30s, STANDARD 5s/10s, BRUTAL 3s/6s (asserted in test), duration presets [3,5,10,15], custom minutes 1–120 enforced (`isValidSessionMinutes` finite-bounded; `sessionEndAt` throws RangeError out-of-range; invalid timing config fade ≥ delete / negative / non-finite → RangeError).
  - Tie semantics assessed: chronological strict-before rule (deletion only when `lastInputAt + deleteThresholdMs < sessionEndAt`; exact tie or session-first → survived) satisfies the plan's "disarm-at-zero precedence over deletion" and T3's "disarm at 0:00 stops all threat transitions" — no threat transition may occur at or after sessionEnd — while keeping R1's single hidden-tab-overstay recompute historically correct (delete boundary crossed while live stays deleted; crossed after disarm never deletes). Unit test pins the exact-tie instant to survived.
  - Logged deviations reviewed and accepted: (a) `deletesInMs` additive field — required downstream by T9's reduced-motion numeric inactivity countdown, additive to the contracted `{phase, opacity, remainingMs}` shape; (b) seeded mulberry32 PRNG (deterministic seeds) instead of fast-check — genuine randomized invariant tests, preserves T1's zero-extra-dep stance; (c) `sessionEndAt: number | null` — null encodes the plan's own `pre-arm` phase, which the contracted phase union requires expressing.
  - File inventory: only `src/engine/timing.ts` + `src/engine/timing.test.ts` new since T1; no stray files.
  - T2 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above).

✅ T6 Archive module — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files (all new): `src/data/archive.ts`, `src/data/archive.test.ts` (colocated per T1 conventions; both under vitest's `src/**/*.test.ts` include). No UI, no changes to existing files; `src/data/` lane directory created.
- Exported API: `createArchive(storage: StorageAdapter = localStorageAdapter())` → `{ list, save, update, remove, clear }`; types `ArchiveEntry` (`{ id, createdAt, endedAt, durationSec, preset, wordCount, title, excerpt, text }` — exactly the planning-decision shape), `SaveEntryInput`, `UpdateEntryPatch`, `ArchiveResult<T>` (`{ ok: true; value: T } | { ok: false; kind: 'quota-exceeded' | 'storage-error' | 'not-found'; cause? }`), `StorageAdapter` (injectable localStorage seam: `getItem`/`setItem`/`removeItem`); pure metadata derivation `deriveWordCount`/`deriveTitle`/`deriveExcerpt`/`deriveMetadata`; constants `ARCHIVE_STORAGE_KEY` ('the-disappearing-draft:archive'), `ARCHIVE_QUARANTINE_KEY`, `ARCHIVE_SCHEMA_VERSION` (1), `TITLE_MAX_WORDS` (6), `EXCERPT_MAX_CHARS` (160), `UNTITLED_SESSION_TITLE` ('Untitled session'); `localStorageAdapter()` (guarded for privacy modes that throw on bare `localStorage` access). `preset` reuses `PresetId` from `src/engine/timing.ts`.
- Load-bearing semantics decisions (documented in module docstring): (1) stored payload is a versioned envelope `{ version: 1, entries }`, stored pre-sorted newest-first; (2) corruption = unparseable JSON, wrong schema version, non-array entries, or any structurally invalid entry (full-payload quarantine, not per-entry filtering — a version bump/migration story is deliberately out of scope); (3) corrupt fallback is NON-DESTRUCTIVE: the raw bytes are preserved verbatim under `ARCHIVE_QUARANTINE_KEY`, the corrupt key is removed (warned once via console.warn), and the archive continues empty — recovery saves work immediately; (4) newest-first = endedAt desc, then createdAt desc, then id desc (deterministic ties); (5) quota detection covers Chrome/Safari `QuotaExceededError` (incl. legacy code 22) and Firefox `NS_ERROR_DOM_QUOTA_REACHED`; all storage write failures return typed results — no crash paths; (6) programmer errors (NaN/Infinity/negative durationSec, endedAt < createdAt, unknown preset, non-string text) throw RangeError/TypeError at the boundary, matching timing.ts's convention; (7) entry ids generated at save time (crypto.randomUUID with non-crypto fallback, collision-checked against existing ids).
- Deviations: (a) added `update(id, { text?, endedAt?, durationSec? })` beyond the scoped save/list/delete/clear — T10 explicitly requires "entry written at disarm; Done updates it" and this keeps T10 from touching storage directly; text patches re-derive wordCount/title/excerpt and endedAt bumps re-sort; (b) title/excerpt are derived from the TRIMMED text (leading blank lines never leak into list rows) and title truncation appends " …" (space + ellipsis) only when words were cut — word-boundary truncation per the decision, exact marker unspecified there; (c) excerpt carries no truncation marker (strictly the first 160 chars of trimmed text) so display ellipsis stays T11's presentation choice.
- Test coverage vs acceptance (all in `src/data/archive.test.ts`, in-memory `FakeStorage` adapter — real localStorage never touched): empty archive → []; save derives full metadata + newest-first order regardless of save order + persistence round-trip through a second store instance with the versioned envelope verified on disk; corrupt storage (7 fixtures: unparseable JSON, JSON null, wrong version, entries-not-array, invalid entry ×3) → list [] + raw payload quarantined verbatim + corrupt key cleared + console.warn exactly once (no repeat on second read) + recovery save works; delete single entry (unknown id → `{ ok: true, value: false }`, list unchanged); clear-all (count returned, key removed, empty clear → 0); quota path (`QuotaExceededError` DOMException → `{ ok: false, kind: 'quota-exceeded', cause }` with stored data untouched and recovery after heal; NS_ERROR_DOM_QUOTA_REACHED and name-only legacy errors → quota-exceeded; generic Error → storage-error; quota during update → typed failure, stored payload byte-identical); metadata derivation (wordCount trim-split incl. newline/tab separators and empty/whitespace → 0; title 6-word full text with whitespace collapse, 7+ words cut at word boundary with long 7th word dropped whole, empty/whitespace fallback 'Untitled session'; excerpt exactly 160 chars at the boundary, 161 → 160, trimmed source); invalid inputs throw without writing.
- Validation evidence: `npm run test` — 3 files, 54/54 tests passed (28 archive, 25 timing, 1 smoke, ~515 ms); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist unchanged 0.40 kB html / 0.13 css / 0.77 js — archive.ts not yet imported by main.ts; T10/T11 wire it).
- Follow-ups: T10 consumes `save` at disarm + `update` on Done; T11 consumes `list`/`remove`/`clear` (must keep textContent-only rendering of title/excerpt/text) and may add display ellipsis over `EXCERPT_MAX_CHARS`; T14 README notes localStorage ephemerality + the quarantine key.
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 3 files, 54/54 tests passed (28 archive, 25 timing, 1 smoke, 514 ms); `npm run build` → green — `tsc --noEmit` strict gate passed (strict + noUncheckedIndexedAccess/noUnusedLocals/noUnusedParameters), vite 8 built clean (dist 0.40 kB html / 0.13 css / 0.77 js — archive.ts not yet imported by main.ts; T10/T11 wire it).
  - Acceptance coverage verified from test source (`src/data/archive.test.ts`), all five contracted areas genuinely tested: empty archive (`list()` → `[]` on fresh storage); save+list order (newest-first endedAt desc regardless of save order, stored pre-sorted in the envelope, persistence round-trip through a second store instance with `version: 1` asserted on disk); corrupt storage (7 fixtures — unparseable JSON, JSON null, wrong schema version, entries-not-array, three structurally invalid entries — each: `list()` → `[]`, raw payload quarantined verbatim under `ARCHIVE_QUARANTINE_KEY`, corrupt key removed, `console.warn` exactly once with no repeat on second read, and recovery save works); delete (removes exactly one, unknown id → `{ ok: true, value: false }`, list unchanged) + clear (count returned, key removed, empty clear → 0); quota failure path (Chrome `QuotaExceededError` DOMException → `{ ok: false, kind: 'quota-exceeded', cause }` with stored data untouched and recovery after heal; Firefox `NS_ERROR_DOM_QUOTA_REACHED` and name-only legacy errors → quota-exceeded; generic Error → storage-error; quota during `update` → typed failure with stored payload byte-identical). Word-count trim-split semantics pinned (empty/whitespace → 0; trim then split on whitespace runs incl. `\n`/`\t`/`\r`).
  - Metadata derivation per planning decision verified from source + tests: title = first 6 words with whitespace collapsed, word-boundary truncation (" …" marker only when words were cut; long 7th word dropped whole, never mid-word), fallback "Untitled session" for empty/whitespace text; excerpt = exactly 160 chars of trimmed text, no marker.
  - Architecture verified from source (`src/data/archive.ts`): storage adapter injectable (`StorageAdapter` seam; `createArchive(storage = localStorageAdapter())`); test environment is `node` (vite.config.ts) and the only "localStorage" occurrence in the test file is a comment — tests run entirely on the in-memory `FakeStorage`, real localStorage never touched; versioned schema envelope `{ version: 1, entries }` with structural per-entry validation on read (`isArchiveEntry`); no DOM/UI code (only `console.warn`, `DOMException` instanceof checks, `globalThis.crypto`/`localStorage` inside the adapter factory); corrupt fallback is non-destructive (quarantine key preserves raw bytes; if the move itself fails the corrupt key is left in place rather than destroyed).
  - Downstream exports confirmed for T10/T11: `list`/`save`/`update`/`remove`/`clear` on `ArchiveStore`; metadata helpers `deriveWordCount`/`deriveTitle`/`deriveExcerpt`/`deriveMetadata`; constants `ARCHIVE_STORAGE_KEY`/`ARCHIVE_QUARANTINE_KEY`/`ARCHIVE_SCHEMA_VERSION`/`TITLE_MAX_WORDS`/`EXCERPT_MAX_CHARS`/`UNTITLED_SESSION_TITLE`; types `ArchiveEntry` (exact planning-decision shape)/`SaveEntryInput`/`UpdateEntryPatch`/`ArchiveResult`.
  - Logged deviations reviewed and accepted: (a) `update(id, { text?, endedAt?, durationSec? })` — directly required by T10's "entry written at disarm; Done updates it", additive, tested (re-derives metadata, re-sorts, not-found, quota-safe); (b) metadata derived from trimmed text with " …" word-boundary marker — plan specifies word-boundary truncation but not the marker/trim detail, logged and sensible; (c) excerpt carries no truncation marker — plan specifies only "first 160 chars"; display ellipsis stays T11's.
  - File inventory: only `src/data/archive.ts` + `src/data/archive.test.ts` new since T2; no stray files.
  - T6 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above).

✅ T3 Inactivity controller — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: NEW `src/engine/controller.ts`, `src/engine/controller.test.ts` (colocated per T1 conventions); MODIFIED `package.json` + `package-lock.json` only to add devDependency `jsdom` 29.1.1 — per the T1 convention "DOM-needing tasks add jsdom in their own scope" (state.md next-action explicitly directed this). Test file uses the `// @vitest-environment jsdom` per-file override; the global vitest environment stays `node`. No other files touched.
- Exported API: `createInactivityController(deps)` → `InactivityController` with `arm(config: TimingConfig, sessionEndAt)` / `getState()` / `getLastInputAt()` / `onState(listener)` (EVERY recompute — T9's per-frame opacity source) / `onTransition(listener)` (phase CHANGES only — the exactly-once channel for `deleted`/`survived` that T4/T5/T10 consume) / `destroy()` / `isDestroyed()`; both subscriptions return `Unsubscribe`. `ControllerDeps` = `{ editor: HTMLTextAreaElement, window, document, clock?, frame?, timer? }` — editor element, window/document, clock fn and rAF fn all constructor-injected per contract, plus types `FrameScheduler`/`TimerScheduler`/`Unsubscribe` and pure helper `isTextChangingInputType(inputType)`.
- R1 implementation (Option C, exactly as committed): ONE idempotent `reconcile()` — `visibilitychange` (gated on `document.visibilityState === 'visible'`, not the event alone), `focus`, `pageshow` (bfcache; handler identical, `persisted` readable by tests) and the boundary `setTimeout` fallback all funnel into it, handlers interchangeable with zero ordering assumptions. Every wall-clock anchor is the injectable clock (production default `() => Date.now()`); `performance.now` is never read (test-proven via spy) because Chrome can freeze it during page freeze. Transitions are phase-checked (`deriveTimingState` from T2): emitted only when the phase VALUE changes, `clearTimeout` precedes any emission, and terminal `deleted`/`survived` absorb all later triggers — deletion fires exactly once per session. rAF is the foreground tick only: scheduling continues while hidden (browsers pause delivery), the tick reconciles only when visible, and the boundary timer plus return events carry the hidden period (the timer can land fade/delete while hidden in Chrome's throttled tiers; when it never runs — WebKit suspension/freeze — the return events are the guarantee). Backwards `Date.now()` jumps are clamped to a no-op and can never move `lastInputAt` backwards (R1 §7). `arm()` validates config + `sessionEndAt` via a dry-run derive BEFORE mutating state (failed arm leaves the controller fully unarmed and reusable) and restarts cleanly (wind-down → new arm re-emits `armed`).
- Input filtering (text-changing only): editor listeners on `beforeinput`, `input`, and `compositionupdate`; classification rule = absent/empty `inputType` counts (legacy engines — the event implies a value mutation), `insert*`/`delete*`/`history*` prefixes count (all spec + engine-extension mutation types incl. paste `insertFromPaste`, autocorrect `insertReplacementText`, IME `insertCompositionText`, cut `deleteByCut`, undo/redo `historyUndo/Redo`), `format*` excluded; the prefix rule is deliberately failure-asymmetric (a missed type would delete text under active writing; a generous one only grants leniency). Navigation keys, clicks and scroll never fire these events — excluded structurally, and pinned by test. Each counted input stamps `lastInputAt = clock()` and reconciles synchronously (no frame wait).
- Load-bearing details: boundary timer targets `min(fadeStart, deleteAt, sessionEnd)` (fadeStart only while `armed`) — all candidates are strictly future in armed/fading (T2 invariants), so a fired timer can never reschedule itself at the same target and zero-delay spirals are impossible; late (throttled) wake-ups self-correct because the callback derives from the CURRENT wall clock. Terminal phases stop the loop AND the boundary timer. `destroy()` unwires all six listener bindings (editor ×3, document ×1, window ×2), cancels loop + timer, and `arm()` afterwards throws — the R3/T4 seam that lets a later task replace the editor node; re-arm after `deleted` should mount a FRESH instance (docstring states it; the throw enforces the destroyed case).
- Deviations: (a) added an injectable `TimerScheduler` seam (5th injectable beyond the contracted editor/window+document/clock/rAF) so "no accumulated ticks" is assertable by direct inspection of the fake's pending queue — no `vi.useFakeTimers` entanglement with the injected wall clock; (b) `onState` channel (every recompute) in addition to the contracted transition emission — T9's reduced-motion/opacity path needs per-frame values, while the exactly-once guarantee lives on `onTransition`; (c) R1 §5.4 says "every reconcile clears and reschedules the fallback timer" — implemented as clear+reschedule only when the wall-clock TARGET moves (observable invariant identical: at most one pending timer always targeting the true next boundary; a test pins that 100 foreground frames cause zero schedule/cancel churn); (d) jsdom devDependency added (logged above, directed by state.md).
- Test coverage vs acceptance (30 tests in `src/engine/controller.test.ts`, jsdom + injected fakes: FakeClock wall clock, FakeTimers with `fireDue()` for throttled-late wake-ups and `steal()` for already-committed wake-ups that a cancel cannot recall, FakeFrames delivering one frame at a time): input resets BOTH triggers instantly (synchronous emission with no frame, opacity 1 + `deletesInMs` fully reset + fade window restarts from the new input instant + boundary re-target); hidden-tab overshoot past delete threshold applies deletion on visibility return exactly once, with all converging triggers no-ops (focus after visibilitychange, `pageshow persisted`, a STOLEN late timer callback executing after the delete — the true R1 double-fire race), plus the Chrome-tier case (timer lands fade AND deletion while still hidden), the visibilitychange hidden-gate, and bfcache `pageshow.persisted` after 10-min overstay; blur keeps running (loop alive, exactly one pending frame, fade proceeds on later ticks); slow/janky frames cause no drift (per-tick equality against the pure `deriveTimingState` oracle over irregular steps incl. 4_231ms and 1ms jumps, exact opacity spot checks 0.96/0.7138, and one giant 10s single-frame jump → deleted); disarm at 0:00 stops all threat transitions (survived halts loop + timer; post-disarm bombardment of late timers/frames/inputs/return events yields zero `deleted`; exact 0:00/delete tie → survived through the controller); no accumulated ticks anywhere (≤1 pending frame and ≤1 pending timer asserted across 50 churn iterations with interleaved inputs and timer fires, final target = true next boundary; hidden tick skips recompute but keeps exactly one frame scheduled; foreground frames cause zero timer churn); R1 extras — frozen `performance.now` spy never called across a full session, backwards clock jump clamped (no emission, anchor unmoved, forward recovery), inputType taxonomy unit table (24 types), legacy absent-inputType counted, navigation/click/scroll/format excluded with the threat still maturing, failed arm leaves it unarmed, re-arm after survived, unsubscribe, destroy unwires everything (zero emissions/timers/frames after; re-arm throws; idempotent).
- Validation evidence: `npm run test` — 4 files, 84/84 tests passed (30 controller, 25 timing, 28 archive, 1 smoke; ~650 ms); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist unchanged 0.40 kB html / 0.13 css / 0.77 js — controller.ts not yet imported by main.ts; T5 wires it).
- Follow-ups: T4 subscribes `onTransition` for the `deleted` beat and calls `destroy()` as its teardown step (listeners-removed verified here; R3's node-replacement mechanics stay T4's own); T5 wires `arm`/`onTransition`/`onState` and computes `sessionEndAt` via T2's helper; T9 consumes `onState` opacity per frame (and `deletesInMs` for the reduced-motion numeric countdown); T13's manual protocol exercises the physical hidden-tab regimes these mocks model.
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 4 files, 84/84 tests passed (30 controller, 25 timing, 28 archive, 1 smoke, ~650 ms); `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist 0.40 kB html / 0.13 css / 0.77 js — controller.ts not yet imported by main.ts, T5 wires it).
  - Acceptance coverage verified from test source (`src/engine/controller.test.ts`, 30 tests counted across 10 describes = 3 taxonomy + 3 arm + 5 input + 6 overshoot + 1 blur + 3 drift + 2 disarm + 2 churn + 2 clock-guards + 3 teardown), every contracted clause genuinely tested with injected FakeClock/FakeTimers/FakeFrames in jsdom: input resets BOTH triggers instantly (mid-fade 0.8 → synchronous `armed`/opacity 1 emission with no frame wait, `deletesInMs` fully reset, fade window restarts from the NEW input instant, boundary re-targeted); hidden-tab overshoot past delete threshold applies deletion on visibility return exactly once (45 s overstay with suspended timers → return → `['armed','deleted']`, opacity 0; converging triggers — focus, `pageshow persisted`, due timers, frames — all no-ops; plus focus-BEFORE-visibilitychange ordering irrelevance, bfcache after 10-min overstay, Chrome-tier timer landing fade AND delete while still hidden, and the visibilitychange hidden-gate proving `visibilityState` not the event drives the reconcile); blur keeps running (loop alive, exactly one pending frame, fade proceeds on later ticks); slow/janky frames cause no drift (per-tick equality against the pure `deriveTimingState` oracle over irregular steps incl. 4 231 ms and 1 ms, exact opacity spot checks 0.96/0.7138, one giant 10 s single-frame jump → deleted); disarm at 0:00 stops all threat transitions (survived halts loop + timer; post-disarm bombardment of late clocks/frames/inputs/hidden-return/focus/pageshow yields zero `deleted`; exact 0:00/delete tie → survived through the controller); terminal deleted absorbs double-fire incl. a late committed timer callback (`steal()` extracts an already-committed wake-up that clearTimeout cannot recall, executes it AFTER the visibility-handler delete → still exactly one `deleted`, nothing rescheduled).
  - R1 conformance verified from source (`src/engine/controller.ts`): ONE idempotent `reconcile()` — `visibilitychange` (handler gates on `document.visibilityState === 'visible'`, not the event), `focus`, `pageshow` (bfcache) and the boundary `setTimeout` callback all funnel into it, handlers interchangeable, no ordering assumptions; wall clock is the injectable `clock` with production default `() => Date.now()` (line 244) and `performance.now` appears ONLY in the module docstring (zero code reads — grep confirmed; test additionally spies `performance.now` across a full session incl. deletion and asserts never called); transitions emitted only when the phase VALUE changes with `clearTimeout` before any emission; `deleted`/`survived` terminal (loop + timer stopped, later triggers absorbed → deletion exactly once per session); rAF is foreground-only (tick reconciles only while visible, scheduling continues per R1 §5.3; hidden tick skips recompute but keeps one frame queued — pinned by test); no accumulated ticks (single frameHandle/timerHandle; ≤1 pending each proven across 50 churn iterations with interleaved inputs and timer fires, final target = `min(lastInput+fade, lastInput+delete, sessionEnd)`); boundary targets strictly future in armed/fading (T2 invariants) so a fired timer can never reschedule itself at the same target and the fired callback nulls `timerTarget` before reconciling so a fired timer always reschedules fresh; backwards `Date.now()` jumps clamped to no-ops that never move `lastInputAt` backwards; `arm()` dry-run validates before mutating (failed arm leaves controller fully unarmed + reusable); `destroy()` unwires all six listener bindings (editor ×3, document ×1, window ×2), cancels loop + timer, is idempotent, and `arm()` afterwards throws — the R3/T4 node-replacement seam, verified by test (zero emissions/schedules/requests after destroy).
  - Input filter verified against the plan's text-changing-only semantics: `isTextChangingInputType` counts `insert*`/`delete*`/`history*` (24-type table tested incl. paste `insertFromPaste`, autocorrect `insertReplacementText`, IME `insertCompositionText`, cut `deleteByCut`; `compositionupdate` counted separately; legacy absent/empty inputType counted), excludes `format*`; navigation keys/click/scroll/wheel never fire input events and are pinned excluded by test with the threat still maturing (0.8 → 0.4 across them). `historyUndo`/`historyRedo` counting as activity is correct for the armed phase (undo/redo changes text); R3's deleted-phase `preventDefault` guard on those types is T4 scope, not a T3 defect.
  - Deviation (c) assessed against R1 §5.4 ("every reconcile clears and reschedules the fallback timer"): implemented as clear+reschedule only when the wall-clock TARGET moves. ACCEPTED — R1's prescription protects the observable invariant (exactly one pending timer always targeting the true next boundary, never stale, never accumulated), and that invariant is directly proven: 100 foreground frames → `scheduleCount` stays 1 and `cancelCount` 0 with the due time wall-clock-stable at the fade boundary; an input re-targets exactly once (1 cancel + 1 schedule, new due = new input + fade); the churn test's final pending timer equals the true next boundary; and the fired-callback nulls-`timerTarget`-first detail means a fired timer always reschedules rather than falsely matching a stale target. Zero churn is a strict improvement with identical observable behavior. Deviations (a) TimerScheduler seam, (b) `onState` per-recompute channel alongside `onTransition` (exactly-once guarantee lives on `onTransition`; T9's per-frame opacity lives on `onState`), and (d) jsdom 29.1.1 devDependency (per T1's "DOM-needing tasks add jsdom in their own scope" convention, directed by state.md; global vitest environment still `node`, jsdom is a per-file `// @vitest-environment` override) — all additive and accepted.
  - File inventory: only `src/engine/controller.ts` + `src/engine/controller.test.ts` new since T6; `package.json` diff is exactly the jsdom devDependency (+ lockfile); no stray files.
  - T3 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above).

✅ T5 Session phase router — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: NEW `src/ui/router.ts`, `src/ui/router.test.ts` (colocated, `// @vitest-environment jsdom` per-file override; global vitest environment stays `node`); MODIFIED `src/main.ts` (wires the real controller: router over `#app`, boots to setup). No other files touched; no new dependencies.
- Exported API (`src/ui/router.ts`): `createSessionRouter(deps)` → `SessionRouter` with `currentScreen()` / `startSession({ preset, minutes })` / `finishWindDown()` / `reArm()` / `viewArchive()` / `newSession()` / `destroy()` (idempotent; `startSession` afterwards throws); `RouterDeps` = `{ root, window, document, clock?, frame?, timer?, storage? }` — T3's exact injection seams (clock/rAF frame/boundary timer) plus T6's `StorageAdapter`, so tests drive the WHOLE loop through real DOM events with fakes and NO test-only hook exists in any production path (`main.ts` passes only root/window/document → Date.now + rAF + setTimeout + localStorage). Also exports `ScreenId` (`'setup'|'write'|'deleted'|'wind-down'|'archive'`, doubles as `data-phase` values), `SessionConfig`, and pure `formatClock(ms)` (ceil-to-next-second `M:SS`, negatives clamped, e.g. 59 999 ms → "1:00").
- Architecture (one console, five phases): the router swaps exactly ONE `<section data-phase>` into the mount (`root.replaceChildren`) — never two screens live. Session lifecycle: `startSession` validates minutes (RangeError, engine contract), computes `endedAt` via T2's `sessionEndAt` helper, mounts a FRESH editor + FRESH `createInactivityController` per session (R3: re-arm after `deleted` is structurally a fresh instance — arm on a destroyed controller throws), subscribes `onState` (countdown textContent, status line, engine opacity projected onto the textarea as a one-line placeholder for T9's committed surface) + `onTransition` (phase-change beats only). `deleted` → controller teardown via T3's `destroy()` seam, editor reference dropped with the screen swap, SIGNAL LOST board, RE-ARM → setup (comment marks where T4 inserts the permanence sequence). `survived` (disarm at 0:00) → T6 `save` at disarm with `endedAt` = the 0:00 instant itself, wind-down screen with the draft EDITABLE, DONE → T6 `update` with wind-down edits (single entry, never a second one). Archive screen lists newest-first, textContent-only rendering, honest empty state. Navigation guards: `viewArchive` is setup-only and `resetToSetup` no-ops mid-write — a live threat is never navigable away from. Quota failure at disarm is honest: wind-down note names the failure kind, the draft stays in the editor, DONE retries (save-if-never-saved, update-otherwise).
- Setup placeholders (T7 owns the real console): preset `<select>` with GENTLE/STANDARD/BRUTAL and printed fade/loss limits, duration `<select>` with the planned 3/5/10/15 presets (default 5), custom-minutes number input (empty → preset duration; invalid → ARM disabled + inline message), ARM + VIEW ARCHIVE buttons.
- Test coverage vs acceptance (9 tests, all jsdom + injected FakeClock/FakeTimers/FakeFrames/FakeStorage): FULL SURVIVE WALKTHROUGH — config (preset + custom 1 min) → ARM → write (countdown "1:00") → engine wiring proven live (idle 6 s → opacity 0.8 + FADING status via `onState`; typing → opacity 1 + ARMED instantly) → typing every 3 s to exactly 0:00 → disarm → wind-down with the exact draft, `disabled === false`, loop/timers fully down (0 pending frames, 0 pending timers) → disarm-time entry asserted field-by-field (preset/createdAt/endedAt/durationSec 60/text/title via `deriveTitle`) → wind-down edit + DONE → archive screen shows the row (id-addressable, title, "1:00 run", ISO timestamp) with the UPDATED text and still exactly ONE entry → NEW SESSION back to setup. QUOTA PATH — disarm with failing storage → wind-down note "quota-exceeded", draft preserved verbatim, heal + DONE → archive lands with the final text. DELETION WALKTHROUGH — BRUTAL armed, typing, idle past the 6 s loss threshold (timers carry it, hidden-tab style) → deleted board, editor gone from the DOM, terminal hygiene (0 frames/0 timers), archive stays EMPTY → RE-ARM → setup → second session arms with a fresh controller + fresh empty editor (reuse of the destroyed controller would have thrown) and deletes again. Plus: formatClock unit table (11 boundary cases incl. ceil at 999/1 001/59 999 and negative clamp), setup boot shape (three presets with printed limits, duration options exactly 3/5/10/15, single-screen invariant `root.children.length === 1`), invalid custom minutes ('0'/'0.5'/'121'/'-3'/'1e3' each disable ARM with a message; '90' and empty re-enable; noted that 'abc' is untestable through a number input — the spec's value-sanitization reduces it to '' in the DOM, jsdom included), `startSession` RangeError stays on setup, setup→archive empty state→setup navigation, `destroy()` teardown (engine silent afterwards, idempotent).
- Validation evidence: `npm run test` — 5 files, 93/93 tests passed (9 router, 30 controller, 25 timing, 28 archive, 1 smoke; ~570 ms); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean; dist js 0.77 kB → 18.41 kB (gzip 5.90 kB) because main.ts now actually wires router + controller + timing + archive into the artifact (previously unimported).
- Deviations: (a) screens live INSIDE `router.ts` as private builders rather than separate screen modules — thin-slice scope; T7/T9/T10/T11 rebuild their surfaces in place over the stable `data-phase`/`data-action`/`data-*` hooks; (b) the disarm-time `save` + DONE `update` data flow (T10's contracted flow) is implemented here minimally so the loop acceptance is testable end-to-end — T10 owns the committed boards; (c) router calls `controller.destroy()` on terminal outcomes (lifecycle hygiene via T3's documented seam; T4 inserts permanence mechanics before the deleted board); (d) placeholder engine-opacity projection onto the bare textarea + text `M:SS` countdown stand in for T9's committed write surface; (e) invalid-custom-minutes handling (disabled ARM + message) is a minimal slice of T7's validation — placards, keyboard operability and full invalid-state handling stay T7.
- Follow-ups: T7 replaces setup controls on the `startSession` seam; T8 tokens + direction contract (semantic DOM is stable to restyle); T9 replaces the write surface (consume `onState` per frame; `formatClock` reusable); T10 replaces deleted/wind-down boards keeping the save-at-disarm/update-on-Done flow and inserting T4's permanence sequencing (comment marks the seam); T11 replaces the archive list (textContent-only habit established; entry actions are T11's); T12 a11y (no aria-live/roles committed yet — placeholder screens only).
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 5 files, 93/93 tests passed (9 router, 30 controller, 25 timing, 28 archive, 1 smoke, ~830 ms); `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist 0.40 kB html / 0.13 css / 18.41 kB js, gzip 5.90 kB — router+controller+timing+archive now wired into the artifact); `npm run dev -- --port 5231 --strictPort` + curl smoke → `GET /` HTTP 200 serving the shell (`id="app"` + `/src/main.ts` module script), `GET /src/main.ts` HTTP 200 (clean transform: imports router, fail-fast #app mount, passes ONLY root/window/document), `GET /src/ui/router.ts` HTTP 200; dev-server log zero errors; server stopped after check. Built-bundle check: all five phase ids (`setup`/`write`/`deleted`/`wind-down`/`archive`) plus `dataset.phase`/`dataset.action`/`dataset.editor`/`dataset.countdown` hooks and the SIGNAL LOST / DOWN SAFE / RE-ARM boards present in `dist/assets/*.js` (screens client-rendered into `#app` — the expected Vite pattern; `dataset.*` compiles to camelCase property sets, hence no literal `data-phase` string in minified output).
  - Acceptance coverage verified from test source (`src/ui/router.test.ts`, 9 tests: 1 formatClock + 4 setup + 2 survive-walkthrough + 1 deletion-walkthrough + 1 destroy), the contracted fake-clock walkthroughs are genuine end-to-end loops driven through REAL DOM events with injected FakeClock/FakeTimers/FakeFrames/FakeStorage (no `vi.useFakeTimers`, no test-only production hook — `main.ts` passes only root/window/document → Date.now + rAF + setTimeout + localStorage): SURVIVE LOOP — config (STANDARD preset + custom 1 min through the actual `[data-preset]`/`[data-custom-minutes]` controls) → ARM → write screen ("1:00" countdown) → engine wiring live (6 s idle → opacity 0.8 + FADING via onState; real typing → opacity 1 + ARMED instantly) → typing every 3 s to exactly T0+60 000 → wind-down with the exact disarm draft, `disabled === false`, 0 pending frames / 0 pending timers → disarm-time entry asserted field-by-field (preset/createdAt/endedAt/durationSec 60/text/title) → wind-down edit + DONE → archive row (id-addressable, title, "1:00 run", ISO timestamp) with the UPDATED text and still exactly ONE entry → NEW SESSION → setup; plus the quota-failure variant (draft preserved verbatim, honest note, DONE retries save-then-lands). DELETION LOOP — BRUTAL armed, typing, idle past the 6 s loss threshold carried by the boundary timers (hidden-tab style) → deleted board, `[data-editor]` gone from the DOM, terminal hygiene (0 frames/0 timers), archive stays EMPTY → RE-ARM → setup → second session arms with a FRESH controller + FRESH empty editor (`freshEditor !== editor`, value '' — reuse of the destroyed controller would have thrown, pinned by comment) and deletes again.
  - Router architecture verified from source (`src/ui/router.ts`): `ScreenId` = exactly the five phases; every render swaps ONE `<section data-phase>` via `root.replaceChildren` (single-screen invariant also pinned by test: `root.children.length === 1`); `onTransition` routes `deleted` → teardown + SIGNAL LOST board and `survived` → disarm-time T6 `save` (endedAt = the 0:00 instant) → editable wind-down → DONE = `update` (single entry, never a second save unless the disarm save failed); controller seams used throughout — grep of Date.now/performance.now/requestAnimationFrame/setTimeout shows hits ONLY in comments and the constructor's injectable production defaults (lines 169–178); zero `performance.now` reads; no styling beyond the logged engine-opacity placeholder (`editor.style.opacity`) and rows/cols — no tokens, no fonts, no CSS in the module (T8+ untouched); navigation guards correct (viewArchive setup-only, resetToSetup no-ops mid-write); `destroy()` idempotent, `startSession` after destroy throws, and `teardownSession()` calls T3's `controller.destroy()` on every terminal outcome.
  - T4 permanence insertion point verified: comment in `handleDeleted()` ("T4 will insert the permanence sequence (blur → value/defaultValue scrub → editor node replacement → reference drop) before the board renders") + the module docstring line; the seam is real — teardown destroys the controller and drops the editor ref exactly where T4's sequence slots in.
  - Scope creep check: setup console is placeholder selects/input with minimal invalid-minutes handling (placards/keyboard operability/full invalid-state handling stay T7, logged); write surface is a bare textarea + text M:SS countdown + opacity placeholder (T9); deleted/wind-down boards are minimal (T10); archive list is read-only without entry actions (T11); no aria-live/roles (T12). All five logged deviations (a)–(e) reviewed and accepted — colocated screens over stable data-* hooks, minimal disarm-save/Done-update flow testable end-to-end, destroy() lifecycle hygiene, opacity placeholder, invalid-minutes slice.
  - File inventory: only `src/ui/router.ts` + `src/ui/router.test.ts` new, `src/main.ts` modified (production wiring only); no new dependencies, no stray files.
  - T5 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above).

✅ T4 Permanence guard — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: NEW `src/engine/permanence.ts`, `src/engine/permanence.test.ts` (colocated, `// @vitest-environment jsdom` per-file override; global vitest environment stays `node`); MODIFIED `src/ui/router.ts` (permanence wiring at the T5-marked insertion point in `handleDeleted()`, module docstring updated) and `src/ui/router.test.ts` (new "permanence (T4)" describe extending — not duplicating — the T5 deletion walkthrough). No other files touched; no new dependencies; NO visual work; `main.ts` untouched.
- Exported API (`src/engine/permanence.ts`): `createPermanenceGuard({ root })` → `PermanenceGuard` with `destroyEditor(editor)` (the R3 sequence; idempotent) / `armUndoInterceptor()` / `disarmUndoInterceptor()` (both idempotent) / `isUndoInterceptorArmed()`; pure classifier `isHistoryInputType(inputType)` (exactly `'historyUndo' | 'historyRedo'`, mirroring the controller's classifier convention); types `PermanenceDeps`/`PermanenceGuard`/`HistoryInputType`. Zero storage references in the module (the deletion path cannot write storage structurally).
- R3 implementation (committed decision, exactly): `destroyEditor` = 1. `blur()` (unconditional — terminates any in-flight IME composition, the one non-cancelable beforeinput class, and drops the undo target BEFORE mutation) → 2. `value = ''` (Chrome/Firefox wipe their field undo stacks on programmatic sets — Chromium 40656690, Bugzilla 1523270 intentional) → 3. `defaultValue = ''` (string-replaces the textarea's CHILD TEXT nodes, closing the spec-derived form.reset()/dirty-flag resurrection vector; test pins that `value=''` alone leaves the draft in `textContent` and only the defaultValue scrub kills it) → 4. `editor.remove()` — node DETACHED, never hidden; the load-bearing layer (Safari/WebKit deliberately preserves undo across programmatic value sets — bug 61340/r87204 — but all three engines bind undo state to the element instance, so a detached node is unreachable and a fresh node starts empty). Layer 5: capture-phase `beforeinput` listener on the console MOUNT that `preventDefault()`s historyUndo/historyRedo — armed ONLY deletion → RE-ARM, add/remove based (zero cost during normal typing), never blocking non-history inputTypes, and provably capture-phase (tests reach it with a NON-BUBBLING dispatch, which only a capture listener on an ancestor can see). `execCommand`/`setRangeText` never used (they preserve the undo buffer — the opposite of T4).
- Router wiring (at the marked insertion point): `handleDeleted()` now captures the editor ref, runs `destroyEditor`, arms the interceptor, THEN `teardownSession()` (controller `destroy()` + `writeRefs = null` — reference drop AFTER the sequence, while the ref is still live), then renders SIGNAL LOST. Disarm sites: `resetToSetup()` (RE-ARM / leaving the deleted board — the R3 "until RE-ARM" lifetime) + `startSession()` and `destroy()` as structural guarantees that no write session can EVER run armed (blocking Ctrl+Z during live writing would be a permanence bug in reverse). Verified-and-kept R3 seams from T5: RE-ARM routes to a FRESH controller + FRESH editor (`startSession` → `renderWrite` creates a new textarea; arm on the destroyed controller would throw) and the editor is never wrapped in a `<form>` (asserted in tests: `editor.closest('form') === null`, no `<form>` in the console).
- Test coverage vs acceptance (11 new tests: 8 unit + 3 router): no storage write on the deletion path (FakeStorage `setItem`/`removeItem` spies record ZERO calls through the live deletion, and the T6 archive is untouched — a pre-seeded survived entry's localStorage envelope stays byte-identical and `list()` deep-equal after a later deletion); textarea value cleared (both stores: `value === ''` AND `defaultValue === ''` AND `textContent === ''`); editor element replaced/detached so Ctrl+Z cannot resurrect (`isConnected === false`, not hidden; no connected textarea remains in the console); memory references dropped (old node stays scrubbed through RE-ARM and a fresh session; late input/beforeinput events at the dead editor + 60s of clock/frames/timers resurrect nothing); THE SEQUENCE proven by instrumentation (own-property accessor shadows on `value`/`defaultValue` + instance spies on `blur`/`remove` record exactly `['blur', 'value', 'defaultValue', 'remove']`); blur drops `document.activeElement`; interceptor phase behavior end-to-end (mid-write historyUndo NOT prevented; deleted board historyUndo/historyRedo `defaultPrevented === true`; insertText passes; wind-down and setup and the fresh re-armed session all un-intercepted); arm/disarm idempotence (double-arm + single-disarm leaves zero listeners); `isHistoryInputType` taxonomy table.
- jsdom honesty notes (per R3 §"What T4's test can assert"): jsdom implements no native undo, so unit tests prove the DOM-level SEQUENCE and interceptor strategy, not physical Cmd/Ctrl+Z — that is T13's manual per-browser protocol (recorded as follow-up below). Memory assertability: what IS provable in jsdom — every DOM holder of the draft string scrubbed (value/defaultValue/textContent of the dead node), node detached, router drops editor+controller refs, zero storage trace, archive byte-identical. What is NOT provable — actual GC collection (would need `--expose-gc` + WeakRef, not part of the vitest invocation); the claim is therefore "GC-eligible by structure", with the instrumented-scrub + detached-node + dropped-refs evidence above.
- Validation evidence: `npm run test` — 6 files, 104/104 tests passed (8 permanence, 12 router incl. the 3 new permanence walkthroughs, 30 controller, 25 timing, 28 archive, 1 smoke; ~770 ms); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean; dist js 18.41 kB → 19.27 kB (gzip 6.10 kB) — permanence module now wired into the artifact.
- Deviations: (a) the interceptor attaches to the console MOUNT (the router's root) rather than the literal write-screen node — the write screen's own node is removed BY deletion, so the mount (which survives screen swaps and parents the deleted board) is the only node whose capture listener can span deletion → RE-ARM; (b) node DETACH via `editor.remove()` rather than `replaceWith(fresh)` — R3 explicitly allows "remove it and mount the SIGNAL LOST board (T10 owns the board)"; the router's `renderDeleted` replaces the whole screen and RE-ARM mounts the fresh editor, satisfying the fresh-instance seam; (c) disarm at three sites (resetToSetup + startSession + destroy) instead of only RE-ARM — the extra sites are structural guards so a fresh write session can never run with undo blocked; (d) `destroyEditor` is a guard method, not a bare export — one facade for the router, keeps call order (destroy → arm → teardown) in one readable site.
- Follow-ups: T13 must run the manual per-browser undo-attempt protocol (jsdom cannot): after deletion, attempt Cmd/Ctrl+Z, Edit-menu Undo, iOS shake-to-undo, and Android long-press toolbar Undo in Chrome/Firefox/Safari desktop+mobile — none restores text; record per-browser results in this log (already Research-locked on T13's entry). T9/T10 keep the `destroy()`/fresh-instance seam (RE-ARM always mounts a NEW surface over the detached editor, focused immediately) and must never place the editor in a `<form>`; T10's deleted board renders after the permanence sequence (already ordered in `handleDeleted`).
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 6 files, 104/104 tests passed (8 permanence, 12 router incl. the 3 new permanence walkthroughs, 30 controller, 25 timing, 28 archive, 1 smoke, ~818 ms); `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist 0.40 kB html / 0.13 css / 19.27 kB js gzip 6.10 kB — permanence module wired into the artifact).
  - R3 sequence verified STRUCTURAL from source (`src/engine/permanence.ts`, `destroyEditor`): `editor.blur()` → `editor.value = ''` → `editor.defaultValue = ''` → `editor.remove()` are four consecutive statements in one method — the order IS the code, not an incidental call arrangement; `armUndoInterceptor()` precedes `renderDeleted()` in `handleDeleted` (`src/ui/router.ts`), so undo attempts are blocked before the board's first paint. Router call order (`destroyEditor → armUndoInterceptor → teardownSession → renderDeleted`) runs the sequence while the editor ref is still live, then drops it.
  - The Safari lesson genuinely proven by execution (`permanence.test.ts` "scrubs BOTH value stores"): the test seeds the real two-store split (defaultValue + textContent hold the draft, then typing sets the dirty API value), pins that `value = ''` ALONE leaves `textContent` holding the draft (the resurrection vector, pre-scrub), and only the `defaultValue` scrub + detach empties all three stores (`value`/`defaultValue`/`textContent` all `''`, `isConnected === false`). Exact order pinned by instrumentation: own-property accessor shadows on `value`/`defaultValue` + instance spies on `blur`/`remove` record exactly `['blur', 'value', 'defaultValue', 'remove']`.
  - Detach-not-hidden verified: `editor.remove()` in source; `isConnected === false` + `root.contains(editor) === false` asserted; grep of production sources for `display.*none`/`visibility.*hidden` → zero code hits (docstring mentions only); grep for `execCommand`/`setRangeText` → zero code hits (docstring "never used" note only — R3: execCommand preserves the undo buffer, the exact opposite of T4's goal).
  - Interceptor phase discipline verified end-to-end through the live router (`router.test.ts` "permanence (T4)"): mid-write `historyUndo` NOT prevented (undo is normal typing behavior); deleted board `historyUndo` AND `historyRedo` `defaultPrevented === true` while `insertText` passes; RE-ARM → setup → not prevented; the next session's FRESH editor → not prevented; wind-down → not prevented (deleted-phase ONLY). Capture flag pinned by a NON-BUBBLING dispatch that only an ancestor capture listener can see. Disarm sites in source are structural: `resetToSetup` (RE-ARM/new-session), `startSession`, `destroy` — no write session can ever run armed; the single arm site is `handleDeleted`; arm/disarm idempotent with full listener removal proven.
  - Storage silence verified twice over: FakeStorage `setItem`/`removeItem` spies record ZERO calls across arm → type → delete (spies attached before arm), AND the pre-seeded archive test first lands a survived entry through the real disarm path, then deletes a second session on top and asserts the `ARCHIVE_STORAGE_KEY` envelope stays byte-identical + `list()` deep-equal. Source grep: `src/engine/permanence.ts` contains ZERO storage references in code; the deletion path (`handleDeleted`) touches no archive API — the only archive-writing sites are `handleSurvived` (disarm) and `finishWindDown` (Done).
  - Fresh-instance seam + teardown verified: `teardownSession()` nulls `offState`/`offTransition`/`controller`/`writeRefs` (+ `session` nulled in `handleDeleted`); RE-ARM → setup → next ARM mounts a fresh controller + fresh textarea (arm on the destroyed controller would throw; test pins `freshEditor !== editor`, empty value, second deletion works); the dead node stays scrubbed and detached through RE-ARM + a full later session, and late `input`/`beforeinput` events at the dead editor plus 60 s of clock/frames/timers resurrect nothing. Editor never inside a `<form>`: no `<form>` is created anywhere in production source (grep — docstring/comment mentions only) and tests assert `editor.closest('form') === null` + no form in the console.
  - jsdom honesty verified: the limitation (jsdom runs no native undo; physical Cmd/Ctrl+Z, Edit-menu Undo, iOS shake, Android long-press toolbar belong to T13's manual per-browser protocol) is documented in the module docstring, in the worker's "jsdom honesty notes" (including the precise GC claim — "GC-eligible by structure", not claiming actual collection without `--expose-gc`), and as an explicit T13 follow-up entry here — matching R3's own contract line ("jsdom asserts the strategy, physical undo is T13's") and plan.md T13's Research-locked protocol. Not papered over.
  - Logged deviations reviewed and accepted: (a) interceptor on the console MOUNT rather than the literal write-screen node — the write screen's node is removed BY deletion, so the mount is the only capture home spanning deletion → RE-ARM; correct and necessary; (b) `editor.remove()` detach rather than `replaceWith(fresh)` — R3 explicitly allows "remove it and mount the SIGNAL LOST board"; the router's screen swap + fresh-editor-on-RE-ARM satisfies the fresh-instance seam (test-proven); (c) disarm at three sites instead of only RE-ARM — strict safety improvement, test-proven (mid-write undo never blocked); (d) `destroyEditor` as a guard method — one facade keeping the load-bearing order in one readable site.
  - File inventory: only `src/engine/permanence.ts` + `src/engine/permanence.test.ts` new since T5; `src/ui/router.ts` + `src/ui/router.test.ts` modified as logged; no other files touched, no new dependencies, no stray files (full tree listing checked).
  - T4 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). Milestone M1 (engine proven headless: T2, T3, T4) closes.

✅ T7 Setup console (function) — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: MODIFIED `src/ui/router.ts` (only `renderSetup()` + module docstring/`SessionConfig` comments; `startSession` seam and every other phase untouched) and `src/ui/router.test.ts` (`arm()` helper rewritten for the radio DOM; the setup describe replaced with T7 acceptance, 12 → 15 router tests). No other files; no new dependencies; NO visual/token work — exactly three class hooks added for T8/T9 (`placard`, `placard-name`, `placard-limits`) plus the semantic structure they will style.
- Console structure (function only, placeholder styling stays): Duration = ONE native radio group in a `<fieldset data-duration-group>` + `<legend>` — the planned 3/5/10/15 preset placards (`data-duration-option`) plus a CUSTOM placard (`data-duration-custom`) carrying the minutes `<input type="number" data-custom-minutes min=1 max=120>` inside the same label (interactive descendants are exempt from label activation, so clicking the field never toggles the radio; `aria-label` names the field). Difficulty = a second radio group `<fieldset data-preset-group>` of three placards (`data-preset-option="GENTLE|STANDARD|BRUTAL"`), each printing its FADE/LOSS limits straight from the T2 `PRESETS` table (`FADE 5s / LOSS 10s` etc. — always visible, not just on selection). Defaults: duration 5, STANDARD. Kept T5 hooks unchanged: `data-custom-minutes`, `data-error`, `data-action="arm"`, `data-action="view-archive"`, `data-phase`.
- Operable logic: typing in the minutes field auto-selects the CUSTOM radio (custom IS a duration mode, not a silent override — the checked state never lies about what ARM will do); choosing a preset duration radio clears the custom field and re-enables ARM; landing on the custom radio focuses its field. ARM reads the groups and calls `startSession({ preset, minutes })` unchanged (double-guarded: disabled button + readConfig error gate + `startSession`'s own RangeError). Keyboard: every control is a native form control (radios: Tab/arrows/Space; buttons: Enter/Space; number field native), PLUS arrow-key roving is owned in code on each fieldset — wrapping ArrowUp/Down/Left/Right move checked state + focus and dispatch `change`, with `preventDefault()` so the browser default never double-applies; arrows on the number input are left to the input (never rove).
- INVALID-UX CHOICE (stated per contract): **disable ARM with feedback** — an invalid/empty custom value disables ARM, prints an inline message on `[data-error]`, and sets `aria-invalid="true"` on the field. NOT revert-with-message: the writer's input stays visible for correction; choosing a preset duration is the one-click recovery (clears the field, re-enables). Validation = T2's `isValidSessionMinutes` exactly (finite, 1–120); non-numeric input is blocked twice (number-input value sanitization reduces it to '' → "Enter custom minutes" branch, and anything that still reaches `Number()` as NaN fails the finite check).
- Test coverage vs acceptance (15 router tests, jsdom + the T5 fake seams, real DOM events): out-of-range/non-numeric/empty custom blocked with feedback — for each of '0', '0.5', '121', '-3', '1e3', 'abc', '': ARM disabled, `[data-error]` non-empty, `aria-invalid` present, custom radio selected; '90' recovers; choosing a preset duration re-enables + clears the field. Keyboard selection — real `keydown` KeyboardEvents drive the roving code: ArrowDown on STANDARD checks+focuses BRUTAL (`document.activeElement` asserted), ArrowUp from GENTLE wraps to BRUTAL, ArrowRight advances; duration roving 5→10, roving off CUSTOM wraps to 3 AND clears the field; ArrowDown on the number input never roves the group. ARM starts with exactly the chosen params, asserted against controller/session behavior, not internal state — GENTLE + custom 7 min: first countdown paint "7:00" (sessionEndAt−armedAt), 8s idle still opacity 1/ARMED (past STANDARD's 5s fade, inside GENTLE's 10s — the controller is running the chosen preset's config), and the disarm-time archive entry records preset GENTLE, durationSec 420, createdAt T0, endedAt T0+420 000; plus a BRUTAL probe (4s idle → opacity exactly 2/3 through the 3s/6s fade window) and the kept `startSession` RangeError + navigation tests. The T5 walkthroughs (survive, quota, deletion, permanence, destroy) all still drive the new console through the updated `arm()` helper — unchanged assertions, all green.
- Validation evidence: `npm run test` — 6 files, 107/107 tests passed (15 router, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke; ~920 ms); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean; dist js 19.27 kB → 21.02 kB (gzip 6.10 → 6.66 kB) — the operable console replaces the selects.
- Deviations: (a) setup data-hook surface changed — the T5 placeholder `[data-preset]`/`[data-duration]` selects became `[data-preset-group]`/`[data-preset-option]`/`[data-duration-group]`/`[data-duration-option]`/`[data-duration-custom]` radios (plan T5 log always scoped T7 to replace the setup controls on this seam; all other hooks kept); (b) invalid-UX = disable-with-feedback, not revert (rationale above); (c) empty custom WHILE CUSTOM IS SELECTED is now invalid ("Enter custom minutes") instead of silently falling back to the preset — with custom promoted to an explicit radio mode, a silent fallback would make the checked state lie; (d) arrow-roving code added on top of native radio semantics — `preventDefault` + manual rove makes the behavior single-application in real browsers and assertable in jsdom (contract allows "radio-group or equivalent semantics"; this is both).
- Follow-ups: T8/T9 style the placards via the `placard`/`placard-name`/`placard-limits` hooks (Michroma caps + B612 Mono limits per R4); T12 a11y pass should give `[data-error]` an aria-live/role decision (left plain here to avoid front-running T12), verify radiogroup announcement in VoiceOver, and confirm Space/Enter activation of ARM in real browsers (jsdom cannot assert native activation — the click-path equivalent is tested); T10 keeps `startSession` as the console's only session entry.
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract. Milestone M2 (naked loop: T5, T7, +T6) closes pending verification.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 6 files, 107/107 tests passed (15 router incl. the 7 T7 setup tests, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke, 869 ms); `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist 0.40 kB html / 0.13 css / 21.02 kB js gzip 6.66 kB — the radio console replaces the selects in the artifact); `npm run dev -- --port 5263 --strictPort` + curl smoke → `GET /` HTTP 200 serving the shell (`id="app"` present), `GET /src/main.ts` HTTP 200, `GET /src/ui/router.ts` HTTP 200 with the module transforming cleanly (13 setup/placard hits); dev-server log zero errors/warnings; server stopped after check. Built-bundle check: `FADE `, `CUSTOM`, `placard-limits`, `setup-preset`, `aria-invalid` all present in `dist/assets/*.js` — the setup markup is wired into the shippable artifact.
  - Acceptance coverage verified from test source (`src/ui/router.test.ts` "setup console (T7 — function)", 7 tests), all three contracted clauses genuine: (1) custom out-of-range blocked — for each of '0', '0.5', '121', '-3', '1e3', 'abc', '' (out-of-range + non-numeric + empty-with-CUSTOM-selected): ARM `disabled === true`, `[data-error]` non-empty, `aria-invalid="true"` on the field, custom radio checked; '90' recovers (ARM enabled, error cleared, aria-invalid removed); choosing a preset duration re-enables + clears the field. (2) presets selectable via keyboard — real `keydown` KeyboardEvents drive the code-owned roving: ArrowDown on STANDARD checks+focuses BRUTAL (`document.activeElement` asserted), ArrowUp from GENTLE wraps to BRUTAL, ArrowRight advances; duration 5→10; roving OFF custom wraps to 3 AND clears the field; ArrowDown on the number input never roves the group. (3) ARM starts with exactly the chosen params, asserted BEHAVIORALLY not via internal state: GENTLE + custom 7 min → first countdown paint "7:00" (= sessionEndAt−armedAt), 8 s idle → opacity 1 + ARMED (past STANDARD's 5 s fade, inside GENTLE's 10 s — the controller demonstrably runs the chosen preset's config), disarm-time archive entry records preset GENTLE / durationSec 420 / createdAt T0 / endedAt T0+420 000; plus a BRUTAL probe — 4 s idle → opacity exactly 2/3 (=(6−4)/(6−3), fade math pinned) + FADING status. Structure test additionally pins: fieldset+legend for both groups, one radio name per group and distinct across groups, duration options exactly [3,5,10,15]+'custom' with 5 default, difficulty [GENTLE,STANDARD,BRUTAL] with STANDARD default, custom input type=number min=1 max=120, all controls native types {radio, number, button}, single-screen invariant.
  - Function-only verified from source (`src/ui/router.ts` `renderSetup()`): grep of className/style/classList/setAttribute('class') → the ONLY class hooks are exactly the three logged deviations (`placard` ×3, `placard-name`, `placard-limits`); the single `.style` hit is line 319 `editor.style.opacity` — the pre-existing T5-logged placeholder projection in `onWriteState`, untouched by T7 and outside the setup console. No tokens, no fonts, no CSS touched (src/styles/main.css mtime predates the T7 turn).
  - Placard data provenance verified: difficulty placards print `FADE ${preset.fadeDelayMs / 1000}s / LOSS ${preset.deleteThresholdMs / 1000}s` computed directly from the imported T2 `PRESETS` table — NOT hardcoded duplicates — and the structure test independently reads `PRESETS` and asserts each placard's textContent contains its preset's values, so any drift between table and print breaks the build. Custom input's min/max/placeholder derive from `MIN_SESSION_MINUTES`/`MAX_SESSION_MINUTES` (1/120, confirmed in `src/engine/timing.ts`), and validation is T2's `isValidSessionMinutes` exactly (finite + 1–120), double-guarded at `startSession` with a RangeError (test: 0 and 121 throw, stays on setup).
  - Invalid-UX and operability verified: ARM blocked THREE ways when invalid (button `disabled` + inline `[data-error]` message + `aria-invalid` on the field; click handler re-gates on `readConfig().error`; `startSession` RangeError as backstop) — disable-with-feedback per the stated deviation, writer's input preserved, preset-duration one-click recovery. Every control keyboard-operable: native radios (Tab/arrows/Space), native number field, native buttons (Enter/Space), PLUS code-owned wrapping arrow roving with `preventDefault()` so the browser default never double-applies; arrows on the number input are left to the input (never rove). Radio/fieldset semantics present: `<fieldset data-duration-group>` + legend "Duration", `<fieldset data-preset-group>` + legend "Difficulty", shared `name` per group, distinct across groups.
  - Logged deviations reviewed and accepted: (a) T5's placeholder selects → radio groups on the same `startSession` seam (plan T5 log explicitly scoped T7 to replace the setup controls; all other data-hooks kept — `data-custom-minutes`, `data-error`, `data-action="arm"`, `data-action="view-archive"`, `data-phase` all verified in source); (b) disable-with-feedback invalid UX — keeps input visible for correction, recoverable, honest; (c) empty-custom-while-CUSTOM-selected invalid rather than silent fallback — correct now that custom is an explicit radio mode (a silent fallback would make the checked state lie about what ARM will do); (d) code-owned arrow roving on top of native semantics — single-application in real browsers, assertable in jsdom; (e) the three class hooks — exactly what T8/T9 need, nothing more.
  - T5/T4 walkthroughs still green through the new console: the updated `arm()` helper drives survive, quota, deletion, permanence, and destroy walkthroughs through the real radio DOM with unchanged assertions (107/107 total).
  - File inventory: mtime check confirms the T7 turn touched exactly `src/ui/router.ts` + `src/ui/router.test.ts` (01:28/01:29); all other sources predate the turn (permanence 01:11–01:13, main.ts 00:54, …); no new dependencies, no stray files.
  - T7 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). Milestone M2 (naked loop: T5, T7, +T6) closes.

✅ T8 Direction contract + design tokens — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: MODIFIED `index.html` (direction-contract comment inserted as FIRST CHILD of body; nothing else touched), `src/styles/main.css` (rewritten to consume the token layer — document-level ground/bone/prose/selection/caret/grain only, zero screen composition), `vite.config.ts` (font-license shipping plugin), `package.json` + `package-lock.json` (`@types/node` devDependency for the plugin). NEW `src/styles/tokens.css` (the token layer) and `src/assets/fonts/` (5 woff2 + 4 license files across 4 family dirs). `src/ui/router.ts` untouched; no test files touched; no screen styling — T9/T10/T11 own screens.
- Direction contract (new-work §5, code-led): five blocks, 149 words total + FINISH line verbatim — THESIS (one console/five phases; clock presides, draft is dying telemetry; refuses the serene writing-app canvas), OWN-WORLD (#14161a / bone #e8e4d8 / locked green-alive · amber-fading · red-destruct lamps; Michroma caps, B612 tabular numerals, quiet grotesk prose; hairlines, machined corners, grain, no soft shadows — recognizable with all copy removed), STORY (arm → keep typing → pale under amber hairline band → DOWN SAFE archived / SIGNAL LOST destroyed / one-motion re-arm; threat believed because real), FIRST VIEWPORT (split-flap duration board top-center at display scale; GENTLE/STANDARD/BRUTAL placards with printed FADE/LOSS limits; guarded ARM low and isolated — the one primary action), FORM ("mission-control countdown room — split-flap boards, range-safety lamps; 7th of 7 grounded candidates; seed da64ca22").
- Contract-survives-build proof: after `rm -rf dist && npm run build`, `grep -c da64ca22 dist/index.html` → 1; all six block labels present in dist/index.html; `<body>` immediately followed by the comment in the built markup.
- Fonts vendored (all downloaded 2026-08-28 from primary sources — google/fonts GitHub raw + keshikan/DSEG v0.46 release zip; subsetting per R4's documented method: fontTools 4.63, `--layout-features=*`, hinted, woff2): Michroma-Regular **11.7 KB** (machine subset: ASCII + typographic punctuation; names unmodified, no RFN), B612Mono-Regular **12.8 KB** + B612Mono-Bold **11.6 KB** (same machine subset; digit advances verified equal at 1300 upm after subsetting), SourceSans3-VF **59.6 KB** (latin + Latin-1 + Latin ext-A + combining marks; wght 200–900 fvar intact; digits verified tabular 472/1000), DSEG14Classic-Regular **5.7 KB** (foundry's own unmodified woff2 — RFN "DSEG" respected, no modification). **Total 101.4 KB** woff2 (R4 ballpark ~69 KB; deltas logged as deviations below). Every family dir carries its license file (michroma/OFL.txt, b612-mono/OFL.txt, source-sans-3/OFL.txt, dseg14/DSEG-LICENSE.txt — all OFL 1.1), and the build copies them to `dist/font-licenses/<family>/` so the shipped bundle carries them.
- Token layer (`src/styles/tokens.css`): (1) COLOR — `--ground #14161a` (fixed, never repaints), `--panel-raised #1a1d23`, `--panel-inset #0e1013`, `--bone #e8e4d8`, `--bone-dim` (58% bone blend, ≥4.5:1), `--bone-faint` (annotations only, documented ~2.7:1 non-body), lamps LOCKED with roles in comments (green = nominal/alive/armed; amber = fading/hold; red = destruct/signal-lost; never decorative) + per-lamp `-dim` unlit states (presence, not text), alpha-based `--line`/`--line-strong` hairlines, `--focus-color`. Contrast computed, not eyeballed: bone/ground 14.25:1, bone/panel 13.28:1, bone-dim/ground 5.46:1, bone-dim/panel 5.09:1, lamps as text on ground 10.44/9.89/5.43:1 (green/amber/red), on panel 9.73/9.21/5.06:1. (2) TYPE — three roles per R4: `--font-placard` 'Michroma' (uppercase transform, 0.12em tracking, 10/11/13px scale), `--font-numeric` 'B612 Mono' (`--numeric-variant: tabular-nums` + tnum feature setting; 12/14/19px + board clamp), `--font-prose` 'Countdown Room Prose' 18px floor / 1.65 leading / 70ch measure / weights 400+600 (VF 200–900 available), `--font-instrument` 'DSEG14 Classic' CONDITION-GATED (reduced-motion countdown + SIGNAL LOST only, never the animated flap board). (3) SPACE — 4px base, --sp-1…--sp-9. (4) HAIRLINE/CORNER — `--hairline 1px`, `--radius-1 2px` / `--radius-2 4px` machined, `--radius-lamp` for lamp domes only. (5) ELEVATION — no soft shadows; `--panel-edge` (zero-blur inset hairline) + z-scale panel/board/band/grain/announce. (6) MOTION — damped/mechanical easings (`--ease-damped` exponential ease-out, `--ease-flap`), durations for flap/board/state/arm; the threat fade deliberately has NO token (opacity is per-frame engine output, never an unwatched CSS transition). (7) GRAIN — CSS-only inline SVG feTurbulence data-URI tile (task-sanctioned approach; brief commits grain to kill flat-black banding), `--grain-strength 0.045`, applied as fixed pointer-transparent overlay. (8) CAPABILITY LAYER — `@media (prefers-reduced-motion: reduce)` collapses all durations to 0.01ms and flips `--motion-level: full → reduced` (JS-readable via getComputedStyle for T9/T10's static-banner + numeric-countdown path).
- main.css wiring (no redesign): `@import './tokens.css'`; `html { color-scheme: dark }` (UA controls/scrollbars follow the room); body gets ground/bone/prose voice/caret; `::selection` bone-on-ground inversion (lamps mean state, never decorate); grain overlay via body::after. The five placeholder screens inherit the ground/bone/text basics automatically — composition untouched.
- Validation evidence: `npm run test` — 6 files, 107/107 passed (unchanged suite; tokens are CSS-only); `npm run build` — green (tsc strict gate incl. vite.config.ts with the plugin, vite 8 clean): dist html 1.86 kB / css 3.68 kB / js 21.02 kB / five woff2 emitted hashed into dist/assets (5.80 + 11.92 + 11.98 + 13.14 + 61.06 kB) + licenses in dist/font-licenses; `npm run dev` + curl — all five font paths HTTP 200 `font/woff2` at exact byte sizes (13140/11924/11988/61068/5804), tokens.css 200, served index.html carries the contract; self-hosted confirmed — the only URL-shaped string in the built bundle is the SVG XML namespace inside the grain data-URI (an identifier, not a request); `grep -c da64ca22 dist/index.html` → 1.
- Deviations: (a) fonts are SUBSET, not unmodified-converted — measured unmodified woff2 conversion = 284.2 KB total (4x the committed ballpark); subsets land at 101.4 KB. R4's own bundling note sanctions subsetting ("optional size trim ... requires RFN care"), and its measured ~69 KB was produced by the same fontTools subset path; (b) Source Sans 3 RFN handled per R4's stated mitigation — internal name records renamed to "Countdown Room Prose" in the subset (OFL §1: a modified version may not carry the RFN "Source"); copyright record kept verbatim; outlines untouched; CSS family is 'Countdown Room Prose' (tokens.css comments carry the provenance); (c) prose subset widened beyond R4's latin recipe to include Latin ext-A + combining marks (+~25 KB): a writing tool's drafts contain Polish/Turkish/Czech/Hungarian/etc. text, and mid-word font fallback on accents is a product defect; machine faces (Michroma/B612 Mono) stay at R4's narrow range and land within 0.4 KB of R4's measurements; (d) optional DSEG14 Classic included — trivially available (foundry woff2 in the release zip, 5.7 KB, unmodified); (e) `@types/node` devDependency added so the license plugin type-checks under the strict gate; (f) OFL licenses ship twice by design — alongside sources in src/assets/fonts/ (canonical) and copied into dist/font-licenses/ by the vite plugin (bundle compliance).
- Follow-ups: T9 consumes `--motion-level` + `--ease-flap`/`--dur-flap`/`--size-numeric-board` for the split-flap board and `--font-numeric` for the countdown (per-digit fixed slots; B612 0.65em advance); T10 condition-gates `--font-instrument` (DSEG14) to the SIGNAL LOST board / reduced-motion readout ONLY; T11 uses `--font-numeric` right-aligned against the margin for archive timestamps; prose ITALIC is not vendored (upright VF only — browser synthesizes oblique; add the italic VF subset ~55 KB if real italics matter in drafts); Vietnamese/Cyrillic/Greek scripts fall back to system fonts by documented scope; T12 gets the precomputed lamp/ground contrast pairs above and should re-verify on final composite surfaces.
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Detector note: the one mandated `detect.mjs` pass over the changed UI files (`index.html`, `src/styles/tokens.css`, `src/styles/main.css`) ran 2026-08-28 in its own disclosed DEGRADED mode (htmlparser2/css-tree et al. not installed in this project → regex fallback; custom properties and computed contrast not evaluated) and returned zero findings — an undercount by its own warning, not a clean bill; the finishing phase's detector pass over the built screens remains the real gate.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 6 files, 107/107 tests passed (15 router, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke; ~895 ms — suite unchanged, tokens are CSS-only); `rm -rf dist && npm run build` → green — `tsc --noEmit` strict gate passed (now also type-checking the plugin-bearing `vite.config.ts`), vite 8 built clean: dist html 1.86 kB / css 3.68 kB / js 21.02 kB (gzip 6.66 kB) + five woff2 emitted hashed into dist/assets (5.80 + 11.92 + 11.98 + 13.14 + 61.06 kB); `npm run dev -- --port 5311 --strictPort` + curl smoke → all FIVE font paths HTTP 200 `font/woff2` at exact on-disk byte sizes (Michroma 11 988 / B612Mono-Regular 13 140 / B612Mono-Bold 11 924 / SourceSans3-VF 61 068 / DSEG14 5 804), `tokens.css` + `main.css` served 200 (Vite dev CSS-module transform, expected), served `/` carries the contract (`da64ca22` → 1); dev-server log zero errors/warnings; server stopped after check.
  - Contract survival verified in FRESH dist: `grep -c da64ca22 dist/index.html` → 1; regex check confirms `<body>` is immediately followed by whitespace then `<!--` — the contract comment is the FIRST CHILD of body in the BUILT markup; all six block labels present (THESIS / OWN-WORLD / STORY / FIRST VIEWPORT / FORM / FINISH); the FINISH line matches the required text verbatim (normalized-whitespace exact-substring check): "unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance". Source `index.html` inspected directly: same comment, first child of body, `#app` + module script after it.
  - OFL compliance verified IN THE BINARIES (fontTools 4.63 + brotli on the shipped woff2, not from worker claims): **SourceSans3-VF subset** — Family/FullName/PostScript/UniqueID name records ALL renamed to "Countdown Room Prose" variants, ZERO "Source" occurrences in any non-copyright name record (RFN 'Source' not used — OFL §1 satisfied for this Modified Version), copyright record (nameID 0) retained verbatim ("© 2023 Adobe (http://www.adobe.com/), with Reserved Font Name 'Source'" — attribution intact, as OFL requires), `fvar` wght 200–900 axis intact, all 10 digits single advance 472/1000 (tabular). **B612Mono Regular+Bold** — name records unmodified ('B612 Mono', Airbus UniqueIDs; no RFN in the family's OFL.txt → subsetting with intact names is compliant), all 10 digits single advance 1300/2000 in BOTH shipped weights. **Michroma** — names unmodified, no RFN; digits 1948/1984/2048 exactly as R4 documented (non-tabular, acceptable — placard labels are static). **DSEG14Classic** — names carry "DSEG" (RFN) but the file is the foundry's UNMODIFIED woff2: UniqueID "FontForge 2.0 : DSEG14 Classic-Regular : 15-3-2020" matches the v0.46 release date (2020-03-15) and 5 804 bytes matches R4's own measurement of the shipped foundry file — RFN use is legitimate only because the file is unmodified, which the evidence corroborates. CSS family names sensible and provenance-documented ('Countdown Room Prose' with the Source Sans 3 lineage in the @font-face comment).
  - Licenses verified per family AND shipped: all four family dirs under `src/assets/fonts/` carry their license (michroma/OFL.txt, b612-mono/OFL.txt, source-sans-3/OFL.txt, dseg14/DSEG-LICENSE.txt), each the genuine SIL OFL 1.1 text with the correct upstream copyright holder (Michroma Project 2011 / B612 Project 2012 / Adobe 2010-2020 with RFN declaration / keshikan 2017 with RFN "DSEG" — matching R4's primary-source evidence); fresh-build `dist/font-licenses/<family>/` contains all four (the vite `shipFontLicenses` plugin's closeBundle copy works as claimed).
  - Self-hosted verified: the ONLY URL-shaped string anywhere in the built runtime bundle (dist html+css+js) is `http://www.w3.org/2000/svg` — the XML namespace identifier inside the grain SVG data-URI, an identifier never fetched; every other http(s) string in dist/ lives inside the OFL license texts (documentation). Built CSS references the five fonts as relative hashed `/assets/*.woff2`.
  - Tokens vs brief verified from source (`src/styles/tokens.css`): palette roles committed — `--ground #14161a` (fixed, never repaints), `--panel-raised`/`--panel-inset`, `--bone #e8e4d8` + computed `--bone-dim`/`--bone-faint` (documented non-body), lamps LOCKED with semantic roles in comments (green = nominal/alive/armed, amber = fading/hold, red = destruct/signal-lost, "never decorative") + unlit `-dim` states, alpha hairlines, the one sanctioned non-lamp green (focus) documented; type roles per R4 — `--font-placard` Michroma (uppercase transform, 0.12em tracking, 10/11/13px), `--font-numeric` B612 Mono (`--numeric-variant: tabular-nums` + tnum feature setting), `--font-prose` 18px floor / 1.65 / 70ch / 400+600, `--font-instrument` DSEG14 CONDITION-GATED to reduced-motion readout + SIGNAL LOST only (matches the R4/T10 lock); damped motion tokens (`--ease-damped`, `--ease-flap`, `--dur-flap/board/state/arm`) with the threat fade deliberately UNTOKENIZED (per-frame engine output — matches the plan's "never a CSS transition left to run unwatched"); grain via CSS-only SVG feTurbulence data-URI tile + `--grain-strength`; `@media (prefers-reduced-motion: reduce)` collapses all four durations to 0.01ms and flips `--motion-level: full → reduced` (JS-readable capability switch for T9/T10's static-banner + numeric-countdown path).
  - main.css wiring WITHOUT redesign verified: document-level only — `@import './tokens.css'`, `color-scheme: dark`, body ground/bone/prose voice/caret, `::selection` bone-on-ground inversion, grain overlay via `body::after` (fixed, pointer-transparent, `--z-grain`). Zero screen/component selectors. Mtime inventory confirms the T8 turn (01:40+) touched exactly `index.html`, `package.json` (+lockfile, `@types/node` for the plugin's strict-gate typecheck), `vite.config.ts`, `src/styles/tokens.css` (new), `src/styles/main.css`, `src/assets/fonts/**` (new) — `src/ui/router.ts` + `router.test.ts` (01:28–01:29, the T7 turn) and every engine/data/test file untouched: screens remain placeholder composition, correctly left to T9/T10/T11.
  - Deviations assessed: (a) 101.4 KB vs ~69 KB — R4's own measurements came from the same fontTools subset path and its bundling note explicitly sanctions subsetting ("optional size trim ... requires RFN care"); machine faces land within 0.4 KB of R4's numbers (Michroma 11.7 vs 11.4, B612 pair 24.4 vs 23.6); the delta is the prose face's Latin ext-A + combining-marks widening (59.6 vs 34.5 KB) — accepted because mid-word system-font fallback on accented drafts is a genuine product defect for a prose tool, plus the always-optional DSEG14 (+5.7 KB, R4 itself priced the DSEG-inclusive set at ≈81 KB); no byte cap exists in plan.md's acceptance. (b) Source Sans 3 rename — verified OFL-correct in the binary (above). (c) ext-A widening — accepted (a). (d) DSEG14 included — R4-reserved option, correctly condition-gated in tokens. (e) `@types/node` devDependency — required for `vite.config.ts` to pass the strict gate; dev-only, trivial. (f) dual license shipping — correct OFL practice. Degraded-mode `detect.mjs` pass honestly disclosed as an undercount with the finishing-phase detector named as the real gate — acceptable.
  - T8 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). Milestone M3 (the world lands: T8, T9, T10, T11) opens with T8 down.

✅ T11 Archive view (flight-log binder) — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: NEW `src/ui/archive-screen.ts` (the binder screen module), `src/styles/archive.css` (the surface's stylesheet, consuming tokens.css only), `src/ui/archive-screen.test.ts` (T11 acceptance suite, `// @vitest-environment jsdom`). MODIFIED `src/ui/router.ts` (only `renderArchive()` — now mounts the screen module — plus its import and one docstring line), `src/ui/router.test.ts` (archive-row assertions updated to the binder's `data-run`/`data-ended` hooks; walkthrough otherwise untouched), `src/styles/main.css` (one added line: `@import './archive.css';` — screens own their sheets, the document file stays ground-only). No other files; no new dependencies; other phases untouched.
- Exported API (`src/ui/archive-screen.ts`): `createArchiveScreen({ doc, archive, onNewSession, clipboard?, execCommand?, objectUrls? })` → the `<section data-phase="archive">` element; every environment dependency is constructor-injected (clipboard writer / legacy execCommand / object-URL factory), production defaults guarded (`navigator.clipboard` if present, `document.execCommand` if callable, `URL.createObjectURL`/`revokeObjectURL` if present) — the same no-test-hook stance as T3/T5. Pure exports: `formatZulu(ms)` → `YYYY-MM-DD HH:MMZ` (deterministic UTC — flight logs keep Zulu time, and no test timezone flakiness), `formatRun(sec)` → `M:SS`, `downloadFileName(entry)` → `draft-YYYYMMDDTHHMMZ.txt` (code-derived from the UTC session time, NEVER from user text — no filename/path sanitization surface exists). Types `ArchiveScreenDeps`/`ClipboardWriter`/`ObjectUrlSeam`.
- SAFETY (the plan's load-bearing line): every user-derived string — title, excerpt, full draft text — renders through `textContent` exclusively; `innerHTML`/`insertAdjacentHTML`/`DOMParser` appear nowhere in the module (grep-verified). Hostile test seeds a draft containing `<img src=x onerror=…>`, `<script>`, `<svg onload=…>` and asserts: zero img/script/iframe/svg elements, zero `on*` attributes anywhere, the title serialized as a text node (`innerHTML` contains `&lt;img`, never `<img`), and the VIEW-opened full text byte-exact and still inert.
- The surface (brief §3 + R4): nameplate strip (panel-raised, bezel hairline only) — Michroma placard "FLIGHT LOG", stacked annotations "ALL TIMES ZULU" + "KEPT ONLY IN THIS BROWSER" (honest local-storage disclosure; split into two lines to honor R4's ~24-char placard-label budget), session count, and the one filled plate NEW SESSION (bone-on-ground). The log: hairline-ruled rows on the ground (a log sheet, not cards) under SESSION/RUN column labels; per row — title (Source Sans 3 600), meta line (B612 Mono Zulu stamp + word count, Michroma preset), excerpt (prose bone-dim, 70ch, display ellipsis only when T6 actually cut text), and the running time flush right in B612 Mono 19px (j-card raised line; `justify-self: end`, grid column 2). VIEW expands the full draft in a recessed panel-inset well (aria-expanded/aria-controls; collapsed via visibility so the text leaves the accessibility tree; `white-space: pre-wrap`; themed scrollbar; `overflow-wrap: anywhere` so hostile unbroken strings cannot overflow). Actions are one machined plate style: transparent ground, 1px bezel, Michroma caps, damped token-gated transitions; transient COPY outcome uses lamp semantics (green COPIED / amber COPY FAILED); focus ring is the sanctioned `--focus-color` green at 2px (WCAG 2.4.11 width — token-ization flagged as follow-up, tokens.css is T8-owned). CLEAR LOG sits low and isolated (the room's guarded-gesture placement), opening an IN-DOM confirm panel (never `window.confirm`): amber 1px hairline caution band on the leading edge with an achromatic field (cloud-edge raised line), amber placard title, prose body naming scope + irreversibility, CONFIRM CLEAR (the destruct lamp red, reserved for the control that destructs) and KEEP LOG; focus moves to CONFIRM on open, back to CLEAR LOG on cancel; Escape cancels; non-modal — the log stays visible while deciding. Empty state honest: placard "No survived sessions yet" + prose "Nothing has outlasted the clock. Arm a session — whatever survives 0:00 lands on this log." (no fake content; columns hidden; CLEAR disabled; count "NOTHING LOGGED"). The ground never repaints — no background set outside panels/wells; `prefers-reduced-motion` collapses the surface's two damped motions via the existing `--dur-board`/`--dur-state` tokens (the 0fr→1fr grid reveal and the confirm settle), no continuous animation exists. Phones (≤40rem): rows tighten, run time drops to 14px but keeps the right margin, plates compress (the "spine-style rows" direction, simplified — logged as a finishing follow-up).
- Behavior details: newest-first straight from T6 `list()`; DELETE removes one entry immediately (deletion is this product's nature — no ceremony) with focus continuing the workflow at the same log position (next entry's DELETE, else previous, else NEW SESSION when the log empties) — asserted in tests; COPY = `navigator.clipboard.writeText` with a select+`execCommand` fallback (offscreen `readonly` textarea, removed after; marked `data-copy-scratch`) and an honest named failure with recovery ("Copy failed. Press VIEW, select the text, and copy it manually."); DOWNLOAD = `Blob([text], text/plain;charset=utf-8)` → object URL → anchor click → revoke, with an honest "not available in this browser" announcement when the URL API is absent; clear-all confirm body names the live count ("All 2 entries … no recovery") and a failed clear is reported without deleting anything; a polite `aria-live` region (`data-archive-status`, visually hidden) announces copy/download/delete/clear outcomes.
- Test coverage vs acceptance (14 new tests in `src/ui/archive-screen.test.ts`, jsdom + injected seams): HOSTILE ENTRY renders inert (assertions above, incl. VIEW-opened full text); newest-first + structure (row order equals T6 `list()` order; title/stamp/preset/words/excerpt/run asserted per row; run-time element is the last child of the row-head grid carrying the `entry-run` class hook — the right-alignment hook; column labels; count; CLEAR enabled); display-ellipsis gating (appended only when text > 160 chars); VIEW toggle (aria-expanded, aria-controls wiring, COLLAPSE label, data-open, newline-preserving text); COPY three paths (clipboard mock resolves → `writeText` called with the full draft + COPIED + announcement + no stray scratch textarea; `clipboard: null` + execCommand mock → legacy path COPIED; clipboard rejects + execCommand false → COPY FAILED + named recovery); DOWNLOAD (object-URL mocks + capture-phase anchor-click listener with preventDefault: Blob type `text/plain;charset=utf-8`, blob text === full draft, `anchor.download` === code-derived filename, `href` === the mock URL, revoke called with it, announcement; `objectUrls: null` → honest unavailable message); DELETE single (middle-of-three → two rows, storage updated, focus on the same-index row's DELETE; deleting all → empty state + focus NEW SESSION + "log is empty" announcement); CLEAR-ALL confirm (open → panel visible, "All 2 entries" + "no recovery" body, focus CONFIRM, list intact; Escape → closed + focus back to CLEAR LOG; KEEP LOG same; CONFIRM CLEAR → archive empty, empty state, count NOTHING LOGGED, CLEAR disabled, focus NEW SESSION, "Log cleared. 2 entries deleted."); honest empty state (no rows, columns hidden, CLEAR disabled, clicking it never opens the panel, "No survived sessions" text); KEYBOARD walkthrough (every interactive is a native `<button>` — Enter/Space by construction, zero `role="button"`; DOM/tab order pinned exactly: new-session, per-entry view/copy/download/delete newest-first, clear-log, with confirm controls excluded while hidden; view toggled; NEW SESSION callback fires once; clear opened and Escape-closed via real keydown events). Plus pure-formatter tables for formatZulu/formatRun/downloadFileName. Router-level: the T5 survive walkthrough's archive assertions updated to the binder (`data-run` '1:00', `data-ended` formatZulu(T0+60s), h3 title, `data-entry-id` addressing all still green through the real DONE → archive path) and the setup↔archive navigation + empty-state test unchanged.
- Validation evidence: `npm run test` — 7 files, 121/121 tests passed (14 archive-screen, 15 router, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke; ~1.1s); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 10.71 kB gzip 2.79 kB / js 27.84 kB gzip 8.92 kB — the binder module + surface wired into the artifact); dev-server smoke (`vite --port 5341`): `GET /` 200, `GET /src/ui/archive-screen.ts` 200 with the surface hooks present in the transform (FLIGHT LOG / CONFIRM CLEAR / DOWNLOAD .TXT / No survived sessions), `GET /src/styles/archive.css` 200, zero errors/warnings in the dev log, server stopped after the check; built bundle carries the surface (`FLIGHT LOG`, `ALL TIMES ZULU`, `CONFIRM CLEAR`, `There is no recovery`, `draft-` filename prefix, `CLEAR LOG`, `No survived sessions` all present in dist js; entry/log/archive classes in dist css). Contrast audit (detector cannot compute contrast in its degraded mode): bone/bone-dim on ground 14.25/5.46:1, bone on panels ≥13:1, amber confirm title on panel-raised 9.21:1, red CONFIRM CLEAR on panel-raised 5.06:1, green/amber transient copy states on ground 10.44/9.89:1 — all ≥4.5:1; bone-faint only on the disabled CLEAR plate (disabled controls exempt).
- Detector note: the mandated `detect.mjs --json` pass over all changed UI files ran 2026-08-28 in its own disclosed DEGRADED mode (htmlparser2/css-tree et al. not installed in this project → regex fallback; custom properties and computed contrast not evaluated) and returned zero findings — an undercount by its own warning, not a clean bill; the finishing phase's detector pass over the built screens remains the real gate.
- Deviations: (a) the archive screen lives in its own module `src/ui/archive-screen.ts` rather than a private builder inside router.ts — T5's logged convention said screens live in the router as thin-slice builders; the binder needs its own injectable I/O seams (clipboard/execCommand/object URLs) that would bloat `RouterDeps`, and the module keeps router.ts focused on phase lifecycle; the router still owns the phase swap and the `data-phase="archive"` contract; (b) timestamps are Zulu (UTC) `YYYY-MM-DD HH:MMZ` instead of the placeholder's `toISOString()` — the flight-log convention is genuinely UTC and this makes rendering deterministic across test machines (locale-independent by construction); (c) the run-time/meta format changed from the placeholder's `N words · M:SS run · ISO` prose line to the structured meta line + right-aligned `data-run` numeral (the brief's j-card line mandates the right-aligned running time; the old free-text line could not carry it); (d) the excerpt display appends " …" when T6 cut text — the marker choice T6 explicitly left to T11; (e) per-entry DELETE is immediate (no confirm) while clear-all requires the in-DOM panel — deletion is the product's own nature, per-entry confirmations would be ceremony, and the plan's acceptance words confirm only clear-all; (f) focus-on-open lands on CONFIRM CLEAR (the expressed intent) with Escape + KEEP both one key away; (g) the copy/download live region (`data-archive-status`, aria-live polite) is a minimal slice added for honest outcomes — full announcement design stays T12's; (h) 2px focus-ring width is a literal (WCAG 2.4.11 minimum band) because tokens.css is T8-owned — token-ization flagged for T8's owner/finishing.
- Follow-ups (finishing phase / T12): visual pass on the real render (fonts, right-margin alignment at every breakpoint, the damped 0fr→1fr reveal and confirm settle at reduced motion — jsdom pins hooks only); the ≤40rem "spine-style rows" is a simplified collapse (run time keeps the right margin) — the finishing pass may compose the fuller spine treatment; a `--focus-width` token would let T9/T10 share the ring; T12 should decide whether `data-archive-status` announcements plus the transient button states suffice in VoiceOver, and verify Enter/Space activation in real browsers (jsdom asserts native-button semantics by construction); T14's README ephemerality note is echoed on-surface by "KEPT ONLY IN THIS BROWSER".
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 7 files, 121/121 tests passed (14 archive-screen, 15 router, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke, 1.45 s); `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 10.71 kB gzip 2.79 kB / js 27.84 kB gzip 8.92 kB — the binder module + surface wired into the artifact); `npx vite --port 5417 --strictPort` + curl smoke → `GET /` HTTP 200 serving the shell, `GET /src/ui/archive-screen.ts` HTTP 200 with the surface present in the transform (FLIGHT LOG / CONFIRM CLEAR / No survived sessions), `GET /src/styles/archive.css` HTTP 200 with token references in the served sheet; dev-server log zero errors/warnings; server stopped after check. Built-bundle check: `FLIGHT LOG`, `ALL TIMES ZULU`, `KEPT ONLY IN THIS BROWSER`, `CONFIRM CLEAR`, `There is no recovery`, `CLEAR LOG`, `No survived sessions` all present in `dist/assets/*.js`; `plate-destruct`/`entry-run`/`lamp-red` in the built css.
  - SAFETY (the plan's load-bearing line) verified twice over: grep of production `src/` for `innerHTML`/`insertAdjacentHTML`/`DOMParser`/`outerHTML`/`document.write` → the ONLY hit is the safety-contract docstring in `src/ui/archive-screen.ts` (zero code uses; every string in the module renders through one of its 29 `textContent` assignments — title line 294, excerpt 322, full draft 362). The hostile test is structurally honest, not tautological: it seeds a draft containing `<img src=x onerror=…>`, `<script>`, and `<svg onload=…>`; asserts ZERO img/script/iframe/svg elements in the mounted section, ZERO `on*` attributes across every element (`getAttributeNames` walk), the title serialized as a TEXT node (`innerHTML` contains `&lt;img`, never `<img`), the VIEW-opened full draft byte-exact and still inert, and `window.__pwned` undefined — the payload demonstrably reached metadata (`entry.title === deriveTitle(hostile)`) before the rendering assertions. Test-side `innerHTML` uses are the assertions themselves (escaped-entity checks), plus one `document.body.innerHTML = ''` cleanup — no production-path sink.
  - All 14 new tests verified genuine from source (`src/ui/archive-screen.test.ts`: 3 pure-formatter + 11 screen, jsdom + injected seams, real DOM events): newest-first + right-aligned run structure (row order asserted equal to T6 `list()` order seeded out-of-order; per-row title/Zulu stamp/preset/word count/excerpt/run asserted; the run-time element pinned as the LAST child of the row-head grid carrying the `entry-run` class hook — the hook archive.css gives `justify-self: end`; column labels; count; CLEAR enabled); display-ellipsis gating (appended only when text > 160 chars, absent otherwise); hostile-entry inertness (above); VIEW toggle (aria-expanded true/false, aria-controls wiring, COLLAPSE label, data-open, newline-preserving text); COPY three paths (clipboard mock resolves → `writeText` called with the full draft + COPIED + announcement + NO stray scratch textarea; `clipboard: null` + execCommand mock → legacy select+execCommand path COPIED with no lingering scratch; clipboard rejects + execCommand false → COPY FAILED + named recovery naming VIEW); DOWNLOAD via object-URL mocks (blob type `text/plain;charset=utf-8`, blob text === full draft, `anchor.download` === the code-derived filename, `href` === the mock URL, `revoke` called with it, announcement; the capture-phase click listener with preventDefault is the TEST's instrument, not production behavior; `objectUrls: null` → honest unavailable message steering to COPY); DELETE single (middle-of-three → two rows, storage updated, focus lands on the same-index row's DELETE via `document.activeElement`; deleting all → empty state + focus NEW SESSION + "log is empty" announcement); CLEAR-ALL in-DOM confirm (open → panel visible + "All 2 entries" + "no recovery" + focus on CONFIRM CLEAR + list intact; Escape via a real keydown → closed + focus back to CLEAR LOG + nothing deleted; KEEP LOG same; CONFIRM CLEAR → archive empty + empty state + NOTHING LOGGED + CLEAR disabled + focus NEW SESSION + "Log cleared. 2 entries deleted."); honest empty state (no rows, columns hidden, CLEAR disabled, clicking CLEAR never opens the panel); KEYBOARD walkthrough (every interactive is a native `<button>` with zero `role="button"`; DOM/tab order pinned exactly — new-session, then view/copy/download/delete per entry newest-first, then clear-log, with confirm controls excluded while hidden; view toggled; NEW SESSION fires once; clear opened and Escape-closed through real keydown events).
  - In-world confirm verified: `window.confirm`/`alert`/`prompt` appear NOWHERE in production src (grep hits are docstring/comments only); clear-all is a non-modal in-DOM panel (`role="group"` + `aria-labelledby`, `hidden`-gated so its controls leave the tab order when closed — pinned by the walkthrough test), amber hairline caution band on the leading edge, body naming live scope + irreversibility, CONFIRM (red) / KEEP LOG, focus in on open and back to CLEAR LOG on cancel, Escape cancels. The deviation question pre-assessed by the coordinator is settled by the plan's own text: T11's scope reads "delete entry, clear all with confirm" and the acceptance line reads "clear-all requires confirmation" — confirmation attaches ONLY to clear-all, so immediate per-entry delete is plan-compliant (the worker still logged it as deviation (e); correct reading, no unmet criterion).
  - Tokens verified from `src/styles/archive.css` (consumes tokens.css only; the few literals are `color-mix` derivatives of tokens, spacing sizes, and the logged 2px focus ring): Michroma placards (`--font-placard` + `--placard-tracking`/`--placard-transform` on FLIGHT LOG, the two stacked honesty notes, count, every plate, preset tag, column labels, confirm title, empty title); B612 Mono numerals (`--font-numeric` + `--numeric-variant`/`--numeric-feature` on the right-aligned run time at `--size-numeric-lg`, Zulu stamps, word counts — dropping to `--size-numeric` at ≤40rem while keeping the right margin); Source Sans 3 prose (`--font-prose` on titles at `--weight-prose-strong`, excerpts, full drafts at `--prose-measure`, confirm body, empty body). Ground/hairline discipline holds: `.archive` sets NO background — panels/wells only (`--panel-raised` nameplate + confirm, `--panel-inset` draft well), rows are hairline-ruled on the ground itself (`--hairline`/`--line`, hover a 4% bone wash), zero soft shadows (`--panel-edge` bezels only), machined radii. Lamp semantics respected: `--lamp-red` appears in archive.css ONLY on `.plate-destruct` (CONFIRM CLEAR — grep-confirmed, and the CSS comment states the reservation); amber carries the caution band + confirm title + COPY FAILED transient; green appears only as the COPIED transient and the sanctioned `--focus-color` ring. Reduced-motion honored via tokens: the sheet's only durations are `--dur-board` (the 0fr→1fr grid reveal + confirm settle) and `--dur-state` (plate/row state) — exactly the two tokens the tokens.css `prefers-reduced-motion: reduce` block collapses to 0.01ms (verified present at tokens.css lines 234–241); no continuous animation exists on the surface; the visibility delay on the collapsed well also rides `--dur-board`, so it collapses to 0s under reduce.
  - No screen work leaked into other phases: `renderArchive()` in `src/ui/router.ts` is pure wiring (screen id + `createArchiveScreen({ doc, archive, onNewSession })` + `replaceChildren`), and the module's entire style/class surface is STILL the T5-logged `editor.style.opacity` placeholder plus T7's three `placard*` hooks (grep of `.style.`/`className`/`classList`; zero font/color/`var(--` references in router.ts). File inventory via mtime confirms the T11 turn (02:07–02:11) touched exactly the six logged files — NEW `src/ui/archive-screen.ts` + `src/styles/archive.css` + `src/ui/archive-screen.test.ts`, MODIFIED `src/ui/router.ts` + `src/ui/router.test.ts` (archive-row assertions moved to the binder's `data-run`/`data-ended`/`formatZulu` hooks, walkthrough otherwise untouched) + `src/styles/main.css` (the one `@import './archive.css';` line and its comment); every engine/data/tokens file predates the turn; no new dependencies, no stray files.
  - Honest-outcome surfaces verified: copy failure names the failure AND the recovery; download unavailability is announced rather than silently dropped; failed clear reports "Nothing was deleted"; the polite `aria-live` region (`data-archive-status`, visually hidden) carries copy/download/delete/clear outcomes — a minimal slice correctly left for T12's full announcement design. `overflow-wrap: anywhere` on title/excerpt/full-text means hostile unbroken strings cannot overflow the log.
  - Logged deviations reviewed and accepted: (a) own module over a router builder — the binder's injectable I/O seams (clipboard/execCommand/object URLs) would bloat `RouterDeps`; the router keeps the phase swap + `data-phase="archive"` contract; (b) Zulu timestamps — deterministic across machines, true to the flight-log world; (c) structured meta + right-aligned numeral — the brief's j-card line requires it, the placeholder's free-text line could not carry it; (d) display ellipsis — the marker T6 explicitly left to T11; (e) immediate per-entry delete — plan-compliant as assessed above; (f) focus-on-CONFIRM — the guarded-gesture intent, with Escape/KEEP one key away; (g) polite live region — honest outcomes, full design stays T12; (h) 2px focus literal — tokens.css is T8-owned, token-ization correctly flagged as follow-up rather than redefining T8's layer here.
  - Follow-ups correctly routed (none block acceptance): visual render pass (faces, right-margin alignment at every breakpoint, damped reveals, ≤40rem spine treatment) belongs to the finishing phase per the plan's own handoff; the `--focus-width` token and the VoiceOver live-region/activation checks belong to T12/finishing — all logged by the worker in the T11 entry.
  - T11 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). Milestone M3 (the world lands: T8, T9, T10, T11) is now T8 + T11 down; T9 → T10 remain on the critical path.

✅ T9 Write surface, fully committed — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: NEW `src/ui/write-surface.ts` (the write-surface module), `src/styles/write.css` (the surface's stylesheet, consuming tokens.css only), `src/ui/write-surface.test.ts` (T9 acceptance suite, `// @vitest-environment jsdom`). MODIFIED `src/ui/router.ts` (renderWrite/onWriteState/teardownSession wiring to the surface + `motionPreference`/`supports3d` pass-through deps; the placeholder write screen, in-router `statusLine`, and the in-router `formatClock` body are gone — `formatClock` now lives in write-surface.ts and is re-exported from router.ts for API stability), `src/ui/router.test.ts` (countdown assertions moved to the authoritative `[data-countdown-text]` SR node; NEW `write surface (T9)` describe with 4 live-loop tests; every T5/T7/T4 walkthrough and permanence assertion otherwise unchanged), `src/styles/main.css` (one added line: `@import './write.css';`). No other files; no new dependencies; setup/deleted/wind-down/archive phases untouched.
- Exported API (`src/ui/write-surface.ts`): `createWriteSurface({ doc, totalSessionMs, timer?, motionPreference?, supports3d? })` → `{ getElement(), getEditor(), update(state), destroy() }`; pure `formatClock(ms)` (the T5 formatter, moved here) and NEW `formatBoardClock(ms, minuteDigits)` (zero-padded `MM:SS` with the minute field padded to the session's fixed slot width). Types `WriteSurfaceDeps`/`WriteSurface`. Every dependency is injectable exactly like the router's seams: the setTimeout seam (flip completion + milestone revert — production default window.setTimeout, router passes ITS injected seam through), the motion probe (default reads T8's `--motion-level` token via getComputedStyle — the T8-locked JS seam), and the 3D probe (default `CSS.supports('transform-style', 'preserve-3d')`).
- THE BOARD (R2, exactly the committed technique): presiding top-center, `role="timer"` + `aria-label="Time remaining in session"` + `aria-atomic="true"`, NO aria-live attribute anywhere on it (implicit off prevents per-second spam). Inside: an aria-hidden glyph row — the engraved `T–` prefix (B612 400, bone-dim) + per-digit slots + a fixed non-flapping colon — and ONE visually-hidden `[data-countdown-text]` node carrying the full `M:SS` per tick. Each digit slot is fixed-width/fixed-height (CSS `1ch` × `1em`; 1ch = B612 Mono's shared digit advance) carrying FOUR glyph surfaces: static top/bottom halves + rotating top/bottom leaves. Flip (R2 geometry): static-top + leaf-bottom load the NEW glyph at flip start, the top leaf falls `rotateX(0→−90deg)` while the bottom leaf rises `rotateX(90→0deg)` (both `transform`-only transitions on `--dur-flap`/`--ease-flap`, `will-change: transform`, `backface-visibility: hidden`, `translateZ(0.1px)` — daformat's Safari fix), and a 300 ms timer (the JS twin of `--dur-flap`) settles static-bottom + leaf-top with a `data-reset` transition-none snap; a flip interrupted by a newer digit completes the old one instantly first (never two pending timers per slot). Ticks are never self-scheduled: every controller recompute (rAF frame, input, return event, boundary timer) arrives on `update()`, the board diffs the formatted string and flips only changed digits — wall-clock-driven by construction (R2 risk R5). Minute-slot count is fixed from the session length at mount (min 2 digits, 3 for ≥100 min) so the slot count can never change mid-session.
- MILESTONES (R2 / MDN egg-timer): at the final minute (remaining crosses 61→≤60 s) and final 10 s, the timer wrapper is TEMPORARILY promoted to `role="alert"` for ~1 s with the message ("One minute remaining." / "Ten seconds remaining."), then reverts to `role="timer"`; per-tick SR text is suppressed while announcing (a second content change mid-alert would re-announce); `aria-live` is never combined with the alert (VoiceOver iOS). The NEARER milestone wins a single recompute that jumps past both (hidden-tab return); a session that STARTS inside a threshold announces nothing (first update is the baseline, not a crossing); each fires once per session.
- THE FADE: the editor's `style.opacity` is written per recompute straight from `state.opacity` — rAF-driven, wall-clock-computed by the engine, never a CSS transition (the editor has no opacity transition anywhere). Threat display: `[data-threat]` on the section drives it all — armed: green LIVE lamp lit (`data-lit`), column leading hairline transparent (green only under `:focus-within` — the sanctioned focus green, "live circuit under your hands", plus the bone caret); fading: amber FADE lamp lights, green goes out, the column's leading hairline turns amber (1px `--lamp-amber` band, the field itself stays achromatic — cloud-edge), status placard turns amber. Lamps keep their `-dim` unlit glass (presence, not absence). Reduced motion NEVER writes opacity at all.
- REDUCED MOTION (settled path): no flap animation (static per-digit swap — the same code path as the @supports fallback), no animated opacity, no glow; instead a static amber caution placard `[data-reduced-banner]` ("SIGNAL FADING — KEEP TYPING", placard voice on panel-raised with the amber leading hairline — the archive confirm's visual grammar) + numeric inactivity countdown `[data-inactivity-countdown]` from T2 `deletesInMs`, CSS-gated to show ONLY under `prefers-reduced-motion: reduce` AND `[data-threat="fading"]`. Lamps and band keep switching (state, not motion). The same SR pattern (per-tick text + milestone alerts) serves both modes.
- CAPABILITY LAYERS (CSS): flap needs BOTH `@media (prefers-reduced-motion: no-preference)` AND `@supports (transform-style: preserve-3d)` — leaves are `display: none` by default and only the static halves render otherwise (exactly what the JS static path updates); glow (a faint bone halo on the board figures + lit-lamp dome glows) needs no-preference only. The perspective (1000px, damped) sits on the glyph row, `preserve-3d` on each slot; no grouping property (overflow/opacity/filter/clip-path) touches any preserve-3d element — glyph clipping lives on the half/leaf wrappers whose children never transform. Grain is NOT regated: T8 committed it as static texture that "carries no motion, so it survives reduced-motion untouched", and the brief's raised lines gate "glow/flap motion", not grain (logged as an interpretation, not a slip).
- EDITOR + HUD: bare `<textarea data-editor>` (never inside a `<form>` — asserted), `aria-label="Draft"`, placeholder "Keep typing. The clock does not wait.", Source Sans 3 at the 19 px dark-ground size / 1.65 leading / 70ch measure / large bone caret / themed thin scrollbar / bone-dim placeholder; the column owns the viewport's middle (flex, min-height 45vh, resize none — the console is not user-resizable). The remaining-time board is the ONLY stat on the surface — no word count anywhere. A `data-fade-announcement` polite live region exists EMPTY from mount (R2: the region must exist before content changes) — T12 owns its content.
- R3 SEAM: `destroy()` cancels every pending flip + milestone timer (idempotent; post-destroy `update()` is a silent no-op); the router calls it in `teardownSession` BEFORE dropping the editor reference, `startSession` always mounts a FRESH surface + controller + editor, and the T4 permanence sequence is untouched (all existing permanence tests pass byte-identical).
- Test coverage vs acceptance (24 new tests: 20 in write-surface.test.ts + 4 router-level): fade begins at the configured delay with opacity decaying at fake-clock points (4.999 s→1, exactly 5 s→1 fade start, 6 s→0.8, 7.5 s→0.5, 9 s→0.2, STANDARD); any text-changing input instantly restores opacity 1 + armed state + green lamp (live loop); lamp/band transitions green→amber at fade start and back (structural `[data-threat]`/`data-lit` assertions at both levels); milestone promotions through the live loop (role timer→alert→timer with the message, revert after ~1 s) and unit-level (freeze-while-announcing, once-each, cold-baseline silence, jump-fires-only-the-nearer); flap mode mid-flip glyph placement per the R2 geometry (changed slots only), completion settle, interrupted-flip instant completion; static fallback (no flip classes, no timers); reduced motion (opacity never written, banner + `deletesInMs` countdown content, lamps still switch, board ticks statically); R2 aria pairing structure (role/label/atomic, glyphs aria-hidden, one live text node, no aria-live on the wrapper); slot count fixed from session length (4 digits at ≤99 min, 5 at 120); editor never inside a form; fade-announcement hook present-and-empty; destroy seam (pending timers cancelled, no-op after, idempotent); zero layout shift structural (same slot ELEMENTS across ticks, count constant, stylesheet pinned: `.flap-slot` `width: 1ch`/`height: 1em`, transform-only flap transition, leaves default-hidden, both capability gates + the reduce+fading banner gate + the amber band rule — read via fs since vitest stubs CSS imports). Pure formatBoardClock table (padding, ceil, clamp).
- Validation evidence: `npm run test` — 8 files, 145/145 tests passed (20 write-surface, 19 router incl. the 4 new T9 integration tests, 8 permanence, 30 controller, 25 timing, 28 archive, 14 archive-screen, 1 smoke; ~1.1 s); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 16.49 kB gzip 3.74 kB / js 34.70 kB gzip 10.71 kB — the surface + sheet wired into the artifact). Dev-server smoke (`vite --port 5477`): `/`, `/src/ui/write-surface.ts`, `/src/styles/write.css` all HTTP 200 with the surface hooks in the served module and the gates in the served sheet; zero errors/warnings in the dev log; server stopped. Built-bundle check: `SIGNAL FADING`, `DELETION IN`, `Time remaining in session`, both milestone messages in dist js; `flap-slot`/`flap-leaf`/`write-caution`/`write-column`/`lamp-dome` classes + `prefers-reduced-motion:no-preference`, `@supports (transform-style:preserve-3d)`, `prefers-reduced-motion:reduce` all present in dist css (all gates survive minification); contract seed `da64ca22` still 1 in dist/index.html. Font-coverage check (fontTools on the shipped subsets): en dash U+2013 (the board's `T–`) and em dash U+2014 (the status line) are present in B612 Mono Bold/Regular and Michroma — no mid-string fallback faces. Contrast audit (detector cannot compute it): board bone on panel-inset ≈15:1; prefix/status/lamp-names/placeholder bone-dim on ground 5.46:1, on panel-raised 5.09:1; amber status/banner/count on ground 9.89:1, on panel-raised 9.21:1 — all ≥4.5:1.
- Deviations: (a) the write surface lives in its own module `src/ui/write-surface.ts` per the T11-established convention (injectable seams would bloat RouterDeps; the router keeps the phase swap + `data-phase` contract); (b) `formatClock` moved from router.ts to write-surface.ts and is re-exported — the SR text, the inactivity countdown, and router tests all consume one formatter; (c) the board shows zero-padded `T–MM:SS` while the SR text stays unpadded `M:SS` ("4:59" reads naturally; the padded form is for the fixed-slot board); (d) the reduced-motion numeric countdown is B612 Mono (`--size-numeric-lg`), NOT the optional DSEG14 — R4 keeps DSEG "optional" and its own risk note says segment faces read as "instrument" only at display sizes; deletion-urgency legibility at 19 px wins, and DSEG stays reserved for T10's SIGNAL LOST board at display scale; (e) grain is NOT regated behind reduced-motion (the worker contract said "glow/grain/flap"; T8's committed token layer and the brief's raised lines gate "glow/flap motion" only — grain is static texture by T8's own comment; leaving T8's layer untouched is the faithful reading); (f) the caution placard appears IN FLOW at fade start under reduce (no reserved empty band — a reserved gap would look broken during the armed phase); it is a one-time state appearance, not per-tick shift, and the "no layout shift while counting" acceptance targets the counting board; (g) `motionPreference`/`supports3d` were added to RouterDeps as pass-through capability seams (same injectable-seam pattern as clock/frame/timer — no test-only hooks; production defaults are the token read + CSS.supports); (h) FLIP_SETTLE_MS (300) is a named JS constant, the documented twin of `--dur-flap` — reading the token from computed style would be jsdom-opaque and browser-fragile for a value that must match a constant anyway.
- Screenshot / visual follow-ups (honest — actual rendered verification is the finishing phase's): a real-browser pass should capture (1) the flap mid-flip frame (top leaf falling, bottom rising, hinge seam, foreshortening at 1000px perspective), (2) the fade in flight (amber band + paling text + amber lamp/glow at several opacity points), (3) reduced-motion emulation (static board + caution placard + numeric countdown), (4) the board at 3/5/15/120-minute widths and ≤40rem, (5) Safari backface check + a minute of ticking in Chrome Layers/FPS panel (R2's own QA additions: cost, Safari translateZ fix, Layers budget), and (6) VoiceOver/NVDA on the timer region + milestone alerts (T12). jsdom pins structure and behavior only — it lays nothing out.
- Follow-ups: T10 consumes `data-threat` conventions for its boards (DSEG14 stays reserved for SIGNAL LOST at display size); T12 fills `data-fade-announcement` content (fade start / deletion / disarm announcements), verifies focus visibility of the editor (column-edge green + caret — WCAG 2.4.7/2.4.11 judgment), and runs the VoiceOver pass on the timer region; the finishing phase owns the screenshot pass above and the `--focus-width` tokenization question flagged by T11.
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Detector note: the mandated `detect.mjs --json` pass over the changed UI files (`src/ui/write-surface.ts`, `src/styles/write.css`, `src/ui/router.ts`, `src/styles/main.css`) returned zero findings — in its own disclosed DEGRADED mode (htmlparser2/css-tree et al. not installed in this project → regex fallback; custom properties and computed contrast not evaluated), i.e. an undercount by its own warning, as in T8/T11; the finishing phase's detector pass over the built screens remains the real gate.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 8 files, 145/145 tests passed (20 write-surface, 19 router incl. the 4 new T9 integration tests, 8 permanence, 30 controller, 25 timing, 28 archive, 14 archive-screen, 1 smoke, 1.19 s); `rm -rf dist && npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 16.49 kB gzip 3.74 kB / js 34.70 kB gzip 10.71 kB — surface + sheet wired into the artifact); `npx vite --port 5513 --strictPort` + curl smoke → `GET /` HTTP 200 (contract seed `da64ca22` → 1), `GET /src/ui/write-surface.ts` HTTP 200 (23 board/flap/milestone hook hits in the served transform), `GET /src/styles/write.css` HTTP 200 with BOTH capability gates + the reduce gate present in the served sheet; dev-server log zero errors/warnings; server stopped after check. Built-bundle check: `SIGNAL FADING`, `DELETION IN`, `Time remaining in session`, both milestone messages present in dist js; `flap-slot`/`flap-leaf`/`write-caution`/`write-column`/`lamp-dome` classes + `prefers-reduced-motion:no-preference` (×1) + `prefers-reduced-motion:reduce` (×2) + `@supports (transform-style:preserve-3d)` all present in dist css — every gate survives minification.
  - R2 technique verified from source (`src/ui/write-surface.ts` + `src/styles/write.css`), implemented exactly as committed: per-digit fixed slots each carrying FOUR glyph surfaces (static top/bottom halves + rotating leaves); flip geometry = static-top + leaf-bottom load the NEW glyph at flip start, top leaf `rotateX(0→−90deg)` falls while bottom leaf `rotateX(90→0deg)` rises (daformat's exact angles), `transform-origin` at the digit midline (`50% 100%` / `50% 0%`), `backface-visibility: hidden` + daformat's `translateZ(0.1px)` Safari fix, `will-change: transform` on leaves only, transform-only transition on `--dur-flap`/`--ease-flap`, JS twin `FLIP_SETTLE_MS = 300` settles static-bottom + leaf-top behind a `data-reset` transition-none snap; an interrupted flip completes instantly first (test-pinned: exactly one pending settle timer per slot); `perspective: 1000px` on the glyph row, `preserve-3d` on the slot with NO grouping property on any preserve-3d element (overflow clipping lives on the half/leaf wrappers whose `.flap-glyph` children never transform — R2 risk R1's mitigation honored); ticks never self-scheduled — every controller recompute arrives on `update()` which diffs the board string and flips only changed digits (R2 risk R5).
  - Fallback wiring verified: leaves are `display: none` by default and only render inside `@media (prefers-reduced-motion: no-preference)` + `@supports (transform-style: preserve-3d)` — exactly the static halves the JS static path (`setStatically`) updates; the JS gate mirrors it (`flapMode = motion === 'full' && supports3d`, probes injectable with production defaults = T8's `--motion-level` token read + `CSS.supports`).
  - rAF opacity verified NOT a CSS transition: `router.ts` `onWriteState` (subscribed to the controller's per-recompute `onState` channel) calls `surface.update(state)`, which writes `editor.style.opacity = String(state.opacity)` per recompute from engine math; grep of write.css → zero transitions naming opacity and zero transitions of any kind on `.write-editor` (the only transitions are state colors: lamp-dome background, status color, column border-color). The plan's "never a CSS transition left to run unwatched" holds structurally.
  - Acceptance coverage verified from test source: fade decay pinned at fake-clock points through the live loop (router test: 4.999 s → 1, exactly 5 s → 1 at fade start, 6 s → 0.8, 7.5 s → 0.5, 9 s → 0.2 for STANDARD — `toBeCloseTo(…, 10)`); instant restore (typing at fade → opacity 1 + threat armed + green lamp + ARMED status, asserted synchronously with no frame wait); lamp/band structural transitions (data-threat armed→fading→armed, live/fade `data-lit` exclusivity, at both unit and router level); reduced-motion emulation (`motion: 'reduced'` injected through RouterDeps → `data-flipping` NEVER set, `editor.style.opacity === ''` — opacity never written, banner `SIGNAL FADING` + `DELETION IN` content, numeric countdown from `deletesInMs` — BRUTAL 6 s loss at 4 s idle → '0:02' — lamps still switching, typing still restoring); fixed-width slots (same slot ELEMENTS across ticks, count fixed from session length — 4 digits ≤99 min, 5 at 120 — plus stylesheet pins read via fs: `.flap-slot` width 1ch/height 1em, transform-only flap transition, leaves default-hidden, both capability gates, the reduce+fading banner gate, the amber band rule); R2 aria pattern (role=timer + aria-label + aria-atomic, NO aria-live on the wrapper, one visually-hidden `[data-countdown-text]` node outside the aria-hidden glyph row updated per tick, glyphs aria-hidden, milestone role timer→alert→timer swap with message + ~1 s revert + per-tick suppression while announcing, once-each, cold-baseline silence, jump-past-both fires only the nearer); T4 permanence seam + no-form rule intact (all 8 permanence tests + walkthroughs unchanged and green; `editor.closest('form') === null` asserted; `teardownSession` calls `surface.destroy()` before dropping refs; `renderWrite` mounts a FRESH surface per session).
  - Surface criteria from the brief verified from source: amber hairline band rides the column's LEADING EDGE (`[data-threat='fading'] .write-column { border-left-color: var(--lamp-amber) }`) while the field stays achromatic (editor background transparent, color bone); green only under `:focus-within` (the sanctioned focus green) and outranked by amber while fading; glow subtle (18px text-shadow at 18% bone on board figures; 10px/2px dome glows at 50% lamp color) and gated behind no-preference ONLY — entirely absent under reduce; no word count or stats anywhere (grep — zero hits in write-surface.ts/write.css); prose measure `--prose-measure: 70ch` (within 65–75ch) at 19px/1.65 in Source Sans 3; the countdown board is the only HUD element (board + lamp/status strip + reduce-only placard + SR-only announcement hook); reduced path = static amber banner + numeric inactivity countdown per the brief's settled line.
  - Coordinator-flagged deviations assessed against the brief's RAISED LINES and R2/R4 records: (e) grain NOT regated — brief §3 (arcade) gates "glow/flap MOTION", brief §6 names "fade/glow" for the reduced swap; T8's own committed layer (predating T9) documents grain as "Static texture — carries no motion, so it survives reduced-motion untouched"; an unanimated feTurbulence tile is not motion — ACCEPT, leaving T8's layer untouched is the faithful reading. (d) reduced-motion countdown in B612 Mono, not DSEG14 — plan T9's R4 lock line literally reads "countdown/timing readouts in B612 Mono"; R4 marks DSEG14 "optional" and plan T10 reserves it for the SIGNAL LOST board ("may use", "never for the animated flap countdown"); B612 Mono is the genuine cockpit face with binary-verified tabular digits — ACCEPT, DSEG stays correctly reserved for T10. (f) caution placard appears IN FLOW once at fade start under reduce — the acceptance targets "no layout shift WHILE COUNTING (tabular numerals)", and the counting board is structurally shift-free; the placard is a one-time state-transition appearance (display-gated reduce+fading), not a per-tick shift, and under full motion fade start changes border-color only (zero layout impact); a permanently reserved band would render a broken empty strip through the armed phase — ACCEPT.
  - Minor observation (accepted, no criterion attached): the opacity write is gated on `flapMode` (full motion AND preserve-3d), so in a hypothetical no-3D browser the fade would not animate — but `preserve-3d` is Baseline since Sept 2015 (every evergreen browser passes both gates; jsdom is the only place the static path runs), lamps/band/status still carry the threat there, and neither the plan nor R2 mandates animating opacity when 3D is unsupported.
  - File inventory (mtime): the T9 turn (02:36–02:42) touched exactly the six logged files — NEW `src/ui/write-surface.ts` + `src/styles/write.css` + `src/ui/write-surface.test.ts`, MODIFIED `src/ui/router.ts` (renderWrite/onWriteState/teardownSession wiring + capability pass-throughs; deleted/wind-down/setup boards untouched) + `src/ui/router.test.ts` + `src/styles/main.css` (the one `@import './write.css';` line); tokens.css/index.html/package.json predate the turn (T8, 01:46–01:47); no new dependencies, no stray files.
  - T9 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). Milestone M3 (the world lands: T8, T9, T10, T11) is now T8 + T11 + T9 down; T10 remains on the critical path.

✅ T10 Deleted beat + wind-down board — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: NEW `src/ui/outcome-screens.ts` (both committed outcome boards), `src/styles/outcome.css` (their stylesheet, tokens-only, imported by main.css). MODIFIED `src/ui/router.ts` (`renderDeleted`/`renderWindDown` now mount the committed screens; `RouterDeps` grows three wind-down environment pass-throughs `clipboard`/`execCommand`/`objectUrls`, same injectable-seam pattern as T9's `motionPreference`/`supports3d`; module docstring updated; setup/write/archive phases byte-untouched), `src/ui/router.test.ts` (NEW `outcome boards (T10)` describe — 5 tests; every T5/T7/T4/T9 walkthrough otherwise unchanged and green), `src/styles/main.css` (one line: `@import './outcome.css';`). No other files; no new dependencies.
- Exported API (`src/ui/outcome-screens.ts`): `createDeletedScreen({ doc, onReArm })` → `{ section, focusReArm() }` (focus AFTER mount — focusing a detached node is a no-op); `createWindDownScreen({ doc, text, durationSec, totalSessionMs, endedAt, note, onDone, timer?, clipboard?, execCommand?, objectUrls? })` → `{ section, textarea, note }` (the router keeps its disarm-time-save/Done-update data flow and its `windDownRefs` shape); pure `windDownFileName(endedAt)` (`draft-YYYYMMDDTHHMMZ.txt`, code-derived from the disarm instant — never from draft text). Reuses the exported seam types from archive-screen (`ClipboardWriter`, `ObjectUrlSeam`) and the pure `formatRun`/`formatBoardClock`/`deriveWordCount` helpers — one definition of each across the console.
- DELETED — SIGNAL LOST, one deliberate red beat: the board mounts AFTER the T4 permanence sequence and controller teardown (router order unchanged: `destroyEditor → armUndoInterceptor → teardownSession → renderDeleted` — the same synchronous transition, so there is structurally no flash of content; `root.children === 1` pinned by test). The console EMPTIES: no editor anywhere in the document (tested), no stats, nothing but the annunciator, one honest line, one action. The readout is the reserved DSEG14 Classic face (R4: this board only, display size) via `--font-instrument` whose stack carries the legible B612 Mono fallback (T8 token); sizing `min(calc(--size-numeric-board * 0.82), 9vw)` — DSEG14's shared 0.816 em advance was verified in the shipped binary (fontTools 4.63), so 11 glyphs ≈ 8.36 em holds ONE line from 320 px phones to the 4.51 rem desktop cap. THE BEAT: the red LOSS lamp LATCHES — one keyframe animation (`loss-latch`, 1.4 s) doing three hard on/off switches via `steps(1, end)` then resting steady-lit, exactly how a real annunciator latches a new fault; no opacity anywhere, base state = lit so the board is fully readable the instant it lands. RE-ARM (the console's primary plate) is FOCUSED the moment the board mounts (`document.activeElement` pinned by test); the T4 interceptor stays armed on the board (undo still dies in capture — pinned) and disarms on RE-ARM → fresh setup (pinned). Copy is the placard voice, honest, no snark, no fake telemetry: "The draft was deleted when the signal went quiet past the loss limit. Nothing was archived. There is no recovery."
- WIND-DOWN — DOWN SAFE, the board's second side (j-card): the whole face (frozen clock + strip) is one rigid board that TURNS OVER — `board-flip`, a damped `rotateX(-88deg → 0)` on `--dur-board`/`--ease-damped`, transform-only, NOT a fade cross-fade (test pins the keyframes contain rotateX and no opacity). The frozen clock is the SAME instrument as the write board — `.flap-board`/`.flap-prefix`/`.flap-slot`/`.flap-colon` reused from write.css with the static-half rendering (both halves carry the digit, the write board's own fallback path), stopped at 0:00, minute-slot count fixed from the session length (`T–00:00`, `role="img"` + aria-label "Session clock, stopped at 0:00"). Green SAFE lamp lit (state, not motion — switches in both modes), h2 `DOWN SAFE — THREAT DISARMED`. The draft stays editable in the reused `.write-column`/`.write-editor` (fresh textarea seeded with the disarm text — T5's established semantics preserved; the controller is already destroyed: 0 pending frames/timers re-pinned). Post-session stats HERE ONLY (never during write — asserted during the write phase): a LIVE word count on T6's own `deriveWordCount` (the number DONE will persist) + the RUN time `formatRun(durationSec)` riding the right margin (`margin-left: auto`, stylesheet-pinned — j-card). COPY + DOWNLOAD .TXT operate on the CURRENT draft (wind-down edits included, pre-Done) through the archive screen's exact clipboard/legacy-execCommand/object-URL plumbing with transient COPIED/COPY FAILED states + a polite live region (`data-winddown-status`) that names failures honestly. DONE finalizes the disarm-time entry via `finishWindDown` (T6 `update` — updated, never duplicated; wordCount/title/excerpt re-derived — pinned) and lands on the T11 archive view; closing without DONE leaves the disarm-time entry (the entry exists at disarm — re-pinned before Done in the new test).
- REDUCED MOTION (both boards): every `animation:` declaration in outcome.css lives INSIDE one `@media (prefers-reduced-motion: no-preference)` block — the test brace-extracts that block and proves each animation declaration appears inside it and NOWHERE else in the sheet; no reduce block exists in this sheet at all (the animations simply do not exist under reduce; T8's token collapse additionally zeroes the durations). The lamp colors, the DSEG14 readout, the frozen clock, the stats all render identically — lamps are state, not motion.
- Test coverage vs acceptance (5 new tests in `router.test.ts`, all through the live router with the fake clock/frames/timers + now clipboard/objectUrl mocks): ONE-sequence deletion (single screen child, SIGNAL LOST present, zero textareas in the document, LOSS lamp lit, honest copy containing "Nothing was archived"/"no recovery", archive empty, RE-ARM `document.activeElement`, historyUndo still `defaultPrevented` on the board, guard disarmed + fresh setup after RE-ARM); wind-down board content (frozen `T–`/00/00/: slots each reading the digit on both halves, SAFE lamp, DOWN SAFE title, editable draft, 0 pending frames/timers, `[data-stats]` ABSENT during the write phase, `[data-words]` live-updating on input via T6 semantics, `[data-run]` "1:00"); DONE finalization (entry id stable, text/title/wordCount updated not duplicated, wordCount differs from the disarm value, archive row shows run "1:00" + the updated word count, phase → archive); copy/download through mocked seams (clipboard.writeText receives the post-edit draft, COPIED transient + announcement; download blob `text/plain;charset=utf-8` of the current draft, anchor `download` = `windDownFileName(disarmInstant)`, href = object URL, revoked after click, announced — anchor click captured via document-capture listener, same technique as the archive tests); reduced-motion stylesheet pins (all animations inside the no-preference gate + nowhere else, no reduce block in the sheet, board-flip = rotateX without opacity, loss-latch = background-color steps without opacity, `steps(1, end)` inside the gate, `.outcome-stat-run { margin-left: auto }`, `.outcome-readout` uses `var(--font-instrument)`).
- Validation evidence: `npm run test` — 8 files, 150/150 tests passed (24 router incl. the 5 new T10 tests, 20 write-surface, 14 archive-screen, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke; ~1.5 s); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 16.49→20.98 kB gzip 4.23 kB / js 34.70→40.88 kB gzip 11.91 kB — outcome screens + sheet wired into the artifact). Dev-server smoke (`vite --port 5621`): `/`, `/src/ui/outcome-screens.ts`, `/src/styles/outcome.css` all HTTP 200 with SIGNAL LOST/DOWN SAFE in the served module; zero errors/warnings in the dev log; server stopped. Built-bundle check: `SIGNAL LOST`, `DOWN SAFE`, `RE-ARM`, `FINALIZE ENTRY`, `Nothing was archived` in dist js; `board-flip`, `loss-latch`, `outcome-readout`, `outcome-stat-run` + `prefers-reduced-motion:no-preference` (×2 — write's gate + outcome's gate) + `DSEG14 Classic` in dist css — all markers survive minification; contract seed `da64ca22` still 1 in dist/index.html.
- Deviations: (a) BOTH outcome screens live in one module `src/ui/outcome-screens.ts` (not one-per-screen as T9/T11) — they are one task's two sides sharing the lamp/board grammar and the copy/download plumbing; the seam TYPES still come from archive-screen.ts, one definition each; (b) the wind-down board reuses write.css's instrument classes and archive.css's `.plate` controls instead of restating them (CSS is global once imported; outcome.css documents this in its header) — one console, one vocabulary, and a finishing-phase restyle of any screen carries the outcomes with it; (c) `SIGNAL LOST` is the `<h2>` itself, set in DSEG14 — 14-seg letterforms carry the heading; the accessible name is the plain text; (d) readout sizing is a calc on `--size-numeric-board` rather than a new token (screens do not mint tokens; the 0.82 factor + 9 vw cap keep the 11-glyph readout on one line, advance verified in the binary); (e) the beat is the LAMP latch, not a board-level flash — one authored moment per board (craft floor), starting AND ending from the already-lit base state so reduce loses nothing but the flash; (f) `RouterDeps` grows `clipboard`/`execCommand`/`objectUrls` pass-throughs (T9 precedent for capability seams; production defaults are navigator.clipboard / document.execCommand / URL statics inside outcome-screens.ts; `main.ts` unchanged); (g) the wind-down editor is a FRESH textarea seeded with the disarm text (T5's semantics preserved verbatim — the write-phase node dies with the write screen, which is also what keeps the wind-down field's undo history empty and the R3 fresh-instance discipline intact).
- Honest omissions (scoped elsewhere, not slipped): no aria-live content for the deletion/disarm transitions themselves (T12 owns announcement content — the region exists in T9's surface; wind-down's own copy/download live region IS filled); no focus management on disarm (T12's listed "Done focused on disarm" decision — currently focus falls to the document on the screen swap; logged as follow-up); no ghost-segment layer behind the DSEG14 readout — the all-on glyph (`~`, verified in the binary) would render as literal tildes in the B612 fallback when DSEG14 is missing, so the honest fallback beat the flourish (a font-detection-gated ghost row is a finishing-phase option).
- Screenshot / visual follow-ups (honest — jsdom lays nothing out): a real-browser pass should capture (1) the deletion beat — the annunciator latch mid-flash and latched, the DSEG14 readout's segment rendering + baseline (segment faces sit high in the em box; the bed's lower-lip padding is an estimate), (2) the tape flip mid-rotation (perspective foreshortening on the frozen clock), (3) both boards under `prefers-reduced-motion: reduce` (no animation, lamp lit, board flat), (4) the readout at 320–40rem widths vs the 9 vw cap, (5) the wind-down stats' right-margin alignment against the column edge at phone widths, and (6) VoiceOver on the readout h2 + the frozen clock's img label (T12).
- Follow-ups: T12 — deletion/disarm announcement content, the disarm focus decision (Done vs editor), 2.4.11 focus appearance on outcome plates (the `--focus-width` tokenization T11 already flagged), keyboard-only pass over RE-ARM/DONE/copy/download; T13 unchanged (manual per-browser undo protocol — RE-ARM focus + interceptor behavior already pinned here); finishing phase owns the screenshot pass + the optional ghost-segment flourish + the DSEG14 padding tune.
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Detector note: the mandated `detect.mjs --json` pass over the changed UI files (`src/ui/outcome-screens.ts`, `src/styles/outcome.css`, `src/ui/router.ts`, `src/styles/main.css`, `src/ui/router.test.ts`) returned `[]` — zero findings in its own disclosed DEGRADED mode (htmlparser2/css-tree not installed → regex fallback; custom properties and computed contrast unevaluated), i.e. an undercount by its own warning, as in T8/T9/T11; the finishing phase's detector pass over the built screens remains the real gate. Manual contrast against T8's precomputed pairs: readout lamp-red on panel-inset 5.06:1 (large text — display size, ≥3:1 required, comfortably over 4.5), verdict/note/stat-labels bone-dim on ground 5.46:1 / on panel-raised 5.09:1, stat values bone on ground 14.25:1.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 8 files, 150/150 tests passed (24 router incl. the 5 new T10 tests, 20 write-surface, 14 archive-screen, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke, 1.31 s); `rm -rf dist && npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 20.98 kB gzip 4.23 kB / js 40.88 kB gzip 11.91 kB — outcome screens + sheet wired into the artifact); `npx vite --port 5733 --strictPort` + curl smoke → `GET /` HTTP 200 (contract seed `da64ca22` → 1), `GET /src/ui/outcome-screens.ts` HTTP 200 with SIGNAL LOST / DOWN SAFE / RE-ARM / FINALIZE ENTRY / "Nothing was archived" / "no recovery" in the served module, `GET /src/styles/outcome.css` HTTP 200 with board-flip / loss-latch / outcome-readout / outcome-stat-run / font-instrument present; dev-server log zero errors/warnings; server stopped after check. Built-bundle check: `SIGNAL LOST` + `DOWN SAFE` in dist js, `DSEG14 Classic` + both outcome keyframe names in dist css, contract seed still 1 in dist/index.html.
  - All 5 new T10 tests verified genuine from source (`src/ui/router.test.ts` "outcome boards (T10)"), every contracted coverage area real: (1) deleted mounts after permanence as ONE sequence — single screen child (`root.children.length === 1`), `[data-editor]` gone AND `document.querySelectorAll('textarea')` length 0 (zero textareas anywhere in the document), SIGNAL LOST present, LOSS lamp `data-lit`, honest copy asserted ("Nothing was archived" + "no recovery"), `[data-stats]` null, archive stays empty; (2) RE-ARM is `document.activeElement` the moment the board mounts, `historyUndo` `defaultPrevented === true` ON the deleted board (T4 interceptor still armed — R3's deletion→RE-ARM lifetime), and after RE-ARM click → setup + a fresh `historyUndo` NOT prevented (guard disarmed on restart); (3) wind-down — threat fully off (`h.frames.size === 0` and `h.timers.size === 0`), draft editable (`disabled === false`, value === textAtDisarm) in a fresh `[data-winddown-text]` textarea, disarm-time archive entry already exists (`list()` length 1 BEFORE Done — closing without DONE keeps the 0:00 version, the plan's acceptance line), frozen clock (4 static slots each reading '00' on both halves, `T–` prefix, colon, `role="img"` aria-label "stopped at 0:00"), SAFE lamp lit, DOWN SAFE title; (4) stats HERE ONLY — `[data-stats]` asserted NULL while the phase is still `write`, then WORDS live-updating on input through T6's own `deriveWordCount` and RUN "1:00" in wind-down; (5) DONE updates-not-duplicates — entry id stable, still exactly 1 entry, text/title/wordCount re-derived from the final draft, wordCount differs from the disarm value (edits provably took), lands on the T11 archive view with the finalized row (run "1:00" + updated word count); (6) copy/download through the mocked seams on the CURRENT draft — post-disarm edit included in the `clipboard.writeText` call, COPIED transient + polite announcement, download blob `text/plain;charset=utf-8` with byte-exact current text, anchor `download` === `windDownFileName(disarmInstant)` (code-derived, never from draft text), href === object URL, revoked after click; (7) reduced-motion — brace-extracted `@media (prefers-reduced-motion: no-preference)` block proving every `animation:` declaration sits inside it and nowhere else in the sheet, no reduce block exists at all, `board-flip` contains rotateX and NOT opacity, `loss-latch` is background-color `steps(1, end)` and NOT opacity, `.outcome-stat-run { margin-left: auto }` and `.outcome-readout` → `var(--font-instrument)` stylesheet-pinned.
  - DSEG14 sizing math re-verified IN THE SHIPPED BINARY (fontTools, my own run): letters 'S/I/G/N/A/L/O/T' all advance 816/1000 = 0.816 em, the SPACE is 200/1000 = 0.2 em → "SIGNAL LOST" = 10 × 0.816 + 0.2 = 8.36 em exactly as the worker claimed (the narrow space correctly accounted, not eyeballed). With `font-size: min(calc(--size-numeric-board × 0.82), 9vw)` and the board token `clamp(2.75rem, 9vw, 5.5rem)` the readout holds ONE line (`white-space: nowrap`) from the 320 px phone floor (readout ≈ 273 px, filling the instrument edge-to-edge at exactly 320 px with <1 px of slack) to the 4.51 rem desktop cap; token stack `'DSEG14 Classic', 'B612 Mono', monospace` confirmed at tokens.css line 162 — the legible fallback. The 320-px-floor edge fit is a finishing-pass screenshot item the worker already lists (readout at 320–40rem widths vs the 9vw cap); no criterion attached.
  - The board flip verified a HARD side-change: `@keyframes board-flip { from { transform: rotateX(-88deg) } to { transform: none } }` — transform-only, zero opacity anywhere in the keyframes, `perspective: 1200px` on the wrap, one iteration. The frozen clock reuses write.css's static `.flap-board`/`.flap-slot` rendering (both halves carry the digit — the write board's own fallback path; no leaves exist on it in any mode).
  - THE BEAT assessed against the brief's intent ("single red beat", deliberate, not glitchy): the LOSS lamp LATCHES — one 1.4 s authored animation, `steps(1, end)` hard switching (lamps switch, they never fade — the brief's own mechanical-motion vocabulary), three dark pulses at regular ~150–170 ms intervals, then resting steady-lit; iteration count 1 (never loops); the base state is ALREADY lit so the board is fully readable the instant it mounts and after the animation ends (no unreadable moment); under reduce the animation simply does not exist (board arrives fully lit). This is the mission-control-native reading — a real annunciator attracts with a bounded flash sequence then latches steady — and it is NOT a glitch/panic read: no strobe loop, no irregular timing, no terminal-less flicker, and the DSEG14 readout itself never animates (only the small lamp dome switches). ACCEPTED: "one deliberate beat" honored as one authored, terminal, mechanical moment. The ghost-segment flourish was declined for an honest, correct reason (the all-on glyph would render as literal tildes in the B612 fallback — a defect in the fallback world; the flourish remains a finishing-phase option).
  - Honest copy verified from source: deletion copy is plain, factual, no snark, no fake telemetry ("The draft was deleted when the signal went quiet past the loss limit. Nothing was archived. There is no recovery."); wind-down note names the real state ("The 0:00 draft is archived. Edits here are safe until you press DONE.") or the named failure kind with a retry path; copy/download failures are named with recovery. Lamp semantics respected: `--lamp-red` appears in outcome.css ONLY on the LOSS lamp dome (+ its latch keyframes/glow) and the SIGNAL LOST readout — the destruct/signal-lost reservation, which this board IS; amber appears ZERO times in the sheet; green only on the SAFE lamp + the sanctioned focus ring; the glow layers sit inside the no-preference gate. textContent-only rendering holds (zero innerHTML/insertAdjacentHTML/outerHTML/DOMParser in outcome-screens.ts; the download filename derives from the disarm instant, never from draft text).
  - Copy/download seams verified injectable with guarded production defaults (`navigator.clipboard` if present / `document.execCommand` if callable / URL statics if present), legacy select+execCommand fallback with the scratch textarea removed after, busy-state guard, `isConnected` guards after the async clipboard await; `RouterDeps` grows only the three logged pass-throughs — `main.ts` unchanged, no test-only hook in any production path.
  - Phases untouched verified by mtime inventory: the T10 turn (03:00–03:03) touched exactly the five logged files (NEW `src/ui/outcome-screens.ts` + `src/styles/outcome.css`, MODIFIED `src/ui/router.ts` + `src/ui/router.test.ts` + the one `@import './outcome.css';` line in `src/styles/main.css`); write-surface (02:42), archive-screen (02:11), tokens.css/index.html (01:47), main.ts (00:54), package.json (01:46) all predate the turn — setup/write/archive phases byte-untouched, no new dependencies, no stray files. Router order in `handleDeleted` is the T4-locked sequence unchanged (`destroyEditor → armUndoInterceptor → teardownSession → renderDeleted → focusReArm` after mount); `finishWindDown` updates the disarm-time entry (save fallback only if the disarm save failed) and lands on the archive view.
  - Honest omissions assessed as correctly routed, not unmet criteria: deletion/disarm aria-live content and the disarm focus decision (Done vs editor) are T12's own contracted scope ("aria-live announcements (fade start, deletion, disarm), focus management (RE-ARM focused on deletion; Done focused on disarm)") — and the R3-LOAD-BEARING half, RE-ARM focused immediately with the interceptor armed until RE-ARM, IS implemented and test-pinned here. The `data-fade-announcement` region from T9 remains present for T12's content.
  - All three plan acceptance lines verified: deletion renders in one deliberate sequence with no flash-of-content (single synchronous transition, single screen child, zero textareas — test-pinned); wind-down edits persist via Done (id-stable update, re-derived metadata — test-pinned); closing without Done leaves the 0:00 version in archive (test-pinned at wind-down before Done). Research-locked lines hold: R3's RE-ARM = fresh surface + immediate focus + interceptor lifetime; R4's DSEG14 condition-gated to THIS board only (sole `--font-instrument` consumer), never the animated flap countdown.
  - Logged deviations (a)–(g) reviewed and all accepted: one module for both screens (one task's two sides sharing grammar + seams); reuse of write.css instrument classes + archive.css plates (one console, one vocabulary); SIGNAL LOST as a DSEG14 h2 with plain-text accessible name; the sizing calc on the T8 token rather than minting a new one (math binary-verified); the lamp-latch beat (assessed above); the three capability pass-throughs (T9 precedent); the fresh wind-down textarea (T5 semantics + R3 fresh-instance discipline + empty undo history in the safe field).
  - T10 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). Milestone M3 (the world lands: T8, T9, T10, T11) closes; T12 (deps T9+T10+T11) and T13 (deps T4+T10) unblock.

✅ T12 Accessibility pass — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: MODIFIED `src/ui/router.ts` (T12 state-announcement regions + `mountScreen()` + focus management + `[data-error]` role=status + exported announcement constants; module docstring documents both contracts), `src/ui/write-surface.ts` (fade-start announcement content fired once per fade onset in T9's region, cleared on restore; `FADE_START_ANNOUNCEMENT` export; visually-hidden phase h1), `src/ui/outcome-screens.ts` (phase h1s on both boards; `focusDone()` on the wind-down screen), `src/ui/archive-screen.ts` (FLIGHT LOG h2→h1, entry titles h3→h2), `index.html` (`<div id="app">`→`<main id="app">` — the one main landmark; contract comment untouched, still first child of body), `src/styles/main.css` (document-level `.vh` utility so no screen depends on another's sheet), `src/styles/write.css` (the column-edge focus band 1px→2px, constant in every state — WCAG 2.4.11 sizing, zero layout shift, padding gives the 1px back), `src/ui/router.test.ts` (children-count/h3 selectors updated for the new structure; NEW `announcements (T12)` 5-test + `keyboard walkthrough (T12)` 4-test describes), `src/ui/archive-screen.test.ts` (h3→h2 selector). NEW `src/ui/a11y.test.ts` (axe-core over jsdom per phase + document-structure describes), `src/styles/contrast.test.ts` (computed contrast table from tokens.css + reduced-motion parity across all four sheets + focus-appearance pins). MODIFIED `package.json` + `package-lock.json` (devDependency `axe-core` ^4.13.0). No other files; no visual redesign anywhere.
- STATE ANNOUNCEMENTS (town-hall mitigation #1, made real): (1) FADE START — polite, in T9's `data-fade-announcement` region (exists empty from mount per R2); the surface tracks the previous phase on `update()` and writes `FADE_START_ANNOUNCEMENT` ("Signal fading — the draft is starting to pale. Keep typing to restore it.") exactly ONCE per armed→fading onset; the fading→armed restore CLEARS it, which is what guarantees the next onset is a real content change (live regions announce changes, not repeated identical text; no timing-fragile clear-and-reinject). The per-tick timer never touches it (the board keeps its own R2 pattern: role=timer + milestone role=alert promotions, unpinned here). (2) DELETION — assertive: a router-owned `role="alert"` region (`data-announcements="alert"`) that SURVIVES every screen swap (mounted forever after the section — T9's region dies with its section, so terminal events needed a region that outlives it); `handleDeleted` writes `DELETION_ANNOUNCEMENT` ("Signal lost — the draft was deleted. Nothing was archived. There is no recovery.") after the board mounts. (3) DISARM — polite, router-owned `role="status"` region: `DISARM_ANNOUNCEMENT` ("Down safe — threat disarmed. The draft is archived.") or the honest `DISARM_FAILURE_ANNOUNCEMENT` (names the DONE retry) when the disarm-time save fails. `mountScreen()` CLEARS both regions on every screen mount — an empty region makes each transition's same-text announcement a content change (test-proven across a two-session loop). Politeness: assertive reserved for loss, polite for recoverable/informational.
- FOCUS MANAGEMENT (every screen swap lands deliberately, all test-pinned on `document.activeElement`): ARM → the editor focused (the writer's hands go to the text; the threat clock is tied to typing); deletion → RE-ARM focused (T10/R3, already present, re-pinned); disarm → DONE focused (the plan's own "Done focused on disarm" — the console's primary circuit; the editor stays one Tab away; noted trade-off: Enter now finalizes immediately, which is the one-motion spirit of the wind-down); DONE → the finalized entry's VIEW focused on the archive (the payoff first; Tab continues the row's copy/download/delete); VIEW ARCHIVE → NEW SESSION focused; RE-ARM / NEW SESSION → ARM focused (the loop is one motion from every exit). Focus is always set AFTER mount (detached focus is a no-op). Keyboard walkthrough (new describe) pins Tab order per screen: setup [duration:checked, custom field, preset:checked, ARM, VIEW ARCHIVE] (unchecked radios skipped by native rules), write [editor only], wind-down [draft, COPY, DOWNLOAD, DONE], archive [NEW SESSION, per-entry view/copy/download/delete, CLEAR] (hidden confirm controls excluded while closed); the full survive loop and the deletion loop each run start-to-end with focus asserted at every hop; the T11 confirm-panel focus discipline (CONFIRM on open, CLEAR on cancel, Escape) remains green.
- CONTRAST — COMPUTED, not eyeballed (jsdom/axe cannot run the cascade, so `src/styles/contrast.test.ts` parses tokens.css and computes WCAG relative luminance; color-mix(in srgb) resolved as the spec defines it for the srgb space — weighted average of gamma-encoded components). Every text/lamp pair actually rendered (fg on bg → ratio, threshold): bone/ground **14.25** (4.5) body prose, entry titles/runs, stat values, drafts; bone/panel-raised **13.28** (4.5) flap-board digits + frozen clock, FLIGHT LOG; bone/panel-inset **14.99** (4.5) full-draft well; bone-dim/ground **5.46** (4.5) lamp names, status, placeholder, meta/excerpts, notes, plate text; bone-dim/panel-raised **5.09** (4.5) T– prefix, caution label, archive notes, confirm body; lamp-green/ground **10.44** (4.5) COPIED transient; lamp-amber/ground **9.89** (4.5) FADING status + COPY FAILED; lamp-amber/panel-raised **9.21** (4.5) reduced-motion caution title/countdown, confirm title; lamp-red/ground **5.43** (4.5) CONFIRM CLEAR; lamp-red/panel-inset **5.71** (3.0 large-text; clears 4.5 anyway) SIGNAL LOST DSEG14 readout; ground/bone **14.25** + ground/bone-88% **11.18** (4.5) primary plate + hover and ::selection. Non-text 1.4.11 (3.0): focus green/ground 10.44, amber threat band 9.89, lit domes green 10.44 / amber 9.89 / red 5.43. DISCLOSED-BY-DESIGN (computed, printed, not gated): bone-faint/ground **2.71** — disabled plates only (WCAG 1.4.3 exempts inactive components; the token documents bone-faint as annotations-only); unlit lamp domes green/amber/red-dim vs ground **1.69/1.66/1.32** — deliberate presence-not-information glass: aria-hidden decorations whose state is fully duplicated by adjacent text (status line / verdicts / board). NO TOKEN CHANGED — every gated pair passes; the one token below 4.5 is exempt by usage and documented at the token (T8's own layer untouched).
- REDUCED-MOTION PARITY re-checked across ALL surfaces incl. the outcome annunciator + tape flip (new describes in contrast.test.ts + the standing T10 pin): tokens.css collapses all four --dur-* tokens to 0.01ms under reduce (pinned); write.css has ZERO animation declarations and its only motion (the leaf transform transition + every glow) exists solely inside the no-preference+preserve-3d gates; outcome.css — the loss-latch annunciator AND the board-flip AND the readout/lamp glows all live inside the no-preference gate (brace-extracted, nowhere outside; latch switches background-color in steps with no opacity, flip is rotateX-only with no opacity — the fade is never co-opted); archive.css's single animation (confirm-settle) is --dur-board-driven → 0.01ms under reduce, no reduce block needed; EVERY transition duration in every sheet is either a --dur-* token or literally 0s (regex-enumerated); main.css carries no animation or transition at all (grain is static texture per T8's commitment). Lamps are state, not motion — they switch in both modes (T9/T10 tests re-run green).
- HEADINGS/LANDMARKS: ONE h1 per phase at all times (each screen IS the page on this SPA console): setup keeps the visible product-title h1; write gains a visually-hidden "Writing session" h1; deleted gains "Session deleted" (SIGNAL LOST stays its h2); wind-down gains "Session complete" (DOWN SAFE stays its h2); archive PROMOTES the visible FLIGHT LOG h2→h1 with entry titles h3→h2 (heading trees h1→h2, no skips — all test-pinned). `<main id="app">` gives the one landmark every phase swaps inside (region rule satisfied); the two announcement regions live inside the landmark, never inside the swapped screen (pinned). `[data-error]` (T7 follow-up) is now role="status" — validation messages are announced politely. All radio groups remain fieldset/legend; every control native.
- AXE-CORE (devDependency ^4.13.0) over the jsdom-rendered DOM of every phase — setup, write armed, write fading, deleted, wind-down, archive with a finalized row + VIEW opened, and the clear-all confirm open: **zero violations at ANY severity** (the acceptance bar was zero critical/serious). DISCLOSED LIMITS, honestly: jsdom runs no cascade/layout/accessibility tree — axe's color-contrast rule lands "incomplete" (which is why contrast is computed against the real token values instead) and nothing here verifies what a screen reader SPEAKS; the harness mirrors index.html (lang, title, main#app) because jsdom's bare document lacks them (the one initial `document-title` finding was the harness, not the product). The spoken half is the manual protocol below.
- Validation evidence: `npm run test` — 10 files, 177/177 tests passed (24 router + 4 new announcement + 4 new walkthrough, 20 write-surface, 14 archive-screen, 9 axe/structure a11y, 8 contrast/motion/focus styles, 8 permanence, 30 controller, 25 timing, 28 archive, 1 smoke; ~1.8 s; the contrast table prints in the run log); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 20.97 kB / js 42.70 kB gzip 12.42 kB). Dev-server smoke (`vite --port 5812`): `/` 200, `/src/ui/router.ts` 200 with announcement/mountScreen markers in the transform, `/src/ui/a11y.test.ts` 200, zero errors in the dev log. Built-bundle check: `<main id="app">` present; FADE/DELETION/DISARM announcement strings, phase h1 texts, and `data-announcements` hooks all in dist js; contract seed `da64ca22` still 1 in dist/index.html with the contract comment still first child of body.
- Deviations: (a) the fade-start announcement is implemented INSIDE the write surface (its region, its phase) while deletion/disarm live in router-owned regions — the surface's region dies with its section, so terminal events structurally needed a region that survives swaps; two mechanisms, one politeness rule each, both test-pinned once-per-transition; (b) archive heading levels promoted (h2→h1, h3→h2) — one-h1-per-view SPA semantics, zero visual change (same classes); (c) `#app` div→main — landmark structure with zero layout impact; (d) the column-edge focus band thickened 1px→2px (constant width in all states, padding compensated — no layout shift, no redesign; WCAG 2.4.11 sizing judgment T9 routed here); (e) axe-core devDependency (the sanctioned cheap headless evidence layer); (f) disarm focus lands on DONE per plan.md's own "Done focused on disarm" — the T10-open decision resolved BY the plan text rather than re-litigated; (g) `mountScreen()` replaces the raw `root.replaceChildren(section)` calls — one mount point that also clears+remounts the announcement regions (tests updated from `children.length===1` to a single-`[data-phase]` invariant).
- Follow-ups — MANUAL VOICEOVER/KEYBOARD PROTOCOL for the human (cannot be run headlessly; honesty over theater): with VoiceOver (macOS Safari/Chrome, cmd+F5) and ideally one NVDA pass: (1) WRITE: hear "Signal fading —…" once at fade start and confirm restore silence; hear the role=timer region stay quiet per tick; hear "One minute remaining." / "Ten seconds remaining." milestones once each; (2) DELETION: hear the assertive "Signal lost —…" exactly once, confirm focus lands on RE-ARM and Enter restarts; (3) DISARM: hear "Down safe…" politely, confirm DONE is focused and announced; (4) ARCHIVE (T11 follow-up): confirm `data-archive-status` copy/download/delete/clear outcomes + transient COPIED states are spoken adequately; (5) SETUP (T7 follow-up): confirm radio groups announce group/legend + roving selection changes and the [data-error] status message is spoken; (6) confirm native Enter/Space activation of ARM/RE-ARM/DONE/VIEW/COPY/DOWNLOAD in real browsers (jsdom cannot synthesize native key activation — every control is a native button/radio by construction, which is the structural guarantee); (7) reduced-motion emulation spot-listen (no announcement should depend on motion). Record results in this log.
- Follow-up — RESIDUAL ACCEPTED RISK, README-able for T14: "The Disappearing Draft's core mechanic is time-pressured deletion, which is inherently hostile to some users — the threat cannot be paused, and a screen-reader or motor-impaired writer may lose a draft to the clock through no fault of their own. We mitigate (live announcements for fading/deletion, a static reduced-motion warning with a numeric countdown instead of the animated fade, warnings never encoded in opacity or color alone, the GENTLE preset as a 10s/30s accommodation) but we do not and cannot eliminate the exclusion; the town-hall Accessibility cluster accepted this residual risk by design. T14 should place this statement (or its sense) in the README alongside the ephemerality note." T8's `--focus-width` tokenization idea remains finishing-phase (the three sheets now share the same 2px literal — the pin test documents it).
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 10 files, 177/177 tests passed (~1.7 s), contrast table printing in the run log with every claimed ratio reproduced exactly (bone/ground 14.25, bone/panel-raised 13.28, bone-dim/ground 5.46, lamp-amber/panel-raised 9.21, lamp-red/ground 5.43, lamp-red/panel-inset 5.71, disclosed bone-faint/ground 2.71 and unlit domes 1.69/1.66/1.32); **zero `[axe:…]` console.warn lines anywhere in the output** — the a11y test prints EVERY remaining violation at any severity, so the observed silence is positive evidence of zero axe findings at ALL severities, not just the gated critical/serious tier; `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist html 1.86 kB / css 20.97 kB / js 42.70 kB gzip 12.42 kB, matching the worker's numbers exactly); `npm run dev -- --port 5277 --strictPort` + curl smoke → `GET /` HTTP 200 serving `<main id="app">`, `GET /src/ui/router.ts` HTTP 200 with 12 announcement/mountScreen marker lines in the transform, `GET /src/ui/a11y.test.ts` HTTP 200, dev log zero errors; server stopped after check.
  - Announcements verified from source + test: the two state regions are created in the router CONSTRUCTOR (`createAnnouncementRegion('status'|'alert')`) and mounted FOREVER after the section by `mountScreen()` (`root.replaceChildren(section, announceStatus, announceAlert)`) — they survive every screen swap while `firstElementChild` stays the screen (pinned by test AND by a11y.test.ts's landmark assertions: regions inside `main`, never inside `[data-phase]`); both regions cleared on EVERY mount (empty-region ⇒ next same-text announcement is a content change, test-proven across a two-session loop for both deletion and disarm); fade-start lives in T9's `data-fade-announcement` polite region, written ONLY on armed→fading onset in `Surface.update()` (prevPhase diff) and cleared on fading→armed restore — once-per-onset with repeat-onset re-announce structurally guaranteed, test-pinned incl. no-churn across further fading frames and the role=timer countdown region never carrying the announcement; the router regions are touched ONLY by `announce()` at terminal transitions (no per-tick path exists — `onWriteState` projects the surface only). Politeness asserted by role/aria-live: fade `aria-live="polite"`, deletion `role="alert"` (assertive), disarm `role="status"` (polite) with the honest quota-failure variant.
  - Focus map verified from source + test, all pinned on `document.activeElement`: ARM → editor (after mount), deletion → RE-ARM (sole focusable circuit on the board), disarm → DONE, Done → the finalized entry's VIEW on archive (id-addressed), VIEW ARCHIVE → NEW SESSION, RE-ARM/NEW SESSION → ARM. The keyboard walkthrough drives the FULL survive loop (setup tab order → ARM → editor → type-to-0:00 → DONE focused → wind-down tab order [draft, COPY, DOWNLOAD, DONE] → DONE → archive with VIEW focused → archive tab order → NEW SESSION → ARM focused → editor) AND the deletion loop (RE-ARM focused → setup ARM focused), with Tab order pinned per screen (unchecked radios and hidden confirm controls excluded).
  - Contrast verified as genuinely COMPUTED: `src/styles/contrast.test.ts` reads tokens.css from disk, regex-parses hex tokens, resolves `color-mix(in srgb)` recursively per the spec's gamma-encoded weighted average — no hardcoded color values anywhere (only the pair LIST and drift-pins like `>14`/`>5.4` are hardcoded, against token-resolved colors); every gated text pair ≥4.5:1 (the DSEG14 readout's formal 3.0 large-text bound passes 4.5 anyway, pinned >5.7); non-text 1.4.11 indicators ≥3:1; bone-faint 2.71 and the unlit domes disclosed-by-design with their WCAG exemptions argued (inactive components; aria-hidden decorative domes whose state is duplicated by adjacent text); spot-pins prevent silent token drift. NO token was changed (tokens.css mtime predates the T12 cluster).
  - Reduced-motion parity re-proven across all four sheets by structural test: tokens collapse all four --dur-* to 0.01ms + --motion-level reduced (pinned); write.css zero `animation:` and its only transform transition inside the no-preference gate (brace-extracted, nothing outside); outcome.css's loss-latch + board-flip + glows all inside the gate, latch/flip never touch opacity (the fade is not co-opted); archive.css's single animation is --dur-board-driven; EVERY transition duration in every sheet is a --dur-* token or literally 0s (regex-enumerated); main.css carries no animation/transition at all.
  - Structure verified: exactly one h1 per phase (setup visible title; write "Writing session", deleted "Session deleted", wind-down "Session complete" — visually hidden `.vh` phase h1s; archive FLIGHT LOG promoted h2→h1 with entry titles h3→h2), heading trees h1→h2 no skips; `<main id="app">` in source index.html with the direction contract still first child of body (seed `da64ca22` grep 1 in dist); `[data-error]` is `role="status"`; `<main>`, the announcement strings, phase h1 texts and `.dataset.announcements` all confirmed in the FRESH dist bundle.
  - Runtime-bundle purity verified: axe-core appears in `package.json` ONLY under devDependencies (`^4.13.0`); grep of the freshly built `dist/assets/*.js` for axe-core → ZERO hits (42.70 kB js consistent with T10's 40.88 kB + T12's announcement/heading code); axe is imported only by `src/ui/a11y.test.ts`, never by any production module.
  - No-visual-redesign check verified by mtime inventory: the 03:21–03:33 modification cluster is exactly the logged changed set (package.json/package-lock.json, write-surface.ts, outcome-screens.ts, archive-screen.ts, index.html, main.css, archive-screen.test.ts, router.ts, router.test.ts, a11y.test.ts NEW, write.css, contrast.test.ts NEW) — tokens.css (01:47), archive.css (02:07), outcome.css (03:01) untouched; the only CSS edits are the a11y-motivated ones (document-level `.vh` utility; the column-edge focus band 1px→2px constant-width with padding compensation — WCAG 2.4.11, zero layout shift); heading promotions reuse the same classes (zero visual change).
  - Honesty check PASSED: the log nowhere claims a screen reader was run — the axe section states "nothing here verifies what a screen reader SPEAKS; the spoken half is the manual protocol below", and the VoiceOver/keyboard protocol is recorded as a numbered FOLLOW-UP for the human (seven concrete listen-for steps) plus the README-able residual-risk statement (mirroring PRODUCT.md's Accessibility & Inclusion section and the town-hall cluster's accepted risk), routed to T14. This matches the plan's own T12 status framing ("a recorded manual VoiceOver protocol for the spoken half, which cannot be verified headlessly") — criterion 7's structural half is machine-proven, the spoken half honestly deferred. jsdom limits (no cascade → axe color-contrast "incomplete", hence computed contrast; no native undo/activation) are disclosed in the test headers rather than papered over.
  - Trivial bookkeeping nit (no criterion affected): the worker's validation line says "4 new announcement" tests; the announcements describe has 5 (router file total 33 = 24 pre-T12 + 5 + 4). The changed-files line has it right; totals (177/177) are exact.
  - T12 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). Milestone M4 (hardened deliverable: T12, T13, T14) opens with T12 down.

✅ T13 Timing hardening verification — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: MODIFIED `src/engine/controller.test.ts` (NEW describe "T13: boundary ties and return-path gaps" — 6 tests; no production code touched), `src/ui/router.test.ts` (NEW describe "reload / exit mid-session (T13)" — 2 tests extending the established FakeStorage spy pattern; no production code touched). NEW `docs/manual-verification-protocol.md` (the four human protocols + R1's extended hidden-tab regimes + R3's per-browser undo attempts, exact steps + expected results + per-browser PASS/FAIL checkboxes). Plan/state bookkeeping: `docs/ultron/plan.md` (T13 → awaiting-approval), this file, `docs/ultron/state.md`.
- (1) FAKE-CLOCK SUITE AUDIT: full suite run first — 177/177 green (10 files). Invariant review against the plan's timing acceptance: exact fade start + per-preset boundary table, exact delete boundary, input reset, disarm-at-zero precedence (incl. exact delete/disarm tie → survived), chronological collision semantics (single post-overstay recompute historically correct), monotonic/strict opacity decay scans, opacity-bounds + no-skip properties (seeded mulberry32, adaptive boundary sampling b−2..b+2), controller reconcile paths (visibilitychange visible-gated, focus, pageshow persisted, boundary timer, rAF tick, input), drift oracles (per-tick equality vs pure deriveTimingState over janky steps, one-giant-frame jump), no-accumulated-ticks (≤1 pending frame/timer across churn; zero timer churn on 100 frames), double-fire prevention (stolen committed wake-up), clock guards (frozen performance.now never read; backwards Date.now clamped). GAP ANALYSIS found six uncovered cases, all added (below) — every one PASSED against the existing implementation on first run: **no timing bugs found; the invariants hold at the exact boundary instants too.** The audit's outcome is the proof, not a fix list.
- (2) NEW BOUNDARY/TIE/GAP TESTS (`src/engine/controller.test.ts`, fake clock STANDARD 5s/10s, fake timers/frames): (a) visibility return landing EXACTLY ON the delete boundary (now === lastInputAt + deleteThresholdMs, wake-ups stolen = suspension) → deleted at the tie instant, suspended wake-up absorbed (0 pending timers); (b) visibility return landing EXACTLY ON 0:00 → survived, PLUS the exact delete/disarm three-way tie (deleteAt === sessionEndAt) held through the RETURN path (T2's disarm-wins-ties rule, previously pinned only through the pure function and the timer path); (c) focus ALONE with no visibility change ever fired (R1 E11 iOS return-visibilitychange miss; visibilityState stays 'visible', frames never ticked = full JS suspension) → focus reconciles the overstay to a single deleted; belated visibilitychange + stray pageshow absorbed; (d) pageshow persisted=false (ordinary load event, no bfcache) reconciles identically (only persisted=true was covered); (e) hidden overstay landing MID-FADE on return → exact fading state (opacity 0.6, deletesInMs 3 000) — not forced terminal — and one keystroke restores instantly (return-during-fade recoverability); (f) boundary timer firing EXACTLY AT the fade delay → fading with opacity exactly 1 (no skip past the boundary), deletesInMs = delete−fade, next boundary re-targeted exactly (T0+DELETE_MS), exactly 1 pending timer (fired callback rescheduled once).
- (3) RELOAD-NO-WRITE (automated jsdom half, `src/ui/router.test.ts`): honest model documented in the describe header — jsdom cannot navigate, so the outgoing page receives real beforeunload + pagehide(PageTransitionEvent) dispatches, then all in-memory state dies with the instance while the storage ADAPTER survives (exactly what a same-origin reload keeps/discards); the physical Cmd+R is protocol D of the manual doc. Test 1: unload events mid-armed-session → zero storage writes (setItem/removeItem spies, archive empty), the live threat keeps honest time straight through them (opacity 0.8 at 6 s idle via the engine), and the whole abandoned lifecycle (arm → type → unload events → deletion) ends with ZERO storage calls. Test 2: pre-seed a SURVIVED entry, arm a second session, type, unload events, destroy the page → zero writes; FRESH router over the SAME storage (the fresh load) boots to setup (session-over), no editor, the abandoned draft's text nowhere in the DOM, 0 pending frames/timers, console.error never called (spied), prior archive entry byte-identical (ARCHIVE_STORAGE_KEY envelope unchanged) with the abandoned draft absent from every entry.
- (4) VALIDATION: `npm run test` — 10 files, **185/185 tests passed** (177 prior + 6 controller + 2 router; controller 36, router 35; ~1.5 s); `npm run build` — `tsc --noEmit` strict gate passed, vite 8 built clean (dist unchanged: html 1.86 kB / css 20.97 kB / js 42.70 kB gzip 12.42 kB — test-and-docs-only task, zero production code changed, hence zero dist delta).
- (5) MANUAL PROTOCOLS (`docs/manual-verification-protocol.md`) — the half that CANNOT be run from this harness, authored as precise reproducible procedures with expected results and per-browser PASS/FAIL checkboxes + a results table to copy back into this log: **Protocol A** hidden-tab overstay per R1's Research-locked span — A1 basic 45 s overstay (GENTLE 10 s/30 s, marker line, switch tabs, return → SIGNAL LOST exactly once, no partial/undead state, RE-ARM focused, nothing archived; Chrome/Safari/Firefox desktop + iOS Safari; notes that deletion may land while hidden or on the return instant — both correct), A2 Chrome >5 min intensive throttling (15-min session), A3 Safari minimized (Cmd+M) + app-hide (Cmd+H) full suspension, A4 bfcache navigate-away-and-Back (both legitimate outcomes recorded: persisted restore → deleted; eviction → reload session-over), A5 fully occluded Chrome window, A6 Chrome freeze / chrome://discards discard (frozen→deleted vs discarded→session-over setup), A7 Firefox-Android 15-min unload; single-delete-beat checked in every variant. **Protocol B** foreground tolerance: frame-stepped ≥30 fps screen recording with in-frame clock → fade start T+10.00 s ±100 ms, deletion T+30.00 s ±100 ms (GENTLE), smooth linear decay; optional STANDARD/BRUTAL + 4× CPU-throttle wall-clock-drift stress; typing-restores-instantly; reduced-motion variant (static banner + numeric inactivity countdown, no animated fade). **Protocol C** native undo defeat per R3 (Safari desktop named load-bearing): multi-level typing, then on SIGNAL LOST — Cmd/Ctrl+Z ×10, Cmd/Ctrl+Shift+Z + Ctrl+Y, Edit-menu Undo (macOS menu bar / Firefox ≡ / Chrome ⋮), iOS shake + three-finger swipe/double-tap (Safari AND Chrome on iOS), Android long-press toolbar Undo — then RE-ARM → fresh editor → undo again; EXPECT nothing resurrects anywhere, nothing archived, AND undo still works normally mid-write/wind-down/setup (interceptor is deleted-phase only). **Protocol D** reload mid-session: pre-seeded survivor, then reload in the armed phase (D1) and mid-fade (D2) + hard reload + Back/Forward → setup screen, localStorage envelope exactly the pre-seeded entry (no new write, no trace of the marker draft), archive row intact, zero console errors, next session arms normally.
- What remains MANUAL (honestly): everything in docs/manual-verification-protocol.md — real hidden-tab schedulers, real native undo engines, physical reload/navigation, and the ±100 ms human-visible tolerance. The jsdom-proven halves are: plural-event reconcile semantics incl. the tie instants and the iOS focus-only path (controller tests), the R3 DOM sequence + storage silence (permanence/router tests), reload unload-event silence + fresh-load session-over (new router tests). The completed results table should be appended to this entry when a human runs the protocols.
- Deviations: none to production code (audit found no timing bugs — the six gap tests passed unchanged); `docs/manual-verification-protocol.md` placed at `docs/` root rather than `docs/ultron/` per the task's stated path.
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 10 files, **185/185 tests passed** (controller 36 = 30 prior + 6 new T13, router 35 = 33 prior + 2 new T13, timing 25, archive 28, write-surface 20, archive-screen 14, contrast 10, permanence 8, a11y 8, smoke 1; ~1.9 s); targeted `npx vitest run -t "T13" --reporter=verbose` → all 8 new tests pass BY NAME (6 controller + 2 router); `npm run build` → green — `tsc --noEmit` strict gate passed, vite 8 built clean (dist 1.86 kB html / 20.97 kB css / 42.70 kB js gzip 12.42 kB — exactly the claimed figures).
  - Dist-unchanged claim proven the strong way: SHA-256 hashes of every dist file taken BEFORE my rebuild, then `npm run build` run again by me, hashes re-taken → **byte-identical across all 12 dist files** (fonts, js, css, html, licenses). A test-and-docs-only task reproducing identical bytes from unchanged source is the strongest available form of the claim.
  - Zero-production-code claim verified by mtime inventory: after the T12 wave (≤03:32), the only files written at T13 time (03:46) are `src/engine/controller.test.ts` (03:46:13) and `src/ui/router.test.ts` (03:46:27) — both TEST files — plus `docs/manual-verification-protocol.md` and the three bookkeeping docs (plan/log/state). Every production source file predates the task; package.json/index.html untouched.
  - Six gap tests verified genuine from source (read, not just green): (a) return EXACTLY ON the delete boundary — clock advanced to precisely `lastInputAt + deleteThresholdMs`, suspended wake-up stolen, → `['armed','deleted']`, 0 pending timers; (b) return EXACTLY ON 0:00 → survived, PLUS the exact delete/disarm tie (`deleteAt === sessionEndAt`, `mount(DELETE_MS)`) held through the return path — previously pinned only via the pure function and the timer path; (c) focus ALONE with visibilitychange never fired and visibilityState stuck 'visible' (R1 E11 iOS miss; frames never ticked = full suspension) → single `deleted`, belated visibilitychange + stray persisted pageshow absorbed to still exactly one; (d) `pageshow persisted=false` (ordinary load — only persisted=true was covered before) reconciles identically; (e) mid-fade return → exact `opacity 0.6` + `deletesInMs 3000` (NOT forced terminal) and one keystroke restores instantly with the boundary re-targeted to the new input + fade; (f) boundary timer firing EXACTLY AT the fade delay → `fading` with opacity exactly 1 (no skip past the boundary), rescheduled exactly once to `T0 + DELETE_MS`. Test helpers inspected: `mount(sessionMs)` genuinely parameterizes the session end (the tie tests are real, not hardcoded), and `steal()`/`fireDue()` model suspension/late wake-ups faithfully.
  - Two router tests verified genuine from source: real `beforeunload` + `PageTransitionEvent('pagehide')` dispatched mid-armed-session with setItem/removeItem spies → ZERO writes through the whole abandoned lifecycle (arm → type → unload events → deletion), threat keeps honest time through them (opacity 0.8 at 6 s); and the fresh-load test — pre-seeded survivor landed through the REAL disarm path, second session abandoned, `router.destroy()` kills the in-memory page while the storage adapter survives, fresh router over the same storage boots to SETUP with no editor, no draft text anywhere in the DOM, 0 pending frames/timers, `console.error` never called (spied), archive envelope byte-identical and the abandoned draft absent from every entry. The describe header documents the honest jsdom model (jsdom cannot navigate; physical reload is Protocol D) — no overreach.
  - Manual protocol assessed as REAL and executable (`docs/manual-verification-protocol.md`, 303 lines): every protocol has numbered concrete steps with specific presets, durations, wall-clock waits, and recognizable marker lines; PASS/FAIL checkboxes per browser per variant; a consolidated empty results table with instructions to copy the completed table into this entry. Mapping against the committed research verified EXACT: R1's T13 prescription items 1–8 → A1 basic overstay (Chrome/Safari/Firefox/iOS table), A2 Chrome >5-min intensive throttling (15-min session), A3 Safari minimize Cmd+M + app-hide Cmd+H full suspension, A4 bfcache navigate-away-and-Back with BOTH legitimate outcomes recorded (persisted restore → deleted vs eviction → reload session-over), A5 fully occluded Chrome window, A6 Chrome freeze / `chrome://discards` discard (frozen→deleted vs discarded→setup), A7 Firefox-Android 15-min unload — with "exactly ONE beat" asserted in every variant; R1 item 8 → Protocol B foreground tolerance (frame-stepped ≥30 fps screen recording with in-frame clock; fade T+10.00 s ±100 ms, deletion T+30.00 s ±100 ms, smooth linear decay, optional STANDARD/BRUTAL + 4× CPU-throttle drift stress, typing-restores-instantly, reduced-motion numeric-countdown variant, per-browser Δ table). R3's per-browser prescription → Protocol C (Cmd/Ctrl+Z ×10, redo via Shift+Z and Ctrl+Y, Edit-menu Undo on macOS/Firefox/Chrome, iOS shake + three-finger swipe/double-tap on Safari AND Chrome (both WebKit), Android long-press toolbar, RE-ARM fresh-editor undo re-attempt, plus the positive control that undo still works mid-write/wind-down/setup; Safari named load-bearing with the WebKit r87204 rationale stated). Plan scope's reload/exit item → Protocol D (pre-seed via a real survived session, D1 armed-phase + D2 mid-fade reload, hard reload, Back/Forward; setup boots; localStorage envelope exactly the pre-seeded entry; console zero; next session arms). Town-hall criteria 4/5/6/8 all mapped: 4+8 automated (permanence/timing suites incl. the six new tests) with C as the physical half, 5 automated (reconcile paths) + A/B manual, 6 automated (disarm/tie/wind-down) with the exact-0:00 return now pinned. Nothing vague or unexecutable found; the only nit (results-table "App commit/build" line — repo is not yet a git repo) is accommodated by the blank.
  - Honesty check PASSED: the log entry separates automated results (185/185 with an itemized list of what is jsdom-proven) from an explicit "What remains MANUAL (honestly)" section naming exactly the four things no harness can prove (real schedulers, real native undo engines, physical reload/navigation, human-visible ±100 ms), and states the completed results table should be appended when a human runs the protocols. state.md's next-action likewise says a human must run them. No automated claim overreaches into browser-only territory anywhere; no fabricated results (protocol tables are blank with checkboxes). The plan's "protocol results recorded in production-log" is satisfied in the only honest form a headless harness can produce: the protocol itself is committed and referenced here, results pending the human run.
  - T13 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). The manual protocol (docs/manual-verification-protocol.md, Protocols A–D) remains a USER FOLLOW-UP: a human must run it and copy the completed results table into this entry. Milestone M4: T12 + T13 down; T14 (deps T9–T13) unblocked.

✅ T14 Deliverable check — awaiting-approval (worker subagent for production-supreme, 2026-08-28)
- Changed files: NEW `README.md` (the deliverable README — the task's one source artifact); REBUILT `dist/` from clean (build output, gitignored). Plan/state bookkeeping: `docs/ultron/plan.md` (T14 → awaiting-approval), this file, `docs/ultron/state.md`. ZERO production source files touched — the artifact is documentation + verification only (mtime inventory: every src/ file predates this task; only README.md is new).
- PRODUCTION BUILD from clean: `rm -rf dist && npm run build` → exit 0, ZERO errors and ZERO warnings in the full build output (tsc --noEmit strict gate passed; vite 8.2.2, 13 modules transformed, built in 85 ms). Sizes: index.html 1.86 kB (gzip 1.07), css 20.97 kB (gzip 4.25), js 42.70 kB (gzip 12.42), fonts 103.90 kB total across 5 woff2 — all consistent with the T12/T13 builds; hashed asset names identical before/after the clean rebuild (index-B0AhuKeS.js, index-lMKdHT81.css), i.e. byte-identity by content-addressed name.
- Asset sanity: dist inventory exactly 12 files — index.html, 2 hashed css+js, 5 hashed woff2, and all FOUR license files under `dist/font-licenses/{source-sans-3,b612-mono,michroma}/OFL.txt + dseg14/DSEG-LICENSE.txt` (the vite license plugin works from clean). Contract survives: `da64ca22` grep = 1 in dist/index.html and the DIRECTION CONTRACT comment is still the FIRST CHILD of `<body>` (checked in the built HTML: `<body>` → contract comment → `<main id="app">`). OFL §1 rename verified IN THE SHIPPED BINARY with fontTools (strings cannot read compressed woff2 name tables): `dist/assets/SourceSans3-VF-DCKCvDXi.woff2` name records = family "Countdown Room Prose" / full "Countdown Room Prose Regular" / PS "CountdownRoomProse-Regular" — zero "Source Sans" string anywhere in its name table; B612 Mono, Michroma, DSEG14 ship with names intact (no RFN). Only URL-shaped string concern previously cleared (self-hosted; no runtime fetch).
- PREVIEW SMOKE (`npm run preview -- --port 4189 --strictPort`, the built bundle): all 12 URLs → HTTP 200 with correct content types and exact byte sizes — `/` text/html 1 867 B (shell contains `<main id="app">`, contract seed `da64ca22`, `<script type="module" crossorigin src="/assets/index-B0AhuKeS.js">`, `<link rel="stylesheet" …>`), `/assets/index-B0AhuKeS.js` text/javascript 42 704 B, `/assets/index-lMKdHT81.css` text/css 20 977 B, the FIVE fonts all `font/woff2` (SourceSans3 61 068 B, B612Mono-Regular 13 140 B, B612Mono-Bold 11 924 B, Michroma 11 988 B, DSEG14 5 804 B), and the four license files text/plain. Preview server log: zero errors/warnings; server stopped and port freed after the check.
- Console-error evidence (method disclosed — no real browser console exists in this harness): (1) the served JS bundle contains exactly TWO `console.warn` occurrences, both traced to the intentional degrade paths in `src/data/archive.ts` (corrupt-payload quarantine notice; storage-read-failure → empty-archive notice) — zero `console.error`, zero `console.log` in any production module or the built bundle; (2) zero `sourceMappingURL` references, zero `eval(`, zero axe-core strings in dist (devDependency never leaks); (3) the build itself emitted no warnings. What this does NOT prove: a real engine executing the bundle and printing to a real devtools console — that pass belongs to the finishing phase's real-browser work (below).
- FINAL JOURNEY CHECK: `npm test` → 10 files, **185/185 tests passed** (~1.66 s; same census as T13: controller 36, router 35, timing 25, archive 28, write-surface 20, archive-screen 14, contrast 10, permanence 8, a11y 8, smoke 1). One known jsdom environment note in the output ("Not implemented: HTMLCanvasElement.getContext" — jsdom's virtual console while axe probes canvas): test-environment noise, not a served-asset console error; the a11y file passes 8/8.
- README.md inventory (honest, complete, no invented claims — no users/metrics/testimonials exist and none are fabricated): (1) WHAT IT IS — one paragraph in the product's own voice (arm a countdown, write under threat, fade → permanent deletion, disarm at 0:00 → flight log; mission-control styling because "the threat is believed only because it is real"); presets/durations stated factually (GENTLE 10s/30s, STANDARD 5s/10s, BRUTAL 3s/6s; 3/5/10/15-min or custom 1–120). (2) HOW TO RUN — dev/test/build/preview + Node 24/npm 11 note + static-deploy statement + "no backend, no accounts, no network calls at runtime". (3) HONEST EPHEMERALITY — localStorage-only under one key; nothing leaves the browser; clearing browser data deletes the archive permanently with no recovery; copy/download is the backup story; corrupt payloads are quarantined not silently destroyed; in-session deletion is stricter (never written to storage at all); framed as the product principle "stated, not hidden". (4) RESIDUAL-RISK ACCESSIBILITY STATEMENT — T12's README-able statement placed nearly verbatim: the mechanic is inherently hostile (no pause; a screen-reader or motor-impaired writer can lose a draft through no fault of their own); mitigations SHIPPED are listed concretely (live announcements for fade/deletion/disarm + role=timer milestones; reduced-motion = static warning banner + numeric countdown replacing the animated fade, warnings never in opacity/color alone; machine-computed contrast table shipped as part of the suite via src/styles/contrast.test.ts; GENTLE as a 10s/30s accommodation); the residual limitation stated plainly: "We do not and cannot eliminate the exclusion." (5) MANUAL PROTOCOL POINTER — docs/manual-verification-protocol.md linked as the way for a human to verify hidden-tab/undo/tolerance/reload behavior per browser, with its four protocols named. (6) FONTS — OFL for B612 Mono/Michroma/Source Sans 3 with license texts shipped in dist/font-licenses/, the Source Sans 3 subset rename to "Countdown Room Prose" per OFL §1 with copyright retained, DSEG14 under the DSEG Free License, self-hosted (no runtime fetch). Plus a factual project-layout block.
- CLEANUP: the only temp artifact created (a preview log under /tmp) removed; full-tree audit (excl. node_modules/dist) shows every file belongs to the run — no dead paths, no stray files. `.gitignore` verified: covers `node_modules/`, `dist/`, `*.local`, `.DS_Store`, `npm-debug.log*` — appropriate for when the project becomes a repo. NOTE: the project is NOT yet a git repository (`git rev-parse` → "fatal: not a git repository") — recorded here per contract; `git init` deliberately NOT run.
- What a real-browser pass still owes (finishing phase + user follow-ups, honestly owed): (1) an actual browser console over the preview/dev bundle (this harness has no browser — curl + bundle scan is the substitute evidence recorded above); (2) the T12 manual VoiceOver/NVDA spoken-half protocol (seven steps, production-log T12 entry); (3) the T13 manual Protocols A–D per browser with results tables copied into the T13 entry (docs/manual-verification-protocol.md); (4) the finishing phase's visual pass (flap mid-flip, fade in flight, reduced-motion emulation, board widths, Safari backface + Layers/FPS, outcome boards, archive render).
- Delegation record: worker subagent for production-supreme, 2026-08-28.
- Approval state: awaiting verification per run contract.
- Verifier (independent, ultron-supreme): 2026-08-28 — **PASS**. THIS IS THE FINAL PRODUCTION VERIFICATION — production closes with this entry.
  - Commands run independently (not trusting worker output): `rm -rf dist && npm run build` → exit 0, **zero errors and zero warnings** in the full output — `tsc --noEmit` strict gate passed, vite 8.2.2, 13 modules transformed, built in 72 ms; sizes html 1.86 kB (gzip 1.07) / css 20.97 kB (gzip 4.25) / js 42.70 kB (gzip 12.42) / 5 woff2 totalling ~103.9 kB — every figure matching the worker's claims, and the hashed asset names reproduced identically from the clean rebuild (`index-B0AhuKeS.js`, `index-lMKdHT81.css`) — content-addressed byte-identity confirmed. `npm run test` → **185/185 tests passed, 10 files, exit 0** (controller 36, router 35, timing 25, archive 28, write-surface 20, archive-screen 14, contrast 10, permanence 8, a11y 8, smoke 1; the "Not implemented: HTMLCanvasElement.getContext" lines in the output are jsdom virtual-console noise while axe probes canvas — environment noise, not test or product failures; the a11y file passes 8/8 — observed identically to the worker's disclosure).
  - Preview smoke run MY OWN way (`npx vite preview --port 5965 --strictPort` over the freshly rebuilt bundle): `GET /` → **200** text/html 1 867 B (shell contains `<main id="app">`, contract seed `da64ca22`, the module script and stylesheet links); `GET /assets/index-B0AhuKeS.js` → **200** text/javascript 42 704 B; `GET /assets/SourceSans3-VF-DCKCvDXi.woff2` → **200** font/woff2 61 068 B; `GET /font-licenses/source-sans-3/OFL.txt` → **200** text/plain 4 579 B; `GET /font-licenses/dseg14/DSEG-LICENSE.txt` → **200** text/plain 4 487 B. Preview server log: zero errors/warnings; server stopped and port freed after the check.
  - Contract survival: `grep -c da64ca22 dist/index.html` → **exactly 1**, and the DIRECTION CONTRACT comment is the **FIRST CHILD of `<body>`** in the built HTML (verified by reading the built file: `<body>` → `<!-- DIRECTION CONTRACT — The Disappearing Draft · The Countdown Room …` → `<main id="app">`).
  - `dist/font-licenses/` ships **exactly 4 files**: `source-sans-3/OFL.txt`, `b612-mono/OFL.txt`, `michroma/OFL.txt`, `dseg14/DSEG-LICENSE.txt` (find-count 4). Dist inventory exactly 12 files — index.html, 2 hashed css+js, 5 hashed woff2, 4 licenses — no strays.
  - README.md audited section by section against the task's acceptance ("README covers ephemerality honestly" + the run contract's list): (1) **honest ephemerality** — "The archive is ephemeral — read this": localStorage-only under a single key, nothing leaves the browser, clearing browser data deletes the archive permanently with no recovery, copy/download stated as THE backup story, corrupt payloads quarantined not silently destroyed, in-session deletion stricter still (never written to storage at all), framed as "stated, not hidden"; (2) **residual a11y risk with shipped mitigations AND the plain limitation line** — mitigations listed concretely (live announcements polite/assertive/polite + role=timer milestones, reduced-motion static banner + numeric countdown replacing the animated fade with warnings never in opacity/color alone, machine-computed WCAG contrast table running in the suite, GENTLE 10s/30s as accommodation) and the limitation stated plainly: "We do not and cannot eliminate the exclusion." (plus the honest extension: no configuration can prevent it while the product remains what it is); (3) **manual-protocol pointer** — links `docs/manual-verification-protocol.md`, names its four protocols (hidden-tab overstay, ±100 ms tolerance, per-platform native-undo, reload no-write) with per-browser PASS/FAIL tables — the linked file EXISTS (14 263 B); (4) **font-license note incl. the Source Sans 3 rename** — OFL for B612 Mono/Michroma/Source Sans 3 with texts shipped in `dist/font-licenses/`, Source Sans 3 subset rename to "Countdown Room Prose" per OFL §1 with copyright retained, DSEG14 under the DSEG Free License, self-hosted with no runtime fetch; (5) **run instructions** — npm install/dev/test/build/preview, Node 24/npm 11 note, static-deploy statement, "no backend, no account, no network calls at runtime"; (6) **NO invented claims** — full read-through found zero users, zero metrics, zero testimonials, zero fabricated social proof (PRODUCT.md invariant held); every number in the README is verifiable (185 tests — rerun by me; preset timings — pinned by the T2 suite; byte/percent claims — none beyond what the suite and dist verify).
  - Repo hygiene: `.gitignore` covers `node_modules/`, `dist/`, `*.local`, `.DS_Store`, `npm-debug.log*` — sane for when the project becomes a repo; `ls -a` at the project root shows **no `.git`** and `git rev-parse` → "fatal: not a git repository" — no git repo was created, exactly as recorded.
  - Zero-production-source claim spot-checked: the only file added by T14 is `README.md`; the clean rebuild reproduced the T13 dist byte-for-byte by hashed name — consistent with documentation-only scope.
  - T14 status → completed; auto-approved (ultron-supreme). Evidence: this file (verifier entry above). **PRODUCTION COMPLETE — all 14 tasks verified (T1–T14); milestone M4 closes; next phase: finishing ($ultron-impeccable auto mode).**

### Refinement R1 — the setup console built to the FIRST VIEWPORT contract (finishing phase, auto mode)

- **Task:** refinement.md #1 / critique F1 [P1] — the setup console was functionally complete (T7) but unstyled: no sheet consumed the `placard`/`placard-name`/`placard-limits` hooks, ARM/VIEW ARCHIVE were UA-default buttons, `--dur-arm` (260ms) sat unused. Entry brief: build the committed FIRST VIEWPORT — flap duration board presiding, placard switches with printed FADE/LOSS limits, one guarded ARM — keeping ALL T7 functional behavior intact (styling/structure in the world, not a functional rewrite).
- **Changed files:** `src/styles/setup.css` (NEW — the setup sheet), `src/styles/main.css` (import wiring), `src/ui/router.ts` (renderSetup restructure + the setup flap-board factory + board lifecycle), `src/ui/write-surface.ts` (additive exports only: `FLIP_SETTLE_MS`, `defaultMotionPreference`, `defaultSupports3d`), `src/ui/router.test.ts` (+5 R1 structural tests, tabbables() honesty filter for the guarded ARM's tabindex swap, tab-order pins updated for both cover states), `src/styles/contrast.test.ts` (setup.css added to the transition-token sheet list + the 2px focus pin loop; two pair `where` notes extended), `DESIGN.md` (Inputs + Layout + the setup-console section rewritten from the built world — the "pre-styling honest note" retired), `.impeccable/design.json` (+2 components: Setup Placard, Guarded ARM Station).
- **What was built (structure):** section.setup shell 48rem (documented in DESIGN.md Layout); h1 nameplate (Michroma lg, a11y-pinned text unchanged) + one honest prose line; the presiding duration board = write.css's `.flap-board` classes REUSED (test-pinned that setup.css defines no `.flap-board`/`.flap-slot` rules — no fork) with a FIXED 3-minute-well field (`MMM:SS` holds every legal 1–120 length, wells never appear/disappear), `role="img"` + plain-language label ("Session length 15 minutes" / "not set"), digits flipping on selection through the identical four-surface slot + `data-flipping`/`data-reset` sequence behind BOTH capability gates (no-preference motion AND preserve-3d; reduced/unsupported = static swap, same as the write board), JS reusing `FLIP_SETTLE_MS` (300ms twin of `--dur-flap`); "SESSION LENGTH" engraved legend beneath. Duration = 4 preset placards + CUSTOM (entry bound met with equality), difficulty = GENTLE/STANDARD/BRUTAL placards printing `FADE n s` / `LOSS n s` as two engraved lines straight from the T2 table. Placards are pressed-in switch plates: native radio covers the whole placard as an invisible hit target (`appearance:none` + `opacity:0`), face `pointer-events:none`, selected = recessed bed + bone reading (achromatic by commitment), hover = bone text + strong hairline, focus = 2px focus-green outline on the placard via `:has(.placard-radio:focus-visible)`. CUSTOM field = recessed B612 numeral well riding above the covering radio (typing never toggles the radio), spinners removed, `aria-invalid` = amber hairline edge while the field stays achromatic. Error line = amber Michroma placard, `role="status"` (announced), ONE reserved line so the ARM station never shifts.
- **The guarded ARM (the cape-donation ritual):** MASTER ARM station low + isolated (`margin-top:auto` rides it to the bottom of the viewport's remaining space), panel-raised with the panel edge, engraved legend + honest stakes line ("NO PAUSE — NO RECOVERY"). One deliberate motion, two physical steps: LIFT COVER (a plate; hinges at its top edge under 900px perspective, `rotateX(-100deg)` + `visibility` delayed by `--dur-arm` — the committed gesture token finally spent, backface-hidden so it vanishes past edge-on, out of the tab order, pointer-dead) → ARM (the room's one energized plate, `plate plate-primary`, 13px caps). Keyboard-complete: cover Enter/click opens and focuses ARM; Escape re-covers and returns focus to the cover; while closed the cover owns the tab order and ARM carries `tabindex="-1"` — still a live control (the T7 seam: nothing sits between a valid config and `startSession`), and the router's deliberate focus (the RE-ARM one-motion restart, T12's committed decision) lifts the cover by itself via ARM's focus listener. VIEW ARCHIVE rebuilt as the standard secondary `.plate`. The stray `' '` text node between the two buttons (P3 polish finding) died with the restructure — noted here, polish entry #4 carries the rest of that list.
- **World-rule compliance (pinned in tests where mechanical):** Locked Lamp — amber only as the validation hold (error line 9.89:1 on ground, computed), green only as focus, no red anywhere on setup; Fixed Ground — no section background, panels are furniture; Three Instruments — Michroma caps (≤24ch placards), B612 tabular numerals (board, values, field), prose only for the intro; DSEG14 absent; No-Soft-Shadows — `--panel-edge`/inset hairlines only (the board's readout halo arrives free from write.css's existing no-preference gate); every transition duration is a `--dur-*` token (contrast.test sheet list extended) so reduced motion needs no sheet block — the cover lift collapses to 0.01ms via `--dur-arm`, the board flips are JS-gated.
- **Evidence:** `npm run test` **190/190** (baseline 185; +5 new tests: board echo/flips/settle + timer destruction on leaving setup; reduced-motion never-flips; guard closed/open/Escape/direct-press/auto-lift; placard hooks consumed + every setup button carries `plate`; setup.css wired in main.css + `--dur-arm` spent on a rotateX hinge + `aria-invalid` caution amber + zero animation declarations). `npm run build` strict-green (css 26.78 kB / js 47.43 kB). Structural jsdom inspection (temporary test, deleted after): full DOM dump verified — board 5 wells + colon + `T–`, placard hooks with real content, tab order `radio/custom/radio/arm-cover/view-archive` closed and `…/arm/view-archive` open, cover click → `data-open` + ARM focused, BRUTAL+10min board echo, custom "0" → error text + disabled ARM + `aria-invalid` + honest "0 minutes" echo. Detector: FULL ENGINE (htmlparser2/css-select/css-tree/domutils provisioned via temp npm install + symlinks on the script's ESM resolution path, removed after) — `[]` clean on setup.css, main.css, router.ts, write-surface.ts, index.html AND dist (index.html + built css). Markers survive minification in dist css+js (`--dur-arm`, `data-setup-board`, `arm-cover`, `placard-limits`).
- **Honest limits:** NO real-browser rendering exists in this harness — no first-viewport capture, no flap-motion or cover-lift frames, no pixel claims anywhere above (structural assertions only). **Owed follow-up: a real-browser setup console first-viewport capture** (plus cover-lift mid-animation, board flip, phone-width 320px wrap) for the human closing pass.
- **Deviations / routed decisions:** (1) Duration grouping kept at 4 presets + CUSTOM — the entry's bound ("≤4 presets + CUSTOM") met with equality; the critique's stricter "≤4 total visible options" reading would require changing planning-owned `DURATION_PRESET_MINUTES` (a product decision, PRODUCT.md "session-duration input shape: planning-owned") — routed to the coordinator rather than silently changed in a styling entry; the designed placard grouping (presets as one tight instrument row + CUSTOM as a fielded placard) mitigates the scan-load concern visually. (2) The title + one-line intro precede the board (the write surface has no title; setup is the front door and the h1 is a11y-pinned) — flagged to the owed capture to confirm the board still reads as presiding. (3) `.plate` is consumed cross-sheet from archive.css as outcome.css already does — the dedicated polish entry (#4) owns extracting it.
- **Finish review:** harness has NO agent-spawn tool and NO agent CLI (claude/zcode/codex/gemini/ollama all absent) — the shipped finish-reviewer role ran as a DISCLOSED in-thread substitution, reviewing from code + DESIGN.md + the direction contract with zero visual verification claimed. Findings: contract/world/craft checks pass (details in the review notes above); three non-blocking craft observations (title-above-board reading, Escape discoverability on the cover, the extended `.plate` dependency). **Disposition: SHIP** (craft feedback only; the run's independent verifier still gates).
- R1 status → done candidate. Evidence: this entry + refinement.md #1.
- Verifier (independent, ultron-supreme — finishing phase, auto mode): 2026-08-29 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 10 files, **190/190 tests passed** (router file 40 = 35 pre-R1 + the 5 new R1 structural tests, all five verified genuine from source at router.test.ts:449–572); `rm -rf dist && npm run build` → exit 0, `tsc --noEmit` strict gate passed, vite 8 clean — css 26.78 kB / js 47.43 kB, exactly the worker's figures; contract seed `da64ca22` still exactly 1 in dist/index.html.
  - Dist/bundle wiring verified: `@import './setup.css'` at src/styles/main.css:12 (test-pinned too); markers surviving minification in the fresh dist — `--dur-arm`, `arm-cover`, `placard-limits`, `placard-name`, `flap-board`, `SESSION LENGTH` in css/js, and the board hook as `dataset.setupBoard` in js (the log's kebab-case `data-setup-board` phrasing is loose but the substance — hook set at router.ts:1018 and surviving the bundle — holds; setup.css styles the board via write.css's `.flap-board`, so no kebab selector is needed).
  - Hooks + token spend verified from source: `.setup .placard` (setup.css:111), `.placard-name` (:143), `.placard-limits` (:172) all carry real rules; `--dur-arm` spent TWICE on the cover hinge (transition at :336, `rotateX(-100deg)` + `visibility` delayed by the token in `[data-open]` at :341–347) — a hinge, never an opacity fade; tokens.css:213 mints `--dur-arm: 260ms` and collapses it to 0.01ms under reduce (:240), so the gesture needs no reduce block. Focus-green 2px via `:has(.placard-radio:focus-visible)` on placards and `.setup button:focus-visible` on controls; contrast.test.ts extended with the setup.css sheet-list entry + the plate focus-pin loop (verified at :275, :311).
  - No UA-default furniture verified: ARM = `plate plate-primary setup-arm-trigger` (router.ts:689), LIFT COVER = `plate arm-cover` (:695), VIEW ARCHIVE = `plate` (:706); the R1 test pins EVERY setup `button` carries `plate`. Guarded ARM keyboard-complete: cover click/Enter → `data-open` + ARM focused; Escape re-covers and hands focus back to the cover; closed state = ARM `tabindex="-1"` yet still live (direct press starts the session — the T7 seam pinned by test); the router's deliberate focus auto-lifts the cover (RE-ARM one-motion loop preserved — pinned). The `tabbables()` test helper's `tabindex="-1"` exclusion honestly models native Tab semantics (browsers skip tabindex=-1 in Tab traversal) and is documented in-code as the one deliberate exception — not a test dodge.
  - T7 seams intact: all seven T7 describes still present and green (radio-group duration + difficulty with printed FADE/LOSS from the imported T2 `PRESETS` table — `preset.fadeDelayMs/1000`, `preset.deleteThresholdMs/1000` at router.ts:656–658, not hardcoded; out-of-range/non-numeric/empty custom blocked with disabled ARM + `aria-invalid` + polite message + recovery; arrow-key roving with wrapping + focus-follows; ARM runs exactly the chosen params; BRUTAL fade math; RangeError backstop; setup→archive navigation). `DURATION_PRESET_MINUTES = [3,5,10,15]` unchanged (timing.ts:58).
  - Reuse-not-fork verified BOTH ways: setup.css contains ZERO `.flap-*` selector rules (grep — only the documenting comment; test-pinned) and `createSetupFlapBoard` (router.ts:1005–1142) mirrors write-surface.ts's `createSlot`/`flipTo` anatomy exactly — `.flap-half`/`.flap-leaf`/`.flap-glyph` shells, `data-glyph` hooks, the four-surface `data-flipping`/`data-reset` settle over the shared `FLIP_SETTLE_MS` export, plus the two additive capability exports (`defaultMotionPreference`/`defaultSupports3d`) — write.css stays the single board owner.
  - Detector rerun MYSELF: plain run `[]` exit 0 on `src/styles/setup.css` + `src/ui/router.ts` (the designed regex-engine path for .css/.ts with per-path project design context — the DEGRADED fallback applies only to the HTML path). To reproduce the worker's full-engine claim I provisioned htmlparser2/css-select/css-tree/domutils via temp npm install + symlinks on the engine's ESM resolution path (removed after): `[]` exit 0 with NO degraded banner on setup.css, main.css, router.ts, write-surface.ts, index.html, dist/index.html AND the built dist css. One observation (non-blocking, outside the claimed set): a whole-project scan flags a regex-literal false positive at router.test.ts:1280 — the CSS-in-JS extractor reads a test's `toMatch(/…font-family…/)` assertion string as a font declaration; it is a TEST file, not production source, and the worker's clean claim never covered it.
  - mtime inventory verified: the R1 window (04:25–04:45) touched EXACTLY the logged changed set — setup.css (NEW), main.css, router.ts, router.test.ts, contrast.test.ts, write-surface.ts, DESIGN.md, .impeccable/design.json — plus the three bookkeeping docs; no unlogged files, no strays (dist is regenerated build output, gitignored).
  - FIRST VIEWPORT contract assessed structurally (no pixel claims — none were made): flap duration board present with FIXED `MMM:SS` wells (5 slots + colon + `T–`), `role="img"` plain-language label, selection echo incl. the honest dashed/not-set state; placard grid prints FADE/LOSS from the preset table; guarded ARM low + isolated (`margin-top:auto`); honest voice throughout ("NO PAUSE — NO RECOVERY", the one-line intro, "Session length not set") — no invented content found.
  - Duration-grouping judgment (the routed decision, assessed): **honors the intent.** The critique's working-memory concern was a flat 5-option list at the first decision; the built world presents 4 numeric switch plates as one tight instrument row plus CUSTOM as a visually distinct FIELD placard (an input, not a fifth equal choice) — limited grouping, not a long menu. The stricter "≤4 total visible options" reading would require deleting a preset or CUSTOM (custom 1–120 is planning-locked and test-pinned), which a styling entry had no authority to decide; the worker disclosed and routed it correctly. The stricter reading remains open for the coordinator at the closing re-critique (entry #5) — carried in state.md, not silently closed here.
  - Worker disclosures verified accurate: finish-reviewer ran as a disclosed in-thread substitution with zero visual claims; the real-browser setup first-viewport capture is honestly owed as follow-up; craft observations (title-above-board, Escape discoverability, `.plate` cross-sheet) correctly routed to the owed capture and polish entry #4.
  - R1 status → done; auto-approved (ultron-supreme). Evidence: this entry (docs/ultron/production-log.md, Refinement R1 verifier block) + docs/ultron/refinement.md #1. Owed follow-ups that survive this PASS: the real-browser setup capture (human), and the ≤4-total-options product question (coordinator, closing re-critique).

### Refinement R2 — the one-motion restart (finishing phase, auto mode)

- **Task:** refinement.md #2 / critique F2 [P2] — RE-ARM was two motions through an amnesiac setup (config reset to 5/STANDARD), breaking the approved journey's "deleted state funnels INSTANTLY into start again — failure feels like a round, not a funeral" (town-hall Product/UX role; PRODUCT.md principle territory: "a fast, shame-free restart after deletion"). Entry discipline: `$impeccable distill`.
- **Changed files:** `src/data/last-config.ts` (NEW — the config-recall store), `src/data/last-config.test.ts` (NEW — 12 unit tests), `src/ui/router.ts` (remember on startSession; reArm() restarts via startSession on the recalled config, deleted-phase guard; renderDeleted passes the recall + RECONFIGURE wiring; doc comments), `src/ui/outcome-screens.ts` (deleted board: RE-ARM plate prints the recalled calibration via exported `reArmPlateLabel`, RECONFIGURE secondary plate, deps + docstrings), `src/ui/router.test.ts` (deletion-loop walkthrough rebuilt as five R2 tests; four existing suites re-based for the new flow + key-filtered storage assertions), `src/styles/outcome.css` (comment only — "the one action" corrected to the two plates), `DESIGN.md` (four passages updated from the built world).
- **The distill (what was removed from the loop):** deleted → [setup: re-read defaults, re-choose duration, re-choose difficulty, LIFT COVER, ARM] → write is now deleted → RE-ARM → write. One gesture on the plate that is ALREADY focused (Enter-equivalent), zero re-configuration, fresh editor, threat from the top, hands on the text (editor focused exactly like ARM — the restart is a session start, not a navigation).
- **The recall mechanism (small, tested):** `createLastConfigStore(storage?)` — `remember(config)` on every `startSession` (the only path that arms), `recall()` returns the config or null. In-memory per page-load is authoritative; a best-effort persisted copy under its OWN versioned key `the-disappearing-draft:last-config` (`{version:1, preset, minutes}`, mirroring archive.ts's envelope discipline — separate key, never the drafts' home; config is NOT user content, the bounded, disclosed exception to ephemerality). Corruption policy narrower than the archive's ON PURPOSE: unparseable / wrong-version / invalid payloads are ignored + one warn line and never quarantined — recall degrades to "nothing on record" and reArm falls back to the setup console (a defensive branch unreachable in today's UI: a deleted board always follows an arm on the same load, which always records — pinned at store level instead). Write failures (quota, hostile context, private mode) are swallowed with a warn; the in-memory record still serves the same load's restart, so the mercy moment NEVER depends on storage health. No draft text can reach the key (preset id + number only).
- **No-surprise rule, honored on the plate:** the write surface stays time-only HUD by commitment — the calibration rides the RE-ARM plate itself, pre-session: `RE-ARM — 7 MIN · GENTLE` (exported `reArmPlateLabel`, single source, test-pinned; plain "RE-ARM" only in the unreachable no-record case). The plate is printed from the SAME recall read path the restart consumes, with no mutation window between (nothing on the deleted board can arm), so the control can never promise a configuration it will not deliver.
- **Setup stays the full configuration surface (constraint a/e, no dead ends):** nothing removed from the setup console; reachable via boot, wind-down DONE → archive → NEW SESSION, and the NEW standard secondary plate RECONFIGURE on the deleted board → setup with ARM focused (the deliberate focus lifts the MASTER ARM cover by itself — R1's seam re-pointed from RE-ARM to RECONFIGURE/NEW SESSION in code comment + DESIGN.md). RECONFIGURE exists because without it, changing difficulty after a deletion would require first SURVIVING a whole session — a trap, not a dead end technically but one in practice; the critique's own provocative question suggested exactly this ("RE-ARM restarted with the last calibration and offered CHANGE as the secondary").
- **Evidence:** `npm run test` **206/206** (baseline 190; +16). New store unit tests (12): round-trip + copy-semantics + newest-wins; own-key versioned envelope + fresh-store (cross-load) read + archive key never touched; write failure swallowed (memory serves, fresh store honestly null); 8 ignored-payload cases (unparseable, wrong version, missing version, unknown preset, minutes 0/121/non-numeric, array) each asserting null recall + bytes left in place + exactly one warn; hostile adapter degrades without throwing. New router walkthrough tests (4 net; the old single deletion walkthrough grew to five): (1) RE-ARM lands in a FRESH armed session — countdown paint = last duration ('7:00'), preset discrimination via fade math (20s idle → opacity 0.5 is ONLY paintable by GENTLE's 10s/30s window; STANDARD/BRUTAL are past deletion at 20s), fresh-editor T4 seam (different node, empty, undo unblocked — also re-pinned in the permanence suite), editor focused, zero draft-writes (every setItem call on LAST_CONFIG_STORAGE_KEY, archive envelope null, removeItem never), loop repeatable; (2) newest-arm-wins (reconfigure mid-loop changes what the next RE-ARM runs — '3 MIN · GENTLE' + 0.75 @15s GENTLE fade math); (3) RECONFIGURE no-dead-ends + full config surface (a completely different STANDARD/2min config arms and SURVIVES to the archive from it); (4) corrupt persisted config never traps (in-memory record serves; store-level null pinned in the unit file); (5) envelope shape pinned at the router seam + fresh-router-over-same-storage boots setup and runs. Re-based existing suites: permanence storage-silence + interceptor (rearm now lands in write; fresh-editor assertions moved onto the re-armed session), archive-untouched + both T13 reload tests (setItem spies re-based to key-filtered DRAFT assertions — the R2 config write is the one disclosed exception), T10 one-sequence test (restart → write, countdown '2:00', editor focused), T12 deletion announcement (second deletion driven through the re-armed session), keyboard deletion loop (tab order [reArm, reconfigure]; RE-ARM → editor in one gesture; RECONFIGURE → ARM). `npm run build` strict-green (css 26.78 kB unchanged — no rule changes; js 49.00 kB from 47.43 kB). Detector: `[]` exit 0 on the full-engine .ts/.css path for last-config.ts, router.ts, outcome-screens.ts, outcome.css (second run incl. outcome.css after the comment edit); dist marker check: `RECONFIGURE`/`reArmPlateLabel`/`the-disappearing-draft:last-config` all survive minification in the built js; dist scan clean in plain mode with the known HTML-path DEGRADED banner (index.html itself untouched this entry — mtime predates the work window; the pre-existing router.test.ts regex-literal false positive at the font assertion is unchanged and documented by R1's verifier). DESIGN.md cross-checked line-by-line against the code (Overview / instrument readout / Navigation / setup-console passages — no stale RE-ARM-to-setup claims remain, grep-verified).
- **Honest limits:** NO real-browser rendering exists in this harness — no capture of the longer RE-ARM plate at 320px or of the two-plate actions row wrapping; zero pixel claims above (structural assertions only). Owed to the SAME human capture session as R1's: deleted board with the calibration plate (esp. a 3-digit custom like "RE-ARM — 120 MIN · STANDARD" — 27 chars vs the ~24-char placard guideline, flex-wrap prevents overflow) and the RECONFIGURE landing.
- **Deviations / routed decisions:** (1) The persisted config READ is unreachable in today's UI (deleted board ⇒ an arm happened this load ⇒ memory serves). Kept per the entry's explicit persistence allowance ("in-memory per page-load is the minimum; localStorage allowed IF consistent with the ephemerality story — separate key, versioned like archive.ts"); cost is one ~40-byte best-effort write per ARM; a future setup-preselect (the critique's provocative question #2) would exercise the read. Routed to the closing re-critique as an open product thought: should RECONFIGURE/boot PRESELECT the last calibration? (This entry kept setup at honest defaults — preselect would grow the change beyond the deleted-board distill.) (2) RECONFIGURE is an ADDITION to the emptied deleted board (DESIGN.md's "only … RE-ARM" sentence amended deliberately with rationale) — the alternative (no secondary) leaves difficulty changes gated behind surviving a full session; judged a trap, and the addition is one standard plate with focus untouched. (3) README not edited (one honest line naming the second localStorage key belongs with polish entry #4's doc seams; logged here).
- **Finish review:** harness has NO agent-spawn tool and NO agent CLI (claude/zcode/codex/gemini/ollama all absent) — the shipped finish-reviewer role ran as a DISCLOSED in-thread substitution, reviewing from code + DESIGN.md + the direction contract with zero visual verification claimed. Checks: locked lamp triad untouched (RE-ARM keeps the bone primary fill, RECONFIGURE the hairline secondary — no new hue); DSEG14 reservation, fixed ground, no-soft-shadows all intact; the plate text rides the same recall the restart consumes (no divergence window); reArm guarded to the deleted phase; startSession reuse gives fresh controller/editor + interceptor disarm for free; recall validates version/preset/minutes; write failures cannot interrupt arming; focus map (RE-ARM first circuit → editor; RECONFIGURE → ARM) and axe suite green over the reworked deleted phase; craft floor: no new motion, controls name action + parameters, states inherited from the plate vocabulary. Three non-blocking craft observations: (a) 27-char worst-case plate vs the ~24 guideline — owed capture; (b) the unreachable persisted-read disclosure (accepted as spec-allowed, honestly logged); (c) setup-still-defaults-on-RECONFIGURE kept as scope discipline, routed to the closing re-critique. **Disposition: SHIP** (craft feedback only; the run's independent verifier still gates).
- R2 status → done candidate. Evidence: this entry + refinement.md #2.
- Verifier (independent, ultron-supreme — finishing phase, auto mode): 2026-08-29 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 11 files, **206/206 tests passed** (baseline 190; the +16 verified genuine from source: 12 store unit tests at src/data/last-config.test.ts — 3 round-trip/copy/newest-wins, 8 parameterized corrupt-foreign cases each asserting null recall + bytes left in place + exactly one warn, 1 hostile adapter; 4 net router walkthrough tests at router.test.ts:673–853 — the old single deletion walkthrough grew to five); `rm -rf dist && npm run build` → exit 0 (`tsc --noEmit` strict gate is part of the script and re-run standalone: PASS), css 26.78 kB / js 49.00 kB — exactly the worker's figures; contract seed `da64ca22` still exactly 1 in dist/index.html.
  - The one-motion claim verified FROM SOURCE: deleted board mounts with RE-ARM focused (`screen.focusReArm()` at router.ts:905, asserted `document.activeElement` in tests), the plate is a plain `plate plate-primary` (NO cover — the LIFT COVER ritual lives only on setup's MASTER ARM), and `reArm()` (router.ts:406–420, guarded `screen !== 'deleted'`) calls `startSession(config)` directly — fresh `renderWrite` (fresh editor node, test-pinned `freshEditor !== editor`, empty, undo unblocked), fresh controller, interceptor disarmed, editor focused. ONE gesture (Enter on the focused plate / one click) → writing. Fade-math preset discrimination re-computed by the verifier from timing.ts's table: GENTLE 10s/30s → 1−10/20 = 0.5 at 20s idle, while STANDARD (5/10s) and BRUTAL (3/6s) are past deletion at 20s — only GENTLE can paint the asserted 0.5; the second walkthrough's 0.75 @15s (GENTLE 1−5/20) likewise checks out. Against the approved journey ("failure feels like a round, not a funeral"; PRODUCT "a fast, shame-free restart after deletion"): nothing on the deleted→write path remains two-motion; the multi-step paths that remain (setup's guarded ARM, RECONFIGURE→setup) are configuration moments, not the restart, and the archive's NEW SESSION→setup was never in F2's scope (preselect on boot/RECONFIGURE honestly routed to the closing re-critique).
  - Recall integrity: `startSession` calls `remember({preset, minutes})` (router.ts:331) — the ONLY write path; the store's API is `LastSessionConfig { preset: PresetId; minutes: number }` and `remember` stringifies exactly `{version, preset, minutes}` — no text field exists for draft content to reach, type-and-runtime-verified; separate versioned key `the-disappearing-draft:last-config`, archive envelope asserted null/byte-identical across the re-based T4/T13 suites (key-filtered draft assertions verified at router.test.ts:914–918, 988–991, 1013–1016); write failures swallowed with one warn (unit test drives QuotaExceededError); corrupt/foreign payloads ignored-not-quarantined. No-divergence claim verified by reasoning: between the plate's recall read (renderDeleted, router.ts:899) and the press's read (reArm, :417) no `startSession` can run (the deleted board's only actions are RE-ARM and RECONFIGURE; the latter swaps screens), and a deleted board always follows an arm on the same load — so the in-memory record (authoritative once set) serves both reads identically; the worker's "persisted-read unreachable in today's UI" disclosure is accurate (renderDeleted is reachable only through a live session, which always remembers first).
  - Setup stays the full configuration surface with no dead ends: T7 suite intact and green inside 206; RECONFIGURE walkthrough proves a completely different STANDARD/2min config arms from the deleted board and SURVIVES to the archive; boot → setup and fresh-router-over-same-storage → setup both test-pinned; wind-down DONE → archive → NEW SESSION path unchanged; `resetToSetup` focuses ARM and the deliberate focus lifts the cover (R1 seam re-pointed, code comment + DESIGN.md line 284 agree).
  - HUD time-only verified: zero occurrences of preset/GENTLE/STANDARD/BRUTAL in src/ui/write-surface.ts — the calibration identity exists only on the pre-session RE-ARM plate (`reArmPlateLabel`, outcome-screens.ts:104–106, single source, test-pinned against the exact string).
  - Detector rerun MYSELF: `node …/detect.mjs --json src/data/last-config.ts src/ui/router.ts src/ui/outcome-screens.ts` → `[]` exit 0 (the designed .ts engine path — no DEGRADED banner; the HTML-parser degradation applies only to HTML targets, none claimed here); `src/styles/outcome.css` → `[]` exit 0. Dist markers survive minification in the fresh build: `RECONFIGURE`, `reArmPlateLabel`, and `the-disappearing-draft:last-config` all present in dist/assets/index-*.js.
  - Documentation cross-checked: DESIGN.md's four passages (Overview :160 "re-arm is one motion straight into the next session on the last-used calibration"; instrument-readout :269 two plates + recall-store sentence; Navigation :281 "editor again on RE-ARM"; setup-console :284 cover-lift crediting RECONFIGURE/NEW SESSION) match the code — grep finds no stale RE-ARM→setup claim; outcome.css's R2 comments (:7, :109) match the built flow.
  - mtime inventory verified: the R2 window (04:55–05:07) touched EXACTLY the logged changed set — last-config.ts + last-config.test.ts (NEW), router.ts, router.test.ts, outcome-screens.ts, outcome.css, DESIGN.md — plus the two bookkeeping docs (refinement.md, production-log.md); index.html untouched (mtime 03:25 predates the window, as disclosed); no unlogged files, no strays.
  - Worker disclosures verified accurate: finish-reviewer in-thread substitution disclosed with zero visual claims (structural assertions only — none over-claimed); the 27-char worst-case plate ("RE-ARM — 120 MIN · STANDARD") honestly flagged for the owed 320px capture; the unreachable persisted-read kept per the entry's explicit persistence allowance and correctly routed as an open product thought (setup preselect); README second-key line routed to polish #4. One judgment recorded (agreeing with the worker): RECONFIGURE as an addition to the emptied board is the right reading of the critique's own provocative question #2 — without it, changing difficulty after a deletion would require first surviving a whole session, a trap worse than a second plate.
  - R2 status → done; auto-approved (ultron-supreme). Evidence: this entry (docs/ultron/production-log.md, Refinement R2 verifier block) + docs/ultron/refinement.md #2. Owed follow-ups that survive this PASS: the deleted-board capture items folded into R1's owed human capture session (calibration plate at 320px incl. the 27-char worst case, RECONFIGURE landing); routed forward: setup-preselect product question (closing re-critique #5), README last-config key line (polish #4).

### Refinement R3 — hardening the irreversible moments (finishing phase, auto mode)

- **Task:** refinement.md #3 / critique F3 [P2] + F4 [P2] — (F3) the disarm swap lands focus on DONE, so a writer still mid-sentence can have their next Space/Enter prematurely finalize the session; (F4) archive single-entry DELETE was immediate/permanent/unconfirmed while CLEAR LOG carried an in-DOM confirm — inconsistent destruction semantics. Entry discipline: `$impeccable harden`, changes surgical (outcome-screens.ts / archive-screen.ts + tests).
- **Changed files:** `src/ui/outcome-screens.ts` (the DONE keystroke guard + DONE_KEY_GUARD_MS export + docstrings), `src/ui/archive-screen.ts` (the two-step DELETE + DELETE_ARM_TIMEOUT_MS export + optional injectable `timer` seam + docstrings), `src/ui/router.ts` (passes its timer seam to the archive screen; two docstring passages), `src/ui/router.test.ts` (+4 guard tests, `passDoneGuard` helper, 8 DONE-click sites re-based, both timers-quiet assertions re-commented, T12 keyboard walkthrough now exercises the fresh-focus intent path), `src/ui/archive-screen.test.ts` (T11 single-delete walkthrough re-based to arm-then-confirm; +4 two-step tests; FakeArmTimers), `src/ui/a11y.test.ts` (two DONE clicks spend the guard beat), `DESIGN.md` (three passages: plates list, The log, Navigation). NO CSS changes — the armed plate reuses the existing `.plate-destruct`.
- **F3 mechanism — (a) chosen, defended:** DONE keeps its T12-pinned disarm focus (the focus map and its tests are untouched and green); what changes is that the plate's KEYBOARD activation is held for 1000ms after disarm (inside the entry's 750–1500 band): Enter/Space keydown+keyup `preventDefault` (activation dies before any synthesized click) and detail-0 clicks — what browsers synthesize for Enter/Space — are swallowed, with ONE honest spoken hold per window. Pointer clicks (detail ≥ 1) always work. A fresh focus on the plate (Tab away and back, screen-reader navigation; the disarm focus itself is flagged and consumed) is deliberate intent and lifts the hold at once; the hold also expires alone. Why not (b) (focus stays on the editor): it breaks the T12 focus map and forces re-verifying/updating the announcement and focus tests to gain nothing (a) doesn't already give — (a) preserves the map and adds intent-detection on top. The 1000ms defense: longer than the tail of a typing burst (60–100 WPM inter-key intervals run 100–250ms), shorter than any deliberate pause before pressing DONE again. Extra entry guard verified and pinned: Enter inside the wind-down textarea can NEVER activate DONE (no `<form>` exists on the console — grep-verified, buttons `type="button"` by construction; new test pins structure + behavior even outside the window).
- **F4 mechanism — the confirm pattern (entry's stated default):** undo window rejected as heavier machinery and inconsistent with the room's existing confirm vocabulary. Single DELETE is a two-step plate: press 1 ARMS in place (the plate morphs to CONFIRM DELETE in the destruct red — existing `.plate-destruct`, `data-armed` hook; the caution is SPOKEN through the polite region, naming the entry title, permanence, and Escape), press 2 on the same native button performs it. Escape anywhere on the binder cancels (focus stays on the plate as it reverts); a 5s lapse auto-reverts — the SAFE direction, so a distracted writer can never return to a still-armed destruct control (test-pinned: a reverted plate re-arms cleanly); one confirm is live at a time (arming another row disarms the first; opening the CLEAR LOG panel cancels a row arm); `renderLog` clears the arm. T11's same-log-position focus continuation preserved and re-pinned. Honest deviation recorded in DESIGN.md: the amber caution BAND is not replicated per row (DESIGN.md reserves the one raised caution panel for the multi-entry decision; the Locked Lamp / hue-rarity rules argue against scattering amber) — the caution beat is the first press + the spoken region, the red is the confirm control: exactly CLEAR LOG's vocabulary scaled to one row.
- **Evidence:** `npm run test` **214/214** (baseline 206; +8 verified genuine from source: 4 guard tests at router.test.ts — hold across Enter/' '/legacy 'Spacebar' with defaultPrevented on keydown AND keyup, detail-0 click swallowed with the hold announced exactly once, pointer click (detail 1) finalizes INSIDE the window; hold expiry; fresh-focus lift; Enter-in-editor + no-form + all-buttons-type-button pins. 4 two-step tests at archive-screen.test.ts — arm/confirm with red class + data-armed + spoken caution + focus kept on the plate + remaining plates revert + the cancelled lapse timer is a no-op; Escape cancel; lapse auto-revert then clean re-arm; one-confirm-live incl. the CLEAR LOG interplay). Re-based existing tests honestly disclosed: the T11 single-delete walkthrough drives arm-then-confirm with continuation assertions unchanged; 8 DONE-click sites (6 router + 2 a11y) spend the deliberate beat via `passDoneGuard` or — where clock math matters — the fresh-focus path (the announcements test's absolute second-disarm time; the T12 keyboard walkthrough, which now models the writer finishing their sentence: Tab to draft, back to DONE); the two 'live loop fully down' timers assertions now spend the beat first with a comment (at disarm itself the ONE pending timer is the guard — the threat engine is down at the swap). `npm run build` strict-green, css **26.78 kB byte-identical** (no CSS rule changes) / js 49.00→50.32 kB. Detector: `[]` exit 0 (full .ts engine path, no degraded banner) on src/ui/outcome-screens.ts, src/ui/archive-screen.ts, src/ui/router.ts. Dist markers survive minification: CONFIRM DELETE ×2, 'DONE holds keypresses…', 'Delete armed for…', 'Delete cancelled.' all present in dist/assets/index-*.js. mtime inventory: exactly the logged set (05:18–05:25) — three production files, three test files, DESIGN.md, plus bookkeeping; index.html and all CSS untouched this entry.
- **Honest limits:** NO real-browser rendering exists in this harness — no pixel claims anywhere (the armed plate reuses the existing destruct-plate styling, so nothing new is owed to R1's capture session; a keyboard-hold feel-test at a real browser belongs to that same human pass, noted). Two disclosed judgment calls: (1) the detail-0 swallow also holds AT-synthesized clicks inside the 1s window — deliberate AT activation is preceded by navigation focus events which lift the guard, so the worst case is 1s; (2) on the quota-failure disarm path the announcement says 'Press DONE to retry' while the guard holds keys for 1s — the honest hold announcement names the recovery.
- **Finish review:** harness has NO agent-spawn tool and NO agent CLI — the shipped finish-reviewer role ran as a DISCLOSED in-thread substitution, reviewing from code + DESIGN.md + the direction contract with zero visual verification claimed. Checks passed: entry-mechanism compliance (a) as specified with the 1000ms defense at the constant; (b) correctly rejected with the T12 reasoning; F4 confirm-pattern per the entry default with the amber deviation recorded in DESIGN.md; locked lamp triad intact (red only on CONFIRM controls, no new hue, zero CSS delta); textContent-only safety contract holds (user-derived title reaches only the polite region as text); seams injectable, no test-only hooks; T11 continuation and T12 focus map verified unchanged. Non-blocking craft feedback: the two disclosed judgment calls above. **Disposition: SHIP** (craft feedback only; the run's independent verifier still gates).
- R3 status → done candidate. Evidence: this entry + refinement.md #3.
- Verifier (independent, ultron-supreme — finishing phase, auto mode): 2026-08-29 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 11 files, **214/214 tests passed** (exit 0; router 48, archive-screen 18, a11y 8 among them; the canvas-getContext and quota-stderr lines are the same disclosed jsdom noise as prior verifier runs); `npm run build` → exit 0 (`tsc --noEmit` strict gate is part of the script), css 26.78 kB / js 50.32 kB — exactly the worker's figures, css unchanged from R2's 26.78 kB.
  - F3 verified FROM SOURCE (outcome-screens.ts:441–497): `focusDone()` sets `doneKeyboardHeld`, resets `heldAnnounced`, schedules the expiry on the INJECTABLE timer (`DONE_KEY_GUARD_MS = 1000`, exported with the WPM-interval defense at :248–257), flags `disarmFocusPending`, then focuses — so the disarm focus itself is consumed by the focus listener (:473–479) and only a LATER focus on the plate lifts the hold; keydown AND keyup preventDefault Enter/' '/legacy 'Spacebar' while held (:463–472); clicks with `detail === 0` are swallowed + announced ONCE per window, everything else (pointer, detail ≥ 1) finalizes (:480–487). Guard scope is exactly the window: expired or lifted, keydown passes (`defaultPrevented` false, test-pinned). Pointer path structurally unaffected — the click handler's only gate is `doneKeyboardHeld && event.detail === 0`. The router calls `screen.focusDone()` at the disarm swap (router.ts:943) and nothing else arms the guard — no other DONE site exists (RE-ARM/RECONFIGURE are separate controls with plain handlers).
  - No-form / implicit-submit pin verified BOTH ways: grep over production src finds `form` only in docstrings (no `createElement('form')`, no `<form>` markup anywhere); tests pin structure at THREE sites (router.test.ts:888 editor, :1546 wind-down section + `closest('form')` null, write-surface.test.ts:175) and BEHAVIOR outside the window (Enter keydown in the wind-down textarea: not prevented, phase stays wind-down, no hold announcement — router.test.ts:1539–1557); every button on the console asserted `type="button"`.
  - The 4 guard tests verified genuine (router.test.ts:1476–1558): real dispatched `KeyboardEvent`s with `defaultPrevented` asserted on keydown AND keyup for all three key names; jsdom `.click()` (detail 0) swallowed with the phase still wind-down and the spoken hold asserted; a `MouseEvent('click', {detail: 1})` finalizes INSIDE the window (pointer-passes pin); expiry via `passDoneGuard` (clock advance DONE_KEY_GUARD_MS + 1 + `fireDue` — the honest beat, not a bypass); fresh-focus lift with NO clock advance (textarea.focus → done.focus → Enter passes). `surviveToDone` asserts `document.activeElement === done` at disarm with the comment "T12 focus map intact — (a), not (b)".
  - F4 verified FROM SOURCE (archive-screen.ts:488–568): the arm state is `armedEntryId` (the id, never button text — rows rebuild unarmed via `renderLog` → `cancelDeleteArm(false)` at :621); first press arms in place (`CONFIRM DELETE` text + `plate-destruct` class + `data-armed` hook — the EXISTING clear-all destruct class, zero CSS delta, corroborated by no .css file in the mtime window), second press on the same native button deletes (keyboard-complete by construction — native `type="button"`); the caution is SPOKEN through the polite region naming the entry title + 'no recovery' + 'Escape cancels' (:530–533) — the announcement carries the user-derived title as textContent in a live region, inert by the module's safety contract; Escape on the section cancels with focus untouched on the plate (:543–545); 5s lapse auto-reverts via the injectable timer (`DELETE_ARM_TIMEOUT_MS = 5000`, :96–103) and the cancelled timer is a no-op (timer.cancel before re-arm/confirm, test-pinned by `fireAll` after confirm deleting nothing); one-confirm-live both ways (arming row B disarms row A; `openConfirm` cancels a row arm — :585); T11 same-log-position focus continuation PRESERVED (removeEntry focuses next-else-previous DELETE else NEW SESSION on empty, :547–568) and re-pinned by the re-based walkthrough including the empty-log landing.
  - The 4 two-step tests verified genuine (archive-screen.test.ts:534–644), plus the re-based T11 walkthrough (:376–418) driving arm-then-confirm with the continuation assertions unchanged. `FakeArmTimers` is a test-local fake over the PRODUCTION `timer` seam the router really passes (router.ts:954–961, own comment) — no test-only hook in production code.
  - Re-based DONE sites honest: `passDoneGuard` used at every pre-existing finalize site in router.test.ts + the two a11y sites (`h.clock.now += DONE_KEY_GUARD_MS + 1` + fireDue); the T12 keyboard walkthrough now models the writer finishing the sentence (Tab to draft, BACK to DONE — the fresh focus lifts the hold, no clock advance, router.test.ts:1771–1781); the announcements test uses the fresh-focus path to keep its absolute second-disarm time exact (comment at :1674–1676); BOTH 'live loop fully down' assertions (:617–619, :1293–1298) spend the beat first with the honest comment that the one pending timer at disarm is the guard, the threat engine being down at the swap itself.
  - T12 a11y contract: focus map UNCHANGED (DONE still `document.activeElement` at disarm — asserted, not relaxed); all 8 a11y tests green inside 214, axe clean over wind-down WITH the guard active (expectAxeClean runs before the beat is spent).
  - DESIGN.md's three passages match the code: Primary plate list carries the disarm-guard clause (:255), the Destruct plate list reads "CONFIRM CLEAR, CONFIRM DELETE" (:256), The log's two-step paragraph (:275) ends with the amber-deviation rationale verbatim in spirit — "the amber caution band stays reserved for the multi-entry decision; the single row's caution is spoken, not painted" — plus the Navigation disarm-guard clause (:281). The amber omission is a DOCUMENTED deviation with a world-rule argument (Locked Lamp rarity; the one raised caution panel reserved to clear-all), consistent with CLEAR LOG's vocabulary scaled to one row — not an accident; the caution beat is real (first press + spoken region naming title/permanence/Escape) and the confirm control is red per lamp semantics, so F4's intent (confirm-before-destruction) is fully met.
  - The two non-blocking calls JUDGED — neither is a trap. (1) detail-0 swallow vs AT-synthesized clicks: bounded ≤1s from disarm only, ANNOUNCED through the polite region with both recoveries named ("click now, or press again shortly"), lifted by any fresh focus (SR quick-nav/Tab/VO navigation moves real focus onto the plate), and the failure direction is a DELAY to an irreversible finalize — the exact accident F3 exists to prevent; the draft survives either way. Residual worst case (browse-mode activation with no fresh focus inside 1s): one extra press with a spoken explanation. (2) 1s guard vs the quota-retry announcement ("Press DONE to retry", router.ts:990/993): the overlap requires finalizing within 1s of disarm (pointer/lifted key) AND retrying by keyboard inside the same second; the quota path preserves the draft in the editor (finishWindDown returns with the note, router.ts:401–404, test-pinned), the hold line names its own recovery, and the second press succeeds — no data-loss path exists at any point of the overlap. Exempting the retry would couple the guard to router state for a sub-second corner: worse complexity, no safety gain.
  - Detector rerun MYSELF: `node …/detect.mjs --json src/ui/outcome-screens.ts src/ui/archive-screen.ts src/ui/router.ts` → `[]`, exit 0 (the designed .ts engine path — no DEGRADED banner). Dist markers survive minification in the fresh build: `CONFIRM DELETE` ×2 (occurrence-counted), 'DONE holds keypresses for a moment after disarm', 'Delete armed for', 'Delete cancelled.' all present in dist/assets/index-CXMaoN4T.js.
  - mtime inventory verified: the R3 window (05:18–05:25) touched EXACTLY the logged set — outcome-screens.ts, archive-screen.ts, router.ts (production), router.test.ts, archive-screen.test.ts, a11y.test.ts (tests), DESIGN.md — plus the three bookkeeping docs; ZERO .css files, index.html untouched — the "css byte-identical, no production CSS changes" claim corroborated the strong way.
  - Sidecar staleness NOTED (not fixed here — belongs to the closing document refresh): `.impeccable/design.json` (mtime 04:35, the R1 window) predates R3; it does not yet describe the CONFIRM DELETE two-step plate or the disarm guard. DESIGN.md itself is current. Carried forward for the closing refresh alongside the README last-config-key line already routed to polish #4.
  - R3 status → done; auto-approved (ultron-supreme). Evidence: this entry (docs/ultron/production-log.md, Refinement R3 verifier block) + docs/ultron/refinement.md #3. Owed follow-ups that survive this PASS: the keyboard-hold feel-test note rides R1's owed human capture session. Follow-up for the closing document refresh: design.json sidecar staleness (above).

### Refinement R4 — polish: closing the P3 seam list (finishing phase, auto mode)

- **Task:** refinement.md #4 / critique P3 seam list + two follow-ups routed from R2 — (1) cross-sheet `.plate` dependency (definition in archive.css, consumption by outcome + setup), (2) the `--focus-width` token (T11's logged follow-up: the 2px focus literal shared by three sheets), (3) `theme-color` + favicon absent from index.html, (4) the stray `' '` text node, (5) README storage-key documentation for `the-disappearing-draft:last-config`, (6) the RE-ARM plate's 27-char worst case at the 320px floor. Entry discipline: `$impeccable polish` — refinement, never concealed redesign; every change is a token-for-literal substitution of an identical computed value, a relocation, or an addition in the world's own vocabulary.
- **Changed files (mtime window verified, exactly this set):** `src/styles/plates.css` (NEW — the shared plate layer), `src/styles/archive.css` (plate block extracted; header + section comments re-anchored), `src/styles/main.css` (imports plates.css first among sheets; header comment names the shared-furniture principle), `src/styles/tokens.css` (`--focus-width: 2px` minted beside `--focus-color`), `src/styles/write.css` (editor band consumes the token), `src/styles/outcome.css` (ring tokenized; RE-ARM safe-measure rule; header re-anchored), `src/styles/setup.css` (three ring sites tokenized; header re-anchored), `index.html` (theme-color + inline SVG favicon), `src/ui/outcome-screens.ts` + `src/ui/write-surface.ts` (dead whitespace text nodes removed), `README.md` (second storage key; stale suite count 185→218; layout line), tests: `src/styles/contrast.test.ts`, `src/ui/router.test.ts`, `tests/smoke.test.ts`.
- **(1) The plate seam — closed at the narrowest correct level:** the component was DESIGNED once ("Every action button on every styled screen is the same small plate" — DESIGN.md Components) but LIVED in archive.css while the outcome and setup screens spent it — the exact dependency class polish.md calls a one-off implementation. Move: the full vocabulary (`.plate` base/hover/active/disabled, `.plate-primary` + hover, `.plate-destruct` + hover, the `data-state=copied|failed` transient lamps, and the ≤40rem tightening that had been silently styling OTHER screens' plates from inside archive.css) now lives in `src/styles/plates.css`, imported FIRST among sheets in main.css — before write/outcome/archive/setup — because the import order is load-bearing: the shared layer plays the exact cascade position archive.css used to play for setup.css, so per-screen refinements (`.setup-arm-trigger` sizing, `.arm-cover` colors) keep winning at equal specificity, byte-for-byte unchanged rendering. This is the same principle main.css already documents for `.vh` ("the document-level definition so no screen depends on another's sheet"); tokens.css was rejected as the home (its own charter: values only, screens consume). Import order + component completeness + no screen-sheet redefinition are TEST-PINNED (new router test).
- **(2) `--focus-width` — minted and consumed everywhere:** `--focus-width: 2px` sits in tokens.css beside `--focus-color` with the DESIGN.md rationale (the 2px constant focus band; the same value every ring draws and the editor's leading band carries as its WCAG 2.4.11 indicator). Consumers verified six in source + all in the built CSS: `.archive button:focus-visible`, `.outcome button:focus-visible`, `.setup button:focus-visible`, `.setup .placard:has(.placard-radio:focus-visible)`, `.setup-custom-input:focus-visible`, and `.write-column`'s `border-left` — the editor band consumes the SAME token (its width is the 2.4.11 judgment, constant across transparent→green→amber). Contrast tests updated to pin the token (`--focus-width: 2px` in tokens.css, every sheet's ring consuming it, zero `outline: 2px solid` literals left) — the T12 pin's strength preserved, now pointing at the single source.
- **(3) Browser chrome carries the room:** `<meta name="theme-color" content="#14161a" />` — the exact `--ground` value from tokens.css (cross-checked BY TEST, not by hand-copy), so the mobile browser's chrome paints the room's own ground. Favicon: hand-authored inline-SVG data URI in the world's vocabulary — the signature silhouette at 16px, one recessed split-flap digit well (`#0e1013`) with the 1px hinge seam (`#14161a`) across its middle and a bone (`#e8e4d8`) digit, on the raised bezel (`#1a1d23`) with machined corners; every hex is a tokens.css value (asserted by test), no external asset, no network fetch (matches the README's no-third-party-requests claim). No raster provenance owed — it is drawn geometry, four rects.
- **(4) The stray text node:** the critique's specific one ("between setup buttons") no longer exists — R1's renderSetup rebuild removed it (verified: `section.append(heading, intro, boardHead, durationGroup, presetGroup, errorLine, armStation, archiveButton)` carries no text nodes). The grep the entry sanctioned found the remaining three: whitespace-only `' '` children in flex containers (`outcome-screens.ts` stats strip ×2, `write-surface.ts` reduced-motion caution count) — dead by spec (css-flexbox §4: whitespace-only anonymous flex items are never rendered), orphan nodes kept alive for nothing. Removed with zero visual change (the flex `gap` was and remains the separation); no test asserted them.
- **(5) README:** the ephemerality section now names BOTH keys with their exact source values (`the-disappearing-draft:archive`, `the-disappearing-draft:last-config`) and states the routed framing verbatim in substance — the config key is not user content, no draft text ever reaches it, clearing it only resets the recalled calibration (RE-ARM falls back to picking settings again). Also corrected while in the file: the stale suite count (185 — true at T14, stale since R2) → 218, and the project-layout line now names the shared plate layer. No other claims touched.
- **(6) The RE-ARM plate's static safe measure — computed, not rendered:** worst case is code-bounded at `RE-ARM — 120 MIN · STANDARD` (27 chars = 3-digit minutes × longest preset). Math (documented in the rule itself, outcome.css): Michroma caps at `--size-placard-sm` 10px with 0.12em tracking after every glyph → 3.24em tracking alone; per-class glyph advances (caps ≈0.33–1.0em avg ≈0.75, digits ≈0.7, space ≈0.26, em-dash ≈1.0, middot ≈0.28) sum ≈17.2em → ≈20.4em with tracking → ≈204px of text at the token size. The bed budget at the 320px floor, from the sheets' own ≤40rem blocks: `.outcome` sides 2×12 + `.outcome-instrument` sides 2×12 + phone-tightened plate padding 2×8 + borders 2×1 = 66px → 254px of text room — the token size fits with ≈20% slack, so nothing changes at any supported width. The measure makes that fit structural instead of incidental: `font-size: min(var(--size-placard-sm), calc((100vw - 66px) / 22))` (divisor 22em = computed width + ~8% pad) caps the label's rendered width at (100vw − 66px), so face-metric drift or sub-floor viewports trade type size for fit; and because nothing sets `white-space:nowrap`, the structural backstop remains — the plate can wrap before it can ever overflow. Scoped to `.outcome-deleted .plate-primary` alone (the only plate whose text length is bounded by this math). Detector interaction, resolved not ignored: the first detector pass flagged the initial `clamp(8px, …)` endpoint as an off-ramp type size (advisory) — the floor literal was removed so the ramp's 10px token is the only size the plate ever claims; the re-run is clean.
- **Evidence:** `npm run test` **218/218** (baseline 214; +4, each asserting behavior: the contrast suite's new `--focus-width` pin — token value, consumption in all three ring sheets + both extra setup sites, no literal left; the router suite's plates-layer pin — import order before all four screen sheets, component completeness incl. the ≤40rem tightening, no screen-sheet redefinition, no stale cross-references; two smoke tests on the shell — theme-color equals the ground token exactly, favicon is an inline SVG whose every hex is a tokens.css value incl. the recessed-well rect). `npm run build` strict-green, zero warnings — css 27.01 kB (26.78 + the plates relocation, focus token, and one scoped rule) / js 50.30 kB (50.32 − the three dead text nodes). Dist verified: theme-color + favicon present in dist/index.html, contract seed da64ca22 still exactly 1, all five ring sites + the editor band consume `var(--focus-width)` in the built CSS, the min() rule survives minification, `outline:2px solid` literals zero. Detector: first pass (DEGRADED regex mode — parser modules absent, honestly noted) surfaced the 8px advisory, fixed as above; then FULL-ENGINE run by provisioning htmlparser2/css-select/css-tree/domutils via temp npm install + symlink on the engine's ESM resolution path (removed after, directory verified clean) → `[]` exit 0, NO degraded banner, across all ten changed production targets.
- **Honest limits:** NO real-browser rendering exists in this harness — zero pixel claims above. Owed to the already-owed human capture session (R1's, which already carries R2's deleted-board items): the favicon's crispness at 16px, the theme-color in a real tab strip, and the RE-ARM plate at 320px (now with arithmetic behind it, not just flex-wrap hope). The Michroma advances are reasoned per class, not extracted from the binary — mitigated by the 8%-padded divisor, shrink-before-wrap ordering, and the wrap backstop; the capture closes the residual.
- **Out of entry scope, deliberately untouched and routed to the closing re-critique (#5):** the critique's two remaining P3 crumbs were NOT in this entry's six routed items — `.archive-body` (an unstyled semantic wrapper, harmless, archive-screen.ts:225) and the confirm-settle opacity animation (letter-of-rule tension; token-duration-driven so it collapses under reduce — documented by T11/T12). Also still open from R3's verifier: the `.impeccable/design.json` sidecar refresh.
- **Finish review:** harness has NO agent-spawn tool and NO agent CLI (verified absent across this run) — the shipped finish-reviewer role ran as a DISCLOSED in-thread substitution, reviewing from code + DESIGN.md + the direction contract with zero visual verification claimed. Checks: OWN-WORLD intact (favicon/theme-color spend only committed palette hexes, no new hue; no elevation, ground, or type changes — the only values moved are equal-value tokenizations); DESIGN.md prose remains literally true everywhere it names 2px focus or the one plate vocabulary (the code now single-sources both, matching the document's claims better than before); craft floor's browser-surfaces check closed (selection/scrollbars/caret were themed T8–T12; theme-color + favicon were the last unthemed chrome); no banned patterns (the favicon is drawn geometry — four rects — not a unicode/emoji stand-in); README claims all sourced from code constants; scope discipline — six routed items, six closed, the un-routed crumbs left documented rather than smuggled. Non-blocking craft feedback: the reasoned-not-measured Michroma advances (owed capture), and the favicon's seam-vs-digit legibility at 16px (same capture). **Disposition: SHIP** (craft feedback only; the run's independent verifier still gates).
- R4 status → done candidate. Evidence: this entry + refinement.md #4.
- Verifier (independent, ultron-supreme — finishing phase, auto mode): 2026-08-29 — **PASS**.
  - Commands run independently (not trusting worker output): `npm run test` → green — 11 files, **218/218 tests passed** (exit 0; the canvas-getContext and quota-stderr lines are the same disclosed jsdom noise as prior verifier runs); `npm run build` → exit 0 (`tsc --noEmit` strict gate is part of the script, zero warnings), css **27.01 kB** / js **50.30 kB** — exactly the worker's figures (css +0.23 from R3's 26.78, js −0.02 from 50.32 — both deltas arithmetically consistent with the claimed additions: the token declaration + 6 var() substitutions + the min() rule on one side, the three dead text nodes on the other).
  - Plates seam verified from the sheets themselves: main.css import order is tokens → **plates** → write/outcome/archive/setup — plates.css FIRST among the screen sheets exactly as claimed (the router test pins it before all four screen sheets, router.test.ts:587–610, together with component completeness incl. the ≤40rem tightening, `^\.plate` NOT redefined in archive.css, and no stale cross-references). The CASCADE claim independently reasoned, not just trusted: the only rules in any screen sheet that touch the vocabulary are `.setup-arm-trigger` and `.arm-cover` in setup.css (specificity 0,1,0 — setup.css follows plates.css, so they keep winning exactly as they did when archive.css held the block, which also preceded setup.css) and outcome.css's two plate rules, which are HIGHER-specificity compounds (`.outcome-deleted .plate-primary` 0,2,0; `.outcome-windown .outcome-actions .plate-primary` 0,3,0) that won before AND after regardless of order — the one order change the move could have caused (plates now preceding outcome.css, where archive.css followed it) flips nothing because outcome.css contains no equal-specificity `.plate` rule. The ≤40rem tightening is a document-global bare `.plate` rule wherever its sheet sits, so its relocation is render-neutral. No-redefinition grep clean: zero `.plate`/`.plate-primary`/`.plate-destruct` vocabulary rules in write/outcome/archive/setup.
  - `--focus-width` verified at ALL six sites in source and all in dist: tokens.css:130 `--focus-width: 2px` beside `--focus-color`; consumed at archive.css:83, outcome.css:41, setup.css:213/262/354 (the `:has` placard ring + custom-input ring among them) + write.css:243 (the editor's leading band, `border-left: var(--focus-width) solid transparent` — the 2.4.11 indicator sharing the one token); `outline: 2px` / `outline-width: 2px` literals: **ZERO in src AND in the built CSS** (grep exit 1 both); the built sheet carries exactly five `outline: var(--focus-width) solid var(--focus-color)` rules + the one border-left — six, matching source one-for-one. Contrast pin verified genuine (contrast.test.ts:312–326: token value pinned, per-sheet ring assertions, `not.toMatch(/outline:\s*2px solid/)` on all three ring sheets, both extra setup sites).
  - Shell verified: `<meta name="theme-color" content="#14161a">` = the exact `--ground` (tokens.css:85) — and the smoke test cross-checks BY MATCHING the token, not a hand-copied constant (smoke.test.ts:19–23); the favicon is a hand-authored inline-SVG data URI (decoded and inspected: FOUR rects — bezel `#1a1d23` = --panel-raised, recessed well `#0e1013` = --panel-inset, bone stroke digit `#e8e4d8` = --bone, hinge seam `#14161a` = --ground — every hex a tokens.css value, asserted per-hex by smoke.test.ts:25–40, no external asset, no network fetch). Dist carries theme-color + the favicon data-URI + contract seed `da64ca22` exactly 1 in both source and dist/index.html; the min() rule survives minification verbatim (`.outcome-deleted .plate-primary{font-size:min(var(--size-placard-sm),calc((100vw - 66px)/22))}`).
  - Stray nodes verified gone at source: the only remaining `' '` matches in outcome-screens.ts / write-surface.ts are the removal comments themselves (outcome-screens.ts:375–380 stats strip, write-surface.ts:344–348 caution count) and a legitimate `key === ' '` comparison; both removal sites carry the css-flexbox §4 rationale in-rule. The critique's originally-flagged setup node confirmed absent since R1.
  - README verified against code constants: `the-disappearing-draft:archive` = ARCHIVE_STORAGE_KEY (archive.ts:30), `the-disappearing-draft:last-config` = LAST_CONFIG_STORAGE_KEY (last-config.ts:35) — exact string equality both; the "not user content / clearing resets recalled calibration" framing present; suite count reads 218 = the actual count; layout line names the shared plate layer.
  - RE-ARM math RE-COMPUTED at the 320px floor, arithmetically sane: bed budget from the sheets' own ≤40rem blocks — `.outcome` sides 2×`--sp-3`(12) + `.outcome-instrument` sides 2×`--sp-3`(12) + plate padding sides 2×`--sp-2`(8, the phone tightening now in plates.css) + borders 2×`--hairline`(1) = **66px** → 254px of text room; label ≈20.4em (17.2em reasoned glyph advances + 3.24em = 27 × 0.12 tracking) ≈ **204px** at the 10px token → (254−204)/254 ≈ **19.7% ≈ the claimed 20% slack**; the string is code-bounded at 27 chars ("RE-ARM — 120 MIN · STANDARD" — 120 is the R1-validated max minutes, STANDARD the longest preset); `min()` at 320px yields min(10, (320−66)/22 ≈ 11.55) = 10px — the token wins at every supported width, so nothing changes, and the ramp only trades size for fit below ~286px; divisor 22 = 20.4em + ~7.8% pad as documented. Backstop confirmed: no `white-space:nowrap` on any plate path (all nowrap rules target numeric readouts / placard-limit spans / `.vh` clips) and `.outcome-actions` keeps `flex-wrap: wrap` — the plate can wrap before it can overflow. The detector-advisory resolution verified in-rule: no clamp floor endpoint exists, `--size-placard-sm` is the only size claimed.
  - No-redesign review (no VCS in this project — reviewed from content + mtime): plates.css contains ONLY the extracted vocabulary, every value a pre-existing token (`translateY(1px)` micro-press included) plus the moved ≤40rem block; the screen sheets' diffs are the six token substitutions of identical computed values, comment re-anchoring, and one ADDITIVE scoped rule (the min()); index.html gained only the head pair; the UI modules lost only the dead nodes. mtime inventory: the R4 window (05:43:27–05:47:53) touched EXACTLY the logged 14 files — seven stylesheets + index.html + outcome-screens.ts + write-surface.ts + README.md + the three test files — no strays, DESIGN.md untouched (05:25, R3-era — correct: its 2px-focus and one-plate prose stays literally true with the code now single-sourcing both).
  - Detector rerun MYSELF, full engine: htmlparser2/css-select/css-tree/domutils temp-installed and symlinked onto detect.mjs's ESM resolution path (removed after; skill scripts dir and /tmp verified clean) → **`[]`, exit 0, NO degraded banner, on ALL TEN changed production targets** (plates.css, archive.css, main.css, tokens.css, write.css, outcome.css, setup.css, index.html, outcome-screens.ts, write-surface.ts); the plain-mode run without parsers reproduces the worker's honestly-disclosed DEGRADED regex fallback, matching its disclosure.
  - Worker disclosures verified accurate: no-browser harness honestly noted with the owed capture items (favicon 16px crispness, theme-color in a real tab strip, RE-ARM at 320px) folded into R1's session; Michroma advances reasoned-not-measured, disclosed with mitigations; the two un-routed P3 crumbs (`.archive-body`, confirm-settle opacity) and the design.json sidecar refresh correctly left documented for #5 rather than smuggled in. Finish-reviewer in-thread substitution disclosed with zero visual claims.
  - R4 status → done; auto-approved (ultron-supreme). Evidence: this entry (docs/ultron/production-log.md, Refinement R4 verifier block) + docs/ultron/refinement.md #4. Owed follow-ups that survive this PASS (all folded into R1's owed human capture session): favicon at 16px, theme-color in a real tab strip, RE-ARM plate at 320px. Routed forward to #5: the `.archive-body` wrapper + confirm-settle opacity crumbs, design.json sidecar refresh.

### Refinement R5 — closing re-critique (finishing phase, auto mode) — PENDING
